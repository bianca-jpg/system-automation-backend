---
phase: 02-ingest-o-da-refer-ncia-convers-o-de-grade
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/modules/ingestao/application/casos_uso.py
  - app/modules/ingestao/schemas.py
  - app/modules/ingestao/service.py
  - app/tests/test_ingestao_sync.py
autonomous: true
requirements: [ING-01]

must_haves:
  truths:
    - "A 5ª fonte (referência tamanho->posição) grava produto_tamanho_posicao a partir da view Databricks (critério 1)"
    - "sincronizar_tudo chama a 5ª fonte dentro do mesmo commit único das outras 4 — uma falha em qualquer etapa anterior impede a 5ª de rodar e nada é persistido (critério 2)"
    - "Duas sincronizações sucessivas substituem completamente produto_tamanho_posicao sem duplicar linhas (critério 3)"
    - "Quando a view devolve 0 linhas válidas, o snapshot anterior de produto_tamanho_posicao permanece intacto e um warning é logado (D-02)"
    - "O campo faturamento_colecoes, antes descartado silenciosamente pela resposta HTTP (bug confirmado em 02-RESEARCH.md), agora aparece — junto do campo novo referencia_tamanho_posicao"
  artifacts:
    - path: "app/modules/ingestao/application/casos_uso.py"
      provides: "sincronizar_referencia_tamanhos (guard D-02) + sincronizar_tudo estendido (5ª etapa antes do commit)"
      contains: "async def sincronizar_referencia_tamanhos"
    - path: "app/modules/ingestao/schemas.py"
      provides: "SincronizacaoResponse com faturamento_colecoes (fix) e referencia_tamanho_posicao (novo)"
      contains: "referencia_tamanho_posicao: int = 0"
    - path: "app/tests/test_ingestao_sync.py"
      provides: "5 testes de integração da 5ª fonte + orquestração estendida para 5 etapas"
      min_lines: 60
  key_links:
    - from: "app/modules/ingestao/application/casos_uso.py::sincronizar_tudo"
      to: "app/modules/ingestao/application/casos_uso.py::sincronizar_referencia_tamanhos"
      via: "5ª chamada, imediatamente antes de await db.commit()"
      pattern: "referencia_tamanho_posicao = await sincronizar_referencia_tamanhos"
    - from: "app/modules/ingestao/routes.py::sincronizar"
      to: "app/modules/ingestao/schemas.py::SincronizacaoResponse"
      via: "SincronizacaoResponse(status=\"success\", **resultado)"
      pattern: "referencia_tamanho_posicao"
---

<objective>
Ligar a 5ª fonte (fundação do Plano 02-01) ao caso de uso `sincronizar_tudo` — com o guard de referência vazia (D-02) — e ao contrato HTTP de resposta, provando os 3 primeiros critérios de sucesso da Fase 2 (ING-01) por teste de integração automatizado (sessão real, nunca commitada).

Purpose: sem esta ligação, a 5ª fonte existe mas nunca roda no sync de 2h — `produto_tamanho_posicao` continuaria vazia para sempre. De carona (arquivo `schemas.py` já sendo editado nesta fase, decisão do orquestrador — usuária não respondeu ao Open Question 1 do RESEARCH.md), corrige o bug pré-existente e confirmado de `faturamento_colecoes` ausente na resposta HTTP.

Output: `sincronizar_referencia_tamanhos` (D-02) integrada ao commit único; `SincronizacaoResponse` com os 2 campos corretos; 5 testes de integração novos + orquestração de 5 etapas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-CONTEXT.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-RESEARCH.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-PATTERNS.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<decisoes_travadas>
1. **Guard D-02 fica no caso de uso, nunca no repositório.** `substituir_referencia_tamanhos` (Plano 02-01) permanece incondicional — quem decide SE ela é chamada é `sincronizar_referencia_tamanhos`. Ver `02-RESEARCH.md` Pitfall 3.
2. **Interpretação do guard:** dispara quando `not agrupado` — ou seja, tanto "a view devolveu 0 linhas" quanto "a view devolveu linhas, mas todas foram descartadas pelo parse D-01" contam como "0 linhas válidas" (D-02). Esta leitura é mais protetora que "view devolveu literalmente 0 linhas" e está alinhada com a razão de ser da decisão (nunca zerar a grade E1..E48 por erro na origem).
3. **Contador quando o guard dispara (Open Question 2 do RESEARCH.md):** retorna a contagem ATUAL de `produto_tamanho_posicao` (`SELECT COUNT(*)`), não `0` — mais consistente com o significado dos outros 4 contadores ("quantos itens existem agora").
4. **Bug de carona (Open Question 1 do RESEARCH.md):** `faturamento_colecoes: int = 0` é adicionado a `SincronizacaoResponse` nesta fase, mesmo não fazendo parte do escopo original de ING-01/LINX-03 — decisão do orquestrador (usuária não respondeu), justificada por ser 1 linha no mesmo arquivo já em edição.
</decisoes_travadas>

<tasks>

<task type="auto">
  <name>Task 1: sincronizar_referencia_tamanhos (D-02) + sincronizar_tudo estendido + contrato HTTP</name>

  <files>app/modules/ingestao/application/casos_uso.py, app/modules/ingestao/schemas.py, app/modules/ingestao/service.py</files>

  <read_first>
    - `app/modules/ingestao/application/casos_uso.py` (arquivo inteiro — `sincronizar_faturamento_colecao`, linhas 88-99, e `sincronizar_tudo`, linhas 102-114)
    - `app/modules/ingestao/schemas.py` (arquivo inteiro, 8 linhas — `SincronizacaoResponse` atual, sem `faturamento_colecoes`)
    - `app/modules/ingestao/service.py` (barrel atual, 13 linhas)
    - `app/modules/ingestao/models.py` (classe `ProdutoTamanhoPosicao`)
    - `02-RESEARCH.md` seção "Pattern 1" (corpo completo do guard D-02) e "Pitfall 1" (bug confirmado por execução direta do `SincronizacaoResponse`)
    - `02-01-SUMMARY.md` — confirma que `agregar_referencia_tamanhos`, `ler_referencia_tamanhos`, `substituir_referencia_tamanhos` já existem e suas assinaturas exatas
  </read_first>

  <action>
    Em `casos_uso.py`: adicionar aos blocos de import existentes — `agregar_referencia_tamanhos` (de `domain.agregacao`), `ler_referencia_tamanhos` (de `infrastructure.databricks_reader`), `substituir_referencia_tamanhos` (de `infrastructure.repositorio_snapshot`); adicionar `from sqlalchemy import func, select` (novo import de topo, ainda não existe neste arquivo) e `from app.modules.ingestao.models import ProdutoTamanhoPosicao`.

    Implementar `async def sincronizar_referencia_tamanhos(db: AsyncSession) -> int`: `linhas = await ler_referencia_tamanhos()`; `agrupado, descartadas, conflitos_por_produto = agregar_referencia_tamanhos(linhas)`. SE `not agrupado` (D-02 — cobre tanto "0 linhas brutas" quanto "todas descartadas pelo parse"): logar warning com a mensagem EXATA de `02-RESEARCH.md` Pattern 1 (linhas 172-177: "Referência tamanho->posição: 0 linhas válidas (%d linhas brutas) - mantendo snapshot anterior de produto_tamanho_posicao.", com `len(linhas)` como único argumento); consultar `atual = await db.scalar(select(func.count()).select_from(ProdutoTamanhoPosicao)) or 0`; `return atual` — SEM chamar `substituir_referencia_tamanhos` (o repositório nunca é invocado neste ramo). SE `agrupado` não vazio: `await substituir_referencia_tamanhos(db, agrupado.values())`; iterar `conflitos_por_produto.items()` logando, por produto (nunca por linha), `logger.warning("produto %s: conflito de posição na referência: %s", cd_prod_cor, avisos)` (D-03/D-04); logar o resumo `logger.info("Referência tamanho->posição sincronizada: %d itens (%d linhas brutas, %d descartadas).", len(agrupado), len(linhas), descartadas)`; `return len(agrupado)`.

    Estender `sincronizar_tudo`: adicionar `referencia_tamanho_posicao = await sincronizar_referencia_tamanhos(db)` como 5ª chamada, na mesma posição sequencial das outras (depois de `faturamento`, ANTES de `await db.commit()`); adicionar a chave `"referencia_tamanho_posicao": referencia_tamanho_posicao` ao dict de retorno; atualizar a docstring de `sincronizar_tudo` para mencionar as 5 fontes.

    Em `schemas.py`: adicionar `faturamento_colecoes: int = 0` (correção do bug de carona confirmado em `02-RESEARCH.md` Pitfall 1 — decisão do orquestrador registrada em `02-CONTEXT.md`) e `referencia_tamanho_posicao: int = 0` (campo novo da 5ª fonte) à classe `SincronizacaoResponse`, ambos com um comentário citando a razão (bug pré-existente / campo novo).

    Em `app/modules/ingestao/service.py`: adicionar `sincronizar_referencia_tamanhos` ao import de `application.casos_uso` e ao `__all__`.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run python -c "from app.modules.ingestao.schemas import SincronizacaoResponse; r = SincronizacaoResponse(status='success', pedidos_inseridos=1, estoque_chaves=2, processados_erp=3, faturamento_colecoes=4, referencia_tamanho_posicao=5); d = r.model_dump(); assert d['faturamento_colecoes'] == 4 and d['referencia_tamanho_posicao'] == 5; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - O comando `<automated>` acima imprime `OK` (prova que o bug do Pitfall 1 está corrigido e o campo novo existe e não é descartado)
    - `grep -c "async def sincronizar_referencia_tamanhos" app/modules/ingestao/application/casos_uso.py` retorna `1`
    - `grep -B3 "await db.commit()" app/modules/ingestao/application/casos_uso.py | grep -c "referencia_tamanho_posicao = await sincronizar_referencia_tamanhos"` retorna `1` (a 5ª chamada acontece imediatamente antes do commit)
    - `grep -c "sincronizar_referencia_tamanhos" app/modules/ingestao/service.py` retorna `2` (import + `__all__`)
    - `grep -A15 "async def sincronizar_referencia_tamanhos" app/modules/ingestao/application/casos_uso.py | grep -c "substituir_referencia_tamanhos"` retorna `1` (chamada existe só no ramo `else`/depois do guard, não duplicada)
  </acceptance_criteria>

  <done>5ª fonte integrada ao commit único de `sincronizar_tudo`; guard D-02 nunca chama o repositório quando a agregação está vazia, devolvendo a contagem atual; contrato HTTP inclui os dois campos (bug corrigido + campo novo).</done>
</task>

<task type="auto">
  <name>Task 2: Testes de integração da 5ª fonte (ING-01 critérios 1-3, D-01, D-02)</name>

  <files>app/tests/test_ingestao_sync.py</files>

  <read_first>
    - `app/tests/test_ingestao_sync.py` (arquivo inteiro, já lido — `sincronizar_estoque`/`sincronizar_faturamento_colecao` como molde de teste de integração, e `test_sincronizar_tudo_orquestra_as_quatro_etapas_e_soma_resultado`, linhas 259-285, a ser estendido)
    - `02-RESEARCH.md` seção "Code Examples" (esqueleto pronto do teste D-02, linhas 456-478)
    - `02-VALIDATION.md` — Per-Task Verification Map (mapeia estes casos aos critérios do ROADMAP)
  </read_first>

  <action>
    Adicionar `ProdutoTamanhoPosicao` ao import existente de `app.modules.ingestao.models` no topo do arquivo (junto de `Estoque, Pedido, PedidoProcessadoErp`).

    Adicionar 5 testes novos, na mesma seção estilística das demais fontes (usar `_mock_consulta` já existente no arquivo):

    1. `test_sincronizar_referencia_tamanhos_tabela_nao_configurada_levanta_erro` — espelha `test_sincronizar_estoque_tabela_nao_configurada_levanta_erro`, com `fake_settings = MagicMock(databricks_tabela_tamanho_ref="")`.

    2. `test_sincronizar_referencia_tamanhos_sql_exato` — espelha `test_sincronizar_estoque_sql_exato`; `esperado = f"SELECT cd_prod_cor, sg_tamanho, nr_posicao FROM {tabela}"`; assert `kwargs == {}` (sem `timeout_s`, mesmo padrão de `ler_estoque`).

    3. `test_sincronizar_referencia_tamanhos_persiste_e_descarta_invalidas` (D-01, critério 1) — linhas com `nr_posicao` válido (ex.: `"5"`, `"12"`) e inválido (não-numérico `"abc"`, fora de faixa `"0"` e `"49"`); usar `async_session_factory()` sem commit; `total = await service.sincronizar_referencia_tamanhos(session)`; assert `total` conta só as válidas; consultar `ProdutoTamanhoPosicao` e confirmar que só as linhas válidas foram persistidas; `await session.rollback()` ao final (full refresh nunca deve tocar dado real do ambiente — mesmo padrão dos testes irmãos).

    4. `test_sincronizar_referencia_tamanhos_view_vazia_mantem_snapshot_anterior` (D-02) — usar o esqueleto exato de `02-RESEARCH.md` Code Examples: `session.add(ProdutoTamanhoPosicao(cd_prod_cor="AAA1|001", sg_tamanho="M", nr_posicao=5))` + `await session.flush()`; mockar a view devolvendo `[]`; chamar `await service.sincronizar_referencia_tamanhos(session)`; assert a linha pré-existente continua lá (query por `cd_prod_cor == "AAA1|001"`) e `total == 1` (contagem atual, não `0`); `await session.rollback()`.

    5. `test_sincronizar_referencia_tamanhos_duas_chamadas_sucessivas_substitui_sem_duplicar` (critério 3) — dentro do MESMO `async_session_factory()` sem commit: 1ª chamada mockada com 2 linhas válidas de um produto A; `await session.flush()`; assert `COUNT(*)` (via `select(func.count()).select_from(ProdutoTamanhoPosicao)`) `== 2`; 2ª chamada mockada com 1 linha válida de um produto B (diferente); `await session.flush()`; assert `COUNT(*) == 1` (nunca `3`) e que as linhas do produto A não existem mais; `await session.rollback()` ao final.

    Estender `test_sincronizar_tudo_orquestra_as_quatro_etapas_e_soma_resultado` → renomear para `test_sincronizar_tudo_orquestra_as_cinco_etapas_e_soma_resultado`: adicionar o 5º mock `patch("app.modules.ingestao.application.casos_uso.sincronizar_referencia_tamanhos", new=AsyncMock(return_value=4)) as m5`; adicionar `m5.assert_awaited_once_with(db_fake)`; adicionar `"referencia_tamanho_posicao": 4` ao dict `resultado` esperado (mantendo `"faturamento_colecoes": 6` já existente).

    NÃO modificar `test_sincronizar_tudo_nao_persiste_nada_se_uma_etapa_falhar` — a falha simulada ocorre na 2ª etapa (`sincronizar_estoque`), antes da 5ª etapa (referência) ser alcançada; o teste permanece correto sem alteração.

    6. `test_sincronizar_tudo_nao_persiste_nada_se_referencia_falhar` (critério 2, warning do plan-checker) — NOVO teste espelhando o item anterior, mas com a exceção simulada DENTRO da 5ª etapa: `patch("app.modules.ingestao.application.casos_uso.sincronizar_referencia_tamanhos", new=AsyncMock(side_effect=RuntimeError("boom")))`; chamar `sincronizar_tudo` e assertar que a exceção propaga E que nenhuma das 4 fontes anteriores foi persistida (mesmo mecanismo de verificação do teste irmão). Prova diretamente que uma falha na fonte NOVA não deixa as outras 4 parcialmente sincronizadas.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_sync.py -q</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` mostra todos os testes do arquivo (existentes + novos) em `passed`, `0 failed`
    - `grep -c "^async def test_sincronizar_referencia_tamanhos" app/tests/test_ingestao_sync.py` retorna `5`
    - `grep -c "test_sincronizar_tudo_orquestra_as_cinco_etapas_e_soma_resultado" app/tests/test_ingestao_sync.py` retorna `1` (renomeado, não duplicado)
    - `grep -c "test_sincronizar_tudo_orquestra_as_quatro_etapas" app/tests/test_ingestao_sync.py` retorna `0` (nome antigo não existe mais)
    - `grep -c "test_sincronizar_tudo_nao_persiste_nada_se_referencia_falhar" app/tests/test_ingestao_sync.py` retorna `1` (teste novo do critério 2)
    - Regressão: `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) sai com código 0
  </acceptance_criteria>

  <done>Os critérios de sucesso 1-3 do ROADMAP da Fase 2 e os desvios D-01/D-02 estão cobertos por teste de integração automatizado, sem tocar dado real (nenhum commit), e a suíte completa permanece verde.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Artefatos que ESTE PLANO entrega (visão cumulativa completa da Fase 2 no Plano 02-05):

| Artefato | Tipo | Onde |
|----------|------|------|
| `sincronizar_referencia_tamanhos` | função assíncrona (caso de uso, guard D-02) | `app/modules/ingestao/application/casos_uso.py` |
| `sincronizar_tudo` (estendido) | função assíncrona (orquestração, 5ª etapa) | `app/modules/ingestao/application/casos_uso.py` |
| `SincronizacaoResponse.faturamento_colecoes` | campo Pydantic (correção de bug pré-existente) | `app/modules/ingestao/schemas.py` |
| `SincronizacaoResponse.referencia_tamanho_posicao` | campo Pydantic (novo, 5ª fonte) | `app/modules/ingestao/schemas.py` |
| re-export `sincronizar_referencia_tamanhos` | barrel de import externo | `app/modules/ingestao/service.py` |
| `test_sincronizar_referencia_tamanhos_*` (5 testes) + `test_sincronizar_tudo_orquestra_as_cinco_etapas_e_soma_resultado` | teste de integração | `app/tests/test_ingestao_sync.py` |

Consumidores a jusante: `POST /api/v1/ingestao/sincronizar` (rota já existente, sem mudança de contrato de auth) passa a devolver `referencia_tamanho_posicao` e `faturamento_colecoes` corretamente; o Celery beat de 2h passa a manter `produto_tamanho_posicao` atualizada, que a Fase 3 lê para converter grades via `converter_grade_para_posicoes` (Plano 02-02).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Databricks (view) → Postgres, via commit único | `sincronizar_tudo` já é uma superfície de confiança estabelecida (4 fontes existentes); a 5ª fonte entra no mesmo boundary, sem mudança de modelo de confiança. |
| `POST /api/v1/ingestao/sincronizar` (rota existente) | Já protegida por `Depends(require_admin)`; nenhuma mudança de autenticação/autorização nesta fase — só o corpo da resposta ganha 2 campos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service (interno) | `sincronizar_referencia_tamanhos` — view devolve 0 linhas válidas por erro de origem | mitigate | Guard D-02: nunca substitui a referência com resultado vazio; snapshot anterior preservado, coberto por teste de integração. |
| T-02-07 | Information Disclosure | Resposta HTTP de `/sincronizar` | accept | Só expõe contadores inteiros (quantas linhas foram sincronizadas), nenhum dado de cliente/produto — mesmo padrão das 4 fontes existentes. |
| T-02-01 | Tampering | Nome de tabela interpolado via f-string (herdado do Plano 02-01) | accept | Reafirmado: valor de env var controlada pela equipe, não input HTTP. |
| T-02-SC | Tampering | npm/pip/cargo installs | N/A | Nenhum pacote novo instalado nesta fase. |
</threat_model>

<verification>
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_sync.py -q` → todos os casos (existentes + novos) em `passed`
2. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) → verde, sem regressão
3. Verificação funcional completa (contra Postgres/Databricks reais) fica para o Plano 02-04 (checkpoint) — os testes deste plano provam a LÓGICA com mocks, não substituem a prova end-to-end real.
</verification>

<success_criteria>
- `sincronizar_tudo` chama a 5ª fonte dentro do commit único, na posição certa (antes de `await db.commit()`) — critério 2
- Duas sincronizações sucessivas substituem sem duplicar (critério 3), provado por teste
- View vazia (ou 100% descartada) nunca zera `produto_tamanho_posicao` (D-02), provado por teste
- `SincronizacaoResponse` expõe `faturamento_colecoes` e `referencia_tamanho_posicao` corretamente
- Suíte completa verde
</success_criteria>

<output>
Criar `.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-03-SUMMARY.md` ao terminar, registrando: (a) os 5 testes novos + o teste de orquestração renomeado e seus resultados, (b) a mensagem exata do warning D-02, (c) confirmação de que o bug de `faturamento_colecoes` foi corrigido.
</output>
