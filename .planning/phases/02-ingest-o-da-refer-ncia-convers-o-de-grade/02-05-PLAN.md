---
phase: 02-ingest-o-da-refer-ncia-convers-o-de-grade
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - app/tests/test_grade_linx.py
autonomous: true
requirements: [LINX-03]

must_haves:
  truths:
    - "A função pura de conversão é testada com dados reais já ingeridos nesta fase, sem tocar banco no teste (critério 4 do ROADMAP)"
  artifacts:
    - path: "app/tests/test_grade_linx.py"
      provides: "teste do critério 4 com fixture de dados reais capturados no Plano 02-04, sem I/O"
      contains: "dados reais"
  key_links:
    - from: "app/tests/test_grade_linx.py"
      to: "02-04-SUMMARY.md"
      via: "valores da fixture _REFERENCIA_REAL copiados literalmente da evidência capturada via psql"
      pattern: "_REFERENCIA_REAL"
---

<objective>
Fechar o critério 4 da Fase 2 ("função pura testada sem acesso a banco usando os dados reais já ingeridos nesta fase"): adicionar a `test_grade_linx.py` (criado no Plano 02-02) o teste que usa a amostra REAL capturada no checkpoint do Plano 02-04 — nunca valores inventados.

Purpose: este plano é intencionalmente pequeno porque está inteiramente condicionado à evidência humana do Plano 02-04. Se o Plano 02-04 retornou `sem-acesso` (sem credenciais Databricks reais disponíveis), este plano NÃO deve ser executado com valores inventados — ver `<scope_reduction_prohibition>`: nenhuma fixture "placeholder" substitui dados reais quando o ROADMAP exige explicitamente dados reais. Neste caso, registre o bloqueio no SUMMARY e aguarde o Plano 02-04 ser refeito.

Output: 5º teste em `test_grade_linx.py`, usando a fixture real; nota de bloqueio (deixada no Plano 02-02) removida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-CONTEXT.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-02-SUMMARY.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture de dados reais e teste do critério 4 (LINX-03)</name>

  <files>app/tests/test_grade_linx.py</files>

  <read_first>
    - `app/tests/test_grade_linx.py` — criado no Plano 02-02: os 4 testes sintéticos + a nota no topo apontando para este plano
    - `.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-04-SUMMARY.md` — evidência capturada no checkpoint (linhas `cd_prod_cor, sg_tamanho, nr_posicao` reais, via `psql`)
    - `.planning/ROADMAP.md` — critério 4 da Fase 2, texto exato ("testada sem acesso a banco usando os dados reais já ingeridos nesta fase")
  </read_first>

  <action>
    PRIMEIRO: verificar se `02-04-SUMMARY.md` registrou o bloqueio `sem-acesso`. Se sim, PARAR — não escrever nenhuma fixture inventada; registrar no SUMMARY deste plano que a execução está pendente do checkpoint do Plano 02-04 ser refeito com credenciais reais, e não tocar `test_grade_linx.py`.

    Se `02-04-SUMMARY.md` tem a evidência real: ler as linhas `cd_prod_cor, sg_tamanho, nr_posicao` capturadas. Em `test_grade_linx.py`, adicionar duas constantes de módulo (antes dos testes, depois dos imports): `_CD_PROD_COR_REAL` com o `cd_prod_cor` real capturado, e `_REFERENCIA_REAL` — dict `{sg_tamanho: nr_posicao}` — com os pares REAIS capturados (transcrição literal, nunca arredondar/inventar), ambas com um comentário de uma linha citando a origem ("capturado via psql no Plano 02-04, sync real de {data}").

    Adicionar `test_converter_grade_para_posicoes_com_dados_reais_da_referencia`: montar uma `grade` sintética simples usando 2-3 dos `sg_tamanho` REAIS capturados (quantidades arbitrárias, ex.: `{tam1: 2, tam2: 10}` usando os nomes reais de tamanho da fixture, não "P"/"M" genéricos se os reais forem diferentes); chamar `converter_grade_para_posicoes(grade, _REFERENCIA_REAL, _CD_PROD_COR_REAL)`; para cada tamanho testado, `assert posicoes[f"e{_REFERENCIA_REAL[tam]}"] == grade[tam]`; `assert ignorados == []`.

    Remover a linha de comentário `# NOTA: o teste com dados REAIS ingeridos (critério 4 do ROADMAP) é adicionado no Plano 02-05...` deixada no Plano 02-02 — o critério 4 está coberto agora.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_grade_linx.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Se `02-04-SUMMARY.md` tem evidência real: a saída do comando `<automated>` mostra `5 passed` (4 sintéticos do Plano 02-02 + este novo), `0 failed`
    - `grep -c "NOTA: o teste com dados REAIS" app/tests/test_grade_linx.py` retorna `0` (nota removida)
    - `grep -c "_REFERENCIA_REAL" app/tests/test_grade_linx.py` retorna pelo menos `2` (definição + uso no teste)
    - Regressão: `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa da Fase 2) sai com código 0
    - Se `02-04-SUMMARY.md` registrou `sem-acesso`: nenhum arquivo é alterado; o SUMMARY deste plano registra o bloqueio explicitamente
  </acceptance_criteria>

  <done>O critério 4 do ROADMAP está provado com dados reais já ingeridos pela própria Fase 2, sem qualquer acesso a banco no teste, e a nota de bloqueio foi removida — OU o bloqueio está documentado explicitamente sem fixture inventada.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Artefatos que este plano entrega — visão CUMULATIVA COMPLETA da Fase 2 (Planos 02-01 a 02-05 somados):

| Artefato | Tipo | Onde |
|----------|------|------|
| `databricks_tabela_tamanho_ref` | campo de config (`DATABRICKS_TABELA_TAMANHO_REF`) | `app/shared/config/settings.py`, `.env.example` (02-01) |
| `_parse_posicao` | função pura (parse tolerante, D-01) | `app/modules/ingestao/domain/traducao_databricks.py` (02-01) |
| `agregar_referencia_tamanhos` | função pura (dedup + conflito D-03/D-04) | `app/modules/ingestao/domain/agregacao.py` (02-01) |
| `ler_referencia_tamanhos` | função assíncrona (reader HTTP) | `app/modules/ingestao/infrastructure/databricks_reader.py` (02-01) |
| `substituir_referencia_tamanhos` | função assíncrona (repositório incondicional) | `app/modules/ingestao/infrastructure/repositorio_snapshot.py` (02-01) |
| `converter_grade_para_posicoes` | função pura de domínio | `app/modules/pedidos/domain/grade_linx.py` (02-02, novo arquivo) |
| re-export em `pedidos/service.py` | barrel de import externo | `app/modules/pedidos/service.py` (02-02) |
| `sincronizar_referencia_tamanhos` | função assíncrona (caso de uso, guard D-02) | `app/modules/ingestao/application/casos_uso.py` (02-03) |
| `sincronizar_tudo` (estendido) | função assíncrona (orquestração, 5ª etapa) | `app/modules/ingestao/application/casos_uso.py` (02-03) |
| `SincronizacaoResponse.faturamento_colecoes` / `.referencia_tamanho_posicao` | campos Pydantic | `app/modules/ingestao/schemas.py` (02-03) |
| re-export em `ingestao/service.py` | barrel de import externo | `app/modules/ingestao/service.py` (02-03) |
| Evidência real (COUNT antes/depois, amostra `cd_prod_cor`) | evidência de verificação | `02-04-SUMMARY.md` (02-04, checkpoint) |
| `test_parse_posicao` (7 casos) | teste parametrizado | `app/tests/test_ingestao_parse.py` (02-01) |
| `test_agregar_referencia_tamanhos_*` (3 testes) | teste puro | `app/tests/test_ingestao_sync.py` (02-01) |
| `test_sincronizar_referencia_tamanhos_*` (5 testes) + orquestração de 5 etapas | teste de integração | `app/tests/test_ingestao_sync.py` (02-03) |
| `test_converter_grade_para_posicoes_*` (4 testes sintéticos + 1 com dados reais) | teste puro (sem I/O) | `app/tests/test_grade_linx.py` (02-02 + 02-05, novo arquivo) |

Consumidores a jusante: a Fase 3 (Escrita da OR no formato Linx) usa `converter_grade_para_posicoes` (via `app.modules.pedidos.service`) e lê `produto_tamanho_posicao` (mantida pela 5ª fonte) para preencher `e1..e48` de `ordens_reserva_linx` a cada geração de OR.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Evidência humana (Plano 02-04) → fixture de teste | A única entrada externa deste plano é texto colado por uma pessoa (não uma fonte automatizada) — risco de transcrição, não de segurança de rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Fixture de teste divergindo dos dados reais por erro de transcrição | mitigate | `acceptance_criteria` exige citar a origem exata (`02-04-SUMMARY.md`) e proíbe explicitamente valores inventados; o critério de aceite falha (fixture ausente) se o Plano 02-04 não tiver evidência real. |
| T-02-SC | Tampering | npm/pip/cargo installs | N/A | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_grade_linx.py -q` → `5 passed` (ou bloqueio documentado, sem alteração de arquivo, se `02-04` retornou `sem-acesso`)
2. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa da Fase 2) → verde
</verification>

<success_criteria>
- Critério 4 do ROADMAP provado com dados reais já ingeridos nesta fase, sem acesso a banco no teste — OU bloqueio documentado sem fixture inventada
- Suíte completa da Fase 2 verde (todos os Planos 02-01 a 02-05)
- Todos os 5 critérios de sucesso do ROADMAP da Fase 2 e os requirements ING-01/LINX-03 cobertos
</success_criteria>

<output>
Criar `.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-05-SUMMARY.md` ao terminar, registrando: (a) a fixture real usada (ou o bloqueio `sem-acesso`, se aplicável), (b) confirmação de que a suíte completa da Fase 2 está verde, (c) checklist final dos 5 critérios de sucesso do ROADMAP da Fase 2.
</output>
