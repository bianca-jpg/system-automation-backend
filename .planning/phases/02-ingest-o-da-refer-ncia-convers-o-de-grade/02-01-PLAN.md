---
phase: 02-ingest-o-da-refer-ncia-convers-o-de-grade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/shared/config/settings.py
  - .env.example
  - app/modules/ingestao/domain/traducao_databricks.py
  - app/tests/test_ingestao_parse.py
  - app/modules/ingestao/domain/agregacao.py
  - app/modules/ingestao/infrastructure/databricks_reader.py
  - app/modules/ingestao/infrastructure/repositorio_snapshot.py
  - app/tests/test_ingestao_sync.py
autonomous: true
requirements: [ING-01]

must_haves:
  truths:
    - "agregar_referencia_tamanhos descarta linhas com nr_posicao fora de 1..48 sem lançar exceção (D-01)"
    - "Duas linhas do mesmo produto com sg_tamanho diferentes apontando pra mesma nr_posicao geram 1 aviso agregado por produto, sem interromper a agregação, e AMBAS as linhas permanecem no resultado (D-03/D-04)"
    - "substituir_referencia_tamanhos permanece incondicional (sem guard, sem commit próprio) — simétrico aos 3 repositórios de snapshot irmãos"
  artifacts:
    - path: "app/modules/ingestao/domain/traducao_databricks.py"
      provides: "_parse_posicao — valida nr_posicao (1..48), devolve None para descarte (D-01)"
      contains: "def _parse_posicao"
    - path: "app/modules/ingestao/domain/agregacao.py"
      provides: "agregar_referencia_tamanhos — dedup por (cd_prod_cor, sg_tamanho) + detecção de conflito de posição (D-03/D-04)"
      contains: "def agregar_referencia_tamanhos"
    - path: "app/modules/ingestao/infrastructure/databricks_reader.py"
      provides: "ler_referencia_tamanhos — SELECT cd_prod_cor, sg_tamanho, nr_posicao da view Databricks"
      contains: "def ler_referencia_tamanhos"
    - path: "app/modules/ingestao/infrastructure/repositorio_snapshot.py"
      provides: "substituir_referencia_tamanhos — DELETE+INSERT incondicional, sem commit"
      contains: "def substituir_referencia_tamanhos"
  key_links:
    - from: "app/modules/ingestao/domain/agregacao.py"
      to: "app/modules/ingestao/domain/traducao_databricks.py"
      via: "import direto de _parse_posicao"
      pattern: "_parse_posicao"
    - from: "app/modules/ingestao/infrastructure/repositorio_snapshot.py"
      to: "app/modules/ingestao/models.py"
      via: "db.add_all(ProdutoTamanhoPosicao(**item) for item in itens)"
      pattern: "ProdutoTamanhoPosicao\\("
---

<objective>
Construir a fundação da 5ª fonte do sync Databricks (referência tamanho→posição, ING-01): env var da tabela origem, parse tolerante de `nr_posicao` (D-01), agregação com detecção de conflito de posição agregada por produto (D-03/D-04), reader HTTP e repositório de substituição incondicional — seguindo exatamente o molde das 4 fontes existentes (`ler_faturamento_colecao` / `agregar_estoque` / `substituir_faturamento_colecao`).

Purpose: sem esta fundação não há como a 5ª fonte entrar no commit único de `sincronizar_tudo` (Plano 02-03) nem como a Fase 3 preencher `e1..e48`. Este plano NÃO liga a 5ª fonte ao caso de uso nem ao contrato HTTP — isso é o Plano 02-03, que depende deste.

Output: `_parse_posicao`, `agregar_referencia_tamanhos`, `ler_referencia_tamanhos`, `substituir_referencia_tamanhos` implementadas e testadas (parse via `test_ingestao_parse.py`; agregação, D-01/D-03/D-04, via teste puro novo em `test_ingestao_sync.py`, sem tocar banco).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-CONTEXT.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-RESEARCH.md
@.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Env var da 5ª fonte (settings + .env.example)</name>

  <files>app/shared/config/settings.py, .env.example</files>

  <read_first>
    - `app/shared/config/settings.py` (campo `databricks_tabela_pedidos_processados`, linhas 79-83) — analog exato a copiar
    - `.env.example` (bloco Databricks, linhas 27-43) — onde a linha nova entra
    - `02-CONTEXT.md` seção `code_context` — nome do env var já travado: `DATABRICKS_TABELA_TAMANHO_REF`
  </read_first>

  <action>
    Em `app/shared/config/settings.py`, dentro da classe `Settings`, adicionar o campo `databricks_tabela_tamanho_ref: str` logo depois de `databricks_tabela_pedidos_processados` (antes de `ingestao_intervalo_segundos`), com `Field(default="", validation_alias="DATABRICKS_TABELA_TAMANHO_REF")` — mesmo padrão de `Field` dos 4 campos `databricks_tabela_*` já existentes. Adicionar um comentário de uma linha acima explicando: "Referência tamanho->posição da grade Linx (view programa_estagio.refined.system_automation_prod_tamanho_ref) — 5ª fonte do sync (Fase 2)".

    Em `.env.example`, adicionar a linha `DATABRICKS_TABELA_TAMANHO_REF=catalogo.schema.tamanho_ref` no bloco Databricks, imediatamente após `DATABRICKS_TABELA_PEDIDOS_PROCESSADOS=...` e antes de `INGESTAO_INTERVALO_SEGUNDOS=...`, com um comentário de uma linha citando a view real.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run python -c "from app.shared.config.settings import Settings; assert 'databricks_tabela_tamanho_ref' in Settings.model_fields; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - O comando `<automated>` acima imprime `OK`
    - `grep -c "DATABRICKS_TABELA_TAMANHO_REF" .env.example` retorna `1`
    - `grep -c "databricks_tabela_tamanho_ref" app/shared/config/settings.py` retorna `1`
  </acceptance_criteria>

  <done>`Settings` expõe `databricks_tabela_tamanho_ref` (default `""`, lido de `DATABRICKS_TABELA_TAMANHO_REF`); `.env.example` documenta a nova variável no mesmo bloco das outras tabelas Databricks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse tolerante de nr_posicao (D-01)</name>

  <files>app/modules/ingestao/domain/traducao_databricks.py, app/tests/test_ingestao_parse.py</files>

  <read_first>
    - `app/modules/ingestao/domain/traducao_databricks.py` (`_parse_int`, linhas 5-11) — base a reaproveitar
    - `app/tests/test_ingestao_parse.py` (`test_parse_int`, linhas 38-53) — molde de parametrize a seguir
    - `02-CONTEXT.md` decisão D-01: nr_posicao inválido (não-numérico, <1, >48) descartado na tradução, SEM CheckConstraint no banco
    - `02-RESEARCH.md` seção "Pattern 2" (corpo completo de `_parse_posicao`)
  </read_first>

  <behavior>
    - `_parse_posicao(5)` == `5` (dentro da faixa)
    - `_parse_posicao(1)` == `1` e `_parse_posicao(48)` == `48` (limites inclusivos)
    - `_parse_posicao(0)` é `None` (abaixo do mínimo)
    - `_parse_posicao(49)` é `None` (acima do máximo)
    - `_parse_posicao("abc")` é `None` (não numérico — `_parse_int` já devolve `0` para isso, que cai fora da faixa)
    - `_parse_posicao(None)` é `None`
    - `_parse_posicao("5,0")` == `5` (reaproveita a tolerância de `_parse_int` a vírgula decimal, trunca)
  </behavior>

  <action>
    Implementar `def _parse_posicao(v) -> int | None` em `traducao_databricks.py`, imediatamente após `_parse_int`: chama `n = _parse_int(v)` e retorna `n if 1 <= n <= 48 else None`. Documentar em docstring de uma linha por que o valor seguro é `None` e não `0` (D-01: `0` seria uma posição real inválida e mascararia o descarte — o chamador, em `agregacao.py`, usa `is None` para decidir descarte).

    Em `test_ingestao_parse.py`, adicionar `_parse_posicao` ao bloco de imports de `app.modules.ingestao.domain.traducao_databricks` e um novo `test_parse_posicao` com `@pytest.mark.parametrize`, cobrindo os 7 casos do `<behavior>` acima, no mesmo estilo de `test_parse_int`.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_parse.py -q -k parse_posicao</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` mostra `7 passed` (uma linha por caso do parametrize) e `0 failed`
    - `grep -c "def _parse_posicao" app/modules/ingestao/domain/traducao_databricks.py` retorna `1`
  </acceptance_criteria>

  <done>`_parse_posicao` existe, nunca lança exceção, e os 7 casos parametrizados (faixa válida, limites, abaixo/acima da faixa, não numérico, `None`, vírgula decimal) passam.</done>
</task>

<task type="auto">
  <name>Task 3: Agregação com conflito de posição (D-03/D-04), reader e repositório da 5ª fonte</name>

  <files>app/modules/ingestao/domain/agregacao.py, app/modules/ingestao/infrastructure/databricks_reader.py, app/modules/ingestao/infrastructure/repositorio_snapshot.py, app/tests/test_ingestao_sync.py</files>

  <read_first>
    - `app/modules/ingestao/domain/agregacao.py` (`agregar_estoque`, linhas 58-73, e imports de `_parse_*`, linhas 1-12) — analog de dedup+contagem
    - `app/modules/ingestao/infrastructure/databricks_reader.py` (`ler_estoque`, linhas 32-42) — analog de reader sem filtro
    - `app/modules/ingestao/infrastructure/repositorio_snapshot.py` (docstring do arquivo, linhas 1-3, e `substituir_faturamento_colecao`, linhas 47-58) — repositório incondicional, sem commit
    - `app/modules/ingestao/models.py` (classe `ProdutoTamanhoPosicao`, criada na Fase 1)
    - `02-RESEARCH.md` seção "Pattern 3" (corpo completo de `agregar_referencia_tamanhos`, com a estrutura auxiliar `posicao_por_produto`) e "Pitfall 2" (conflito de posição ≠ tamanho duplicado) e "Pitfall 3" (guard D-02 NÃO entra aqui — fica reservado para o Plano 02-03/caso de uso)
    - `02-CONTEXT.md` decisões D-01, D-03, D-04
  </read_first>

  <action>
    Em `agregacao.py`: adicionar `_parse_posicao` ao bloco de imports de `traducao_databricks`. Implementar `def agregar_referencia_tamanhos(linhas: list[dict]) -> tuple[dict[tuple, dict], int, dict[str, list[str]]]`: itera `linhas`, extrai `cd = str(linha.get("cd_prod_cor") or "").strip()`, `tam = str(linha.get("sg_tamanho") or "").strip().upper()`, `pos = _parse_posicao(linha.get("nr_posicao"))`; descarta (incrementa `descartadas`) quando `not cd or not tam or pos is None` (D-01); mantém um índice reverso auxiliar `posicao_por_produto: dict[str, dict[int, str]]` (via `defaultdict(dict)`) e, quando `pos` já está ocupado por um `sg_tamanho` DIFERENTE do mesmo `cd`, registra em `conflitos_por_produto[cd]` (via `defaultdict(list)`) o aviso no formato EXATO `f"{tamanho_anterior}/{tam}->pos{pos}"` (onde `tamanho_anterior` é o valor já presente em `posicao_por_produto[cd][pos]`) — D-03; em seguida sobrescreve `posicao_por_produto[cd][pos] = tam` (último processado vence) e grava/sobrescreve `agrupado[(cd, tam)] = {"cd_prod_cor": cd, "sg_tamanho": tam, "nr_posicao": pos}` — nota: o conflito NÃO remove nenhuma das duas linhas de `agrupado` (Pitfall 2: ambas continuam presentes, cada uma na sua própria chave `(cd, tam)`; o conflito só importa quando a Fase 3 usar a referência para converter uma grade real). Retorna `(agrupado, descartadas, dict(conflitos_por_produto))`.

    Em `databricks_reader.py`: adicionar `async def ler_referencia_tamanhos() -> list[dict]`, mesmo esqueleto de `ler_estoque`: obtém `settings.databricks_tabela_tamanho_ref`; se vazio, `raise DatabricksError("DATABRICKS_TABELA_TAMANHO_REF não configurada no .env.")`; monta `statement = f"SELECT cd_prod_cor, sg_tamanho, nr_posicao FROM {tabela}"` (string única, sem filtro `WHERE`); `return await executar_consulta(statement)` — SEM `timeout_s` (mesmo padrão de `ler_estoque`).

    Em `repositorio_snapshot.py`: adicionar `ProdutoTamanhoPosicao` ao import de `app.modules.ingestao.models` (ordem alfabética: depois de `PedidoProcessadoErp`). Implementar `async def substituir_referencia_tamanhos(db: AsyncSession, itens: Iterable[dict]) -> None`, mesmo esqueleto de `substituir_faturamento_colecao` mas sem conversão `Decimal` (campos são inteiros): `await db.execute(delete(ProdutoTamanhoPosicao))` seguido de `db.add_all(ProdutoTamanhoPosicao(**item) for item in itens)`. CRÍTICO: nenhum `if`/guard e nenhum `db.commit()` aqui — quem decide SE chama esta função é o caso de uso (Plano 02-03, D-02); este repositório fica incondicional e simétrico aos 3 irmãos.

    Em `test_ingestao_sync.py`: adicionar uma nova seção de testes PUROS (sem sessão/banco), com cabeçalho de comentário `# agregar_referencia_tamanhos (D-01/D-03/D-04 — sem I/O, mesmo espírito de test_ingestao_parse.py)` e o import `from app.modules.ingestao.domain.agregacao import agregar_referencia_tamanhos` no topo do arquivo. Três testes: (1) `test_agregar_referencia_tamanhos_descarta_posicao_invalida_sem_lancar` — linhas com `nr_posicao` válido e inválido (`"0"`, `"49"`, `"abc"`), assert `descartadas` conta só as inválidas e `agrupado` só contém as válidas; (2) `test_agregar_referencia_tamanhos_detecta_conflito_de_posicao_mantem_ambas_linhas` — duas linhas do MESMO `cd_prod_cor` com `sg_tamanho` diferentes (`"M"`, `"GG"`) e a MESMA `nr_posicao` (ex.: `5`); assert que `conflitos_por_produto[cd]` contém a string exata `"M/GG->pos5"` e que AMBAS as chaves `(cd, "M")` e `(cd, "GG")` existem em `agrupado` (Pitfall 2); (3) `test_agregar_referencia_tamanhos_agrega_conflitos_por_produto_nao_por_linha` — 2 produtos diferentes, cada um com 1 conflito; assert que `conflitos_por_produto` tem exatamente 2 chaves (uma por produto), nunca uma entrada por linha bruta (D-04).
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_sync.py -q -k agregar_referencia_tamanhos</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` mostra `3 passed`, `0 failed`
    - `grep -c "def substituir_referencia_tamanhos" app/modules/ingestao/infrastructure/repositorio_snapshot.py` retorna `1`
    - `grep -c "def ler_referencia_tamanhos" app/modules/ingestao/infrastructure/databricks_reader.py` retorna `1`
    - `grep -c "def agregar_referencia_tamanhos" app/modules/ingestao/domain/agregacao.py` retorna `1`
    - `grep -A8 "def substituir_referencia_tamanhos" app/modules/ingestao/infrastructure/repositorio_snapshot.py | grep -vE '^\s*#' | grep -c "db.commit\|if "` retorna `0` (repositório incondicional, sem `if` de guard e sem commit próprio)
    - Regressão: `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` sai com código 0 (baseline 215 passed + os novos casos deste plano, sem quebrar nada existente)
  </acceptance_criteria>

  <done>As 3 funções novas existem, seguem exatamente o padrão dos pares irmãos (repositório sem guard/commit), e o conflito de posição D-03 é detectado e agregado por produto (D-04) com teste automatizado comprovando o comportamento sem tocar banco.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Artefatos que ESTE PLANO entrega (visão cumulativa completa da Fase 2 no Plano 02-05):

| Artefato | Tipo | Onde |
|----------|------|------|
| `databricks_tabela_tamanho_ref` | campo de config (`DATABRICKS_TABELA_TAMANHO_REF`) | `app/shared/config/settings.py`, `.env.example` |
| `_parse_posicao` | função pura (parse tolerante, D-01) | `app/modules/ingestao/domain/traducao_databricks.py` |
| `agregar_referencia_tamanhos` | função pura (dedup + conflito D-03/D-04) | `app/modules/ingestao/domain/agregacao.py` |
| `ler_referencia_tamanhos` | função assíncrona (reader HTTP) | `app/modules/ingestao/infrastructure/databricks_reader.py` |
| `substituir_referencia_tamanhos` | função assíncrona (repositório, DELETE+INSERT incondicional) | `app/modules/ingestao/infrastructure/repositorio_snapshot.py` |
| `test_parse_posicao` | teste parametrizado (7 casos) | `app/tests/test_ingestao_parse.py` |
| `test_agregar_referencia_tamanhos_*` | 3 testes puros (D-01/D-03/D-04) | `app/tests/test_ingestao_sync.py` |

Consumidores a jusante: o Plano 02-03 (caso de uso `sincronizar_referencia_tamanhos`) importa e orquestra as 3 funções assíncronas/de agregação criadas aqui.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Databricks (view externa) → reader/parse | A view `system_automation_prod_tamanho_ref` é dado de origem externa (fonte gerenciada por outra equipe) — nunca confiar no tipo/faixa bruta do valor de `nr_posicao`. |
| Nenhuma superfície HTTP nova | Este plano não expõe nem modifica nenhuma rota; toda a mudança é interna aos módulos de domínio/infraestrutura da ingestão. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `ler_referencia_tamanhos` (nome de tabela interpolado via f-string) | accept | Valor vem de `settings.databricks_tabela_tamanho_ref` (variável de ambiente controlada pela equipe, não input HTTP) — mesmo padrão já aceito pelas 4 fontes existentes (nenhuma delas parametriza o nome da tabela). |
| T-02-02 | Tampering | `agregar_referencia_tamanhos` — `nr_posicao` fora de faixa/não numérico da view | mitigate | `_parse_posicao` descarta na tradução (D-01); linha nunca chega ao repositório com posição inválida. |
| T-02-03 | Tampering | `agregar_referencia_tamanhos` — dois tamanhos do mesmo produto na mesma posição (dado sujo) | mitigate | Detecção via índice reverso `posicao_por_produto` + aviso agregado por produto (D-03/D-04); o sync nunca trava por dado sujo. |
| T-02-SC | Tampering | npm/pip/cargo installs | N/A | Nenhum pacote novo instalado nesta fase (confirmado em `02-RESEARCH.md`, "Package Legitimacy Audit — não aplicável"). |
</threat_model>

<verification>
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_parse.py -q -k parse_posicao` → `7 passed`
2. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_sync.py -q -k agregar_referencia_tamanhos` → `3 passed`
3. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) → verde, sem regressão da baseline (215 passed + os novos)
</verification>

<success_criteria>
- `_parse_posicao`, `agregar_referencia_tamanhos`, `ler_referencia_tamanhos`, `substituir_referencia_tamanhos` existem com as assinaturas descritas
- `_parse_posicao` descarta (`None`) qualquer valor fora de 1..48 ou não numérico, sem lançar exceção (D-01)
- `agregar_referencia_tamanhos` detecta conflito de posição entre tamanhos diferentes do mesmo produto, agrega o aviso por produto (nunca por linha) e preserva ambas as linhas conflitantes em `agrupado` (D-03/D-04)
- `substituir_referencia_tamanhos` é incondicional (sem guard, sem commit próprio) — a decisão de chamá-la ou não pertence ao caso de uso do Plano 02-03
- Suíte completa verde
</success_criteria>

<output>
Criar `.planning/phases/02-ingest-o-da-refer-ncia-convers-o-de-grade/02-01-SUMMARY.md` ao terminar, registrando: (a) os 3 números de teste (7 + 3 + baseline completa), (b) o formato exato usado para as strings de conflito (`"M/GG->pos5"`), (c) confirmação de que o repositório permanece incondicional.
</output>
