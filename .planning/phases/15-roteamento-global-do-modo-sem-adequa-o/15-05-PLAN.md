---
phase: 15-roteamento-global-do-modo-sem-adequa-o
plan: 05
type: execute
wave: 5
depends_on: ["15-03"]
files_modified: ["docs/adequacao.md"]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Um leitor de docs/adequacao.md entende que sem_adequar usa o mesmo caminho global de planejamento (mesma foto hidratada em Python, mesmo build_processing_plan) que adequar, sem paginação/streaming exclusiva"
    - "Um leitor de docs/adequacao.md entende que a decisão de quantidade usa um ledger (OrcamentoPedido) por PEDIDO COMPLETO, com dois orçamentos independentes (adição e corte) que não se compensam, resolvidos em duas passadas"
    - "Um leitor de docs/adequacao.md entende que deferredCount/blockedCreditCount refletem valores reais nos DOIS modos desde o roteamento global da Fase 15, não só em adequar"
    - "A suíte completa do repositório permanece no baseline conhecido (825 passed, 16 skipped, 1 failed) depois da mudança, provando que é uma edição doc-only"
  artifacts:
    - path: "docs/adequacao.md"
      provides: "descrição do motor de adequação atualizada para o estado pós-Fase 14 (ledger/duas passadas) e pós-Fase 15 (roteamento global, remoção do streaming)"
      contains: "OrcamentoPedido"
  key_links:
    - from: "docs/adequacao.md"
      to: "app/modules/pedidos/domain/orcamento_pedido.py"
      via: "referência textual ao ledger OrcamentoPedido e aos dois orçamentos independentes"
      pattern: "OrcamentoPedido"
    - from: "docs/adequacao.md"
      to: "app/modules/pedidos/processing/domain.py"
      via: "referência textual ao caminho único de planejamento compartilhado pelos dois modos"
      pattern: "mesma foto global"
---

<objective>
Atualizar `docs/adequacao.md` para refletir fielmente o motor de adequação como ele existe DEPOIS da Fase 14 (ledger `OrcamentoPedido` por pedido completo, duas passadas, rateio Hamilton) e DEPOIS do plano 15-03 desta fase (roteamento global de `sem_adequar`, remoção do streaming). Este plano só toca documentação — nenhum arquivo de código é modificado.

Purpose: `docs/adequacao.md` é a referência de arquitetura consultada por qualquer pessoa (ou agente) que precise entender o motor sem reler o código inteiro. Depois do 15-03, três afirmações do documento ficam falsas ou incompletas: (1) a descrição de `sem_adequar` como um caminho paginado/streaming separado de `adequar`; (2) a descrição de `adequar_grade_produto` como uma decisão de tolerância `ceil`/`floor` sobre um produto isolado (regra que a Fase 14 substituiu pelo ledger por pedido completo); (3) a implicação de que `deferredCount`/`blockedCreditCount` são exclusivos de `adequar`. `15-RESEARCH.md` § "Ordem de remoção do streaming" e `14-06-SUMMARY.md` já documentam o que precisa entrar no lugar — esta dívida já estava sinalizada em `.planning/research/v1.3/ARCHITECTURE.md` e na wave 5 (opcional, mas cobrada aqui) do `15-RESEARCH.md`.

Output: `docs/adequacao.md` reescrito nas seções afetadas, suíte completa confirmada no baseline conhecido, commit único com escopo `15-05`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-CONTEXT.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-06-SUMMARY.md
@docs/adequacao.md

IMPORTANTE — este plano depende do 15-03 já estar fechado. `15-03` reescreve
`service.py::_plan_once` e `processing/domain.py::build_processing_plan` e
remove o código de streaming; a Task 1 abaixo exige ler o estado REAL desses
arquivos NA HORA DA EXECUÇÃO, não confiar em número de linha de
`15-RESEARCH.md`/`15-CONTEXT.md` (esses documentos descrevem o estado ANTES
da remoção do streaming — servem só como previsão do desenho, não como fonte
de verdade sobre o código final). Se existir
`.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-03-SUMMARY.md`
e `15-04-SUMMARY.md` no momento da execução, leia os dois: o primeiro prova o
que de fato mudou no roteamento; o segundo (medição de pico de memória) só
existe se a wave 4 já rodou — por ordem sequencial de waves ela já terá
rodado antes desta (wave 5), mas o plano só depende formalmente de 15-03, então
trate o SUMMARY de 15-04 como opcional: cite o número medido se ele existir,
não invente um número se não existir.

`app/modules/pedidos/domain/motor_adequacao.py` e
`app/modules/pedidos/domain/orcamento_pedido.py` NÃO são tocados pelo 15-03
(confirmado em `15-PLAN-OUTLINE.md` § "Fronteiras entre planos" — 15-03 só
toca `service.py`, `processing/domain.py`, `ports.py`, `adapters.py`,
`repository.py` e arquivos de teste). O ledger e as duas passadas já estão
estáveis desde o 14-06 — a Task 2 abaixo pode se basear neles com confiança,
sem esperar por nenhuma wave desta fase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever a orquestração durável — caminho global único, sem streaming</name>
  <files>docs/adequacao.md</files>
  <read_first>
    - app/modules/pedidos/processing/domain.py (ler a função `build_processing_plan` INTEIRA no estado atual, pós-15-03 — não presumir a partir de `15-RESEARCH.md`)
    - app/modules/pedidos/processing/application/service.py (ler `_plan_once` INTEIRA no estado atual)
    - app/modules/pedidos/processing/infrastructure/repository.py (localizar `store_plan` e o valor atual de `APPLY_CHUNK_SIZE` — confirmar se ainda é 250 ou se mudou)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-03-SUMMARY.md (se existir — descreve o que de fato foi implementado, prevalece sobre a previsão de 15-RESEARCH.md)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-04-SUMMARY.md (se existir — número real de pico de memória de `sem_adequar` pós-roteamento; opcional)
    - docs/adequacao.md (seção `## Orquestração durável`, item numerado 3 — hoje descreve `sem_adequar` materializando "uma vez somente as chaves elegíveis em tabela temporária transacional" e lendo/persistindo "páginas de até 250 pares", com os números "17,668 s" e "8,9 MiB de RSS" medidos só para esse caminho de streaming)
  </read_first>
  <action>
    Reescreva o item 3 da lista numerada em `## Orquestração durável` (o parágrafo que hoje começa em "O preflight operacional limita a foto a 100.000 pares..."). Mantenha a primeira frase sobre o preflight (100.000 pares / 200.000 itens / 100 itens por par / 96 MiB) — isso não mudou e vale para os dois modos. Substitua tudo depois disso para descrever que `adequar` e `sem_adequar` agora compartilham o MESMO caminho de planejamento: a foto elegível inteira é hidratada em Python (a função de leitura do port, confirme o nome exato lendo `adapters.py`/`ports.py` no estado atual — era `load_pending_items` antes do 15-03, confirme se o nome sobreviveu) e `build_processing_plan` monta o plano chamando o motor de adequação (`processar_pedidos`) com o modo correspondente para os dois casos — não existe mais um caminho de paginação exclusivo para `sem_adequar`. Use literalmente a substring "mesma foto global" em algum ponto da frase (ex.: "`sem_adequar` passa a hidratar a mesma foto global que `adequar` já usa") — é o texto que a verificação automatizada procura. Remova as substrings "tabela temporária transacional" e "17,668 s" e "8,9 MiB" — eram medições do caminho de streaming que deixou de existir; NÃO as substitua por um novo número inventado. Se `15-04-SUMMARY.md` existir e contiver um número de pico de memória medido para `sem_adequar` pós-roteamento, cite esse número explicitamente (com a fonte, ex.: "medido no plano 15-04"); se não existir, mantenha só a frase qualitativa sobre a foto global compartilhada, sem número. Mantenha a frase final "Ambos produzem plano fingerprintado em `pedido_processamento_plan`" (ou equivalente, se o nome da tabela mudou — confirme lendo `repository.py`). Não toque os itens 1, 2, 4 e 5 da mesma lista numerada, nem qualquer outra seção do arquivo nesta task.
  </action>
  <verify>
    <automated>grep -v '^#' docs/adequacao.md | grep -c "tabela temporária transacional"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' docs/adequacao.md | grep -c "tabela temporária transacional"` retorna 0
    - `grep -c "17,668 s" docs/adequacao.md` retorna 0
    - `grep -c "8,9 MiB" docs/adequacao.md` retorna 0
    - `grep -c "mesma foto global" docs/adequacao.md` retorna >= 1
    - `grep -v '^#' docs/adequacao.md | grep -ic "streaming"` retorna 0 (a palavra não deve aparecer descrevendo o modo sem_adequar)
  </acceptance_criteria>
  <done>Item 3 de "## Orquestração durável" descreve os dois modos compartilhando o mesmo caminho de planejamento (foto global hidratada em Python + `build_processing_plan`), sem menção a streaming/paginação exclusiva nem aos números de benchmark do caminho removido.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever a alocação por pedido (ledger, duas passadas, Hamilton) e a abrangência dos contadores</name>
  <files>docs/adequacao.md</files>
  <read_first>
    - app/modules/pedidos/domain/motor_adequacao.py (ler INTEIRO — `adequar_grade_produto`, `conceder_adicao_pedido`, `aplicar_tudo_ou_nada`, `_recalcular_financeiro_produto`, e o despacho por modo dentro de `_processar_pedidos_canal`; este arquivo é estável desde o 14-06 e NÃO é tocado pelo 15-03)
    - app/modules/pedidos/domain/orcamento_pedido.py (ler INTEIRO — `OrcamentoPedido`, `limite_adicao`/`limite_corte`, `consumir_corte`/`consumir_adicao`)
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-06-SUMMARY.md (contexto de por que o ledger existe e por que os dois orçamentos não se compensam)
    - docs/adequacao.md (seção `## Regras puras — processar_pedidos`, item numerado 5; seção `## Comparação pedido × estoque — adequar_grade_produto` inteira, hoje linhas ~91-106; subseção `### Ordenação de tamanhos`, hoje linhas ~108-113; seção `## Resultado`, hoje linhas ~115-133)
  </read_first>
  <action>
    Três edições nesta task, todas em `docs/adequacao.md`:

    (1) No item 5 de `## Regras puras — processar_pedidos` (hoje "Para cada produto, consome o estoque nessa ordem de prioridade, chamando `adequar_grade_produto` por pedido..."), substitua para explicitar o despacho por modo: `sem_adequar` chama `aplicar_tudo_ou_nada` (tudo-ou-nada por par, sem ledger de orçamento); `adequar` chama `adequar_grade_produto` (passada 1 — decide o corte, usando o ledger do pedido) e, só para os produtos que saíram da passada 1 com folga total, `conceder_adicao_pedido` (passada 2 — extras). Mantenha a frase sobre o estoque local ser decrementado a cada OR gerada nos dois modos.

    (2) Substitua a seção inteira `## Comparação pedido × estoque — adequar_grade_produto` por uma nova seção (pode renomear o título, sugestão: "## Alocação por pedido — ledger e duas passadas") que explique: o motor itera por PEDIDO (não mais por produto isolado) em ordem de prioridade global, instanciando um `OrcamentoPedido` uma única vez por pedido; o ledger tem DOIS orçamentos independentes — adição e corte —, cada um `floor(total_original_do_pedido × 5%)` em aritmética inteira, que NÃO se compensam entre si (use literalmente a substring "não se compensam"); `total_original` é a soma de TODOS os produtos do pedido (não só os pendentes desta execução) e o consumido em execuções anteriores é somado antes de calcular o restante, para o orçamento nunca resetar entre execuções sucessivas (ALOC-09). Descreva as duas passadas: passada 1 (`adequar_grade_produto`, ordenada por corte necessário crescente) decide corte via `ledger.consumir_corte` (tudo-ou-nada: ou o produto inteiro vira "Gerar OR" com cada tamanho em `min(qt_pedida, estoque)`, ou vira "Pedido em Stand By" sem gastar orçamento); passada 2 (`conceder_adicao_pedido`, ordenada por preço unitário médio decrescente, só entre produtos com folga total da passada 1) concede peças extras aos tamanhos de fronteira via `ledger.consumir_adicao` (parcial, nunca mais do que o estoque disponível permite). Feche a seção explicando que, depois das duas passadas, `vl_liquido` de cada produto é recalculado de uma vez via rateio de Hamilton (`ratear_hamilton`, use literalmente essa substring ou "Hamilton"), eliminando o drift de `round()` item a item (FIX-02). Inclua uma frase final deixando claro que `sem_adequar` não usa o ledger: cada par é tudo-ou-nada via `aplicar_tudo_ou_nada`.

    (3) Ajuste `### Ordenação de tamanhos` só na frase que hoje diz que os "extremos" são usados dentro de "adequar_grade_produto" — corrija para dizer que são usados pela passada 2 (`conceder_adicao_pedido`). O restante da subseção (RANKINGS, ordenação alfabética/numérica/calçado/cinto/camisa/UN) continua válido, não precisa reescrever.

    (4) Em `## Resultado`, logo após a frase que descreve o que o GET do job devolve (`plannedCount`, `appliedCount`, `deferredCount`, `blockedCreditCount`), adicione uma frase explícita: esses dois últimos contadores (`deferredCount`/`blockedCreditCount`) refletem `preteridos`/`bloqueados_credito` REAIS nos DOIS modos desde o roteamento global (Fase 15, ARCH-01) — antes do roteamento, `sem_adequar` sempre devolvia os dois zerados, porque não passava pelo motor.
  </action>
  <verify>
    <automated>grep -v '^#' docs/adequacao.md | grep -c "tolerância padrão de 5%"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' docs/adequacao.md | grep -c "tolerância padrão de 5%"` retorna 0 (a descrição de tolerância por produto isolado não existe mais)
    - `grep -c "OrcamentoPedido" docs/adequacao.md` retorna >= 1
    - `grep -c "não se compensam" docs/adequacao.md` retorna >= 1
    - `grep -ic "hamilton" docs/adequacao.md` retorna >= 1
    - `grep -c "aplicar_tudo_ou_nada" docs/adequacao.md` retorna >= 1
    - `grep -c "conceder_adicao_pedido" docs/adequacao.md` retorna >= 1
    - `grep -ic "deferredCount\|blockedCreditCount" docs/adequacao.md` seguido de leitura manual confirma que a frase adicionada em (4) menciona explicitamente os dois modos (não há grep simples que prove isso sozinho — checar por leitura na Task 3)
  </acceptance_criteria>
  <done>As quatro edições estão feitas: item 5 das regras puras reflete o despacho por modo; a seção de alocação por pedido descreve o ledger/duas passadas/Hamilton em vez da tolerância por produto isolado; "Ordenação de tamanhos" referencia a função correta; "Resultado" explicita que os dois contadores valem para os dois modos desde a Fase 15.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de suíte completa e commit</name>
  <files>docs/adequacao.md</files>
  <read_first>
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-PLAN-OUTLINE.md (baseline conhecido: 825 passed, 16 skipped, 1 failed; comando de suíte completa via Docker)
    - docs/adequacao.md (arquivo final inteiro — releitura de conferência antes do commit)
  </read_first>
  <action>
    Releia `docs/adequacao.md` inteiro depois das Tasks 1 e 2 e confirme por leitura (não só grep) que: nenhuma frase remanescente descreve `sem_adequar` como um caminho de paginação/streaming separado; a seção de alocação por pedido não menciona mais `ceil`/`floor` por produto isolado; a frase sobre `deferredCount`/`blockedCreditCount` explicitamente cobre os dois modos. Rode a suíte completa via Docker (`docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q`) — é edição doc-only, então o resultado esperado é EXATAMENTE o mesmo baseline conhecido (825 passed, 16 skipped, 1 failed, a única falha sendo a pré-existente de `test_pedidos_read_projection.py`); qualquer divergência deste baseline é sinal de que algo além da documentação foi tocado (working tree compartilhado com trabalho de SSO não commitado) e deve ser investigado antes de commitar. Depois de confirmar o baseline, rode `git status --short -- docs/adequacao.md` para confirmar que só este arquivo mudou, adicione com `git add docs/adequacao.md` (caminho explícito, nunca `git add -A`/`git add .`, por causa do trabalho de SSO não commitado compartilhando a working tree) e commite com Conventional Commits, escopo `15-05`, sem coautoria de Claude (per memória do usuário: nunca adicionar Co-Authored-By nem menção a Claude nos commits deste projeto).
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - A suíte completa via Docker retorna exatamente "825 passed, 16 skipped, 1 failed" (mesmo baseline da Phase 14/wave anterior — nenhuma regressão introduzida por uma edição doc-only)
    - `git show --stat HEAD` (depois do commit) lista SOMENTE `docs/adequacao.md` como arquivo alterado
    - A mensagem de commit segue Conventional Commits com escopo `15-05` e não contém "Co-Authored-By" nem qualquer menção a Claude
  </acceptance_criteria>
  <done>Suíte completa confirmada no baseline conhecido, commit único de `docs/adequacao.md` criado com Conventional Commits (escopo 15-05), sem coautoria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Nenhuma | Este plano só edita `docs/adequacao.md`, um arquivo Markdown estático. Ele não é executado, não é interpretado por nenhum parser em tempo de execução deste repositório (nenhum módulo de `app/` faz `open()`/parse de `docs/*.md`), não expõe endpoint, não recebe input de usuário e não cruza fronteira de confiança alguma. |

## STRIDE Threat Register

Nenhum item aplicável e nenhum é inventado aqui. Justificativa: documentação Markdown consumida apenas por leitura humana (ou por um agente Claude numa sessão futura) não tem superfície de ataque — não há Spoofing/Tampering/Repudiation/Information Disclosure/Denial of Service/Elevation of Privilege possível através da edição do próprio texto. A única "mutação" em jogo é o conteúdo do arquivo, revisável via `git diff` antes do commit (Task 3 já inclui essa releitura de conferência). Não há pacote novo instalado (sem gate de legitimidade de pacote a aplicar) e nenhum código de produção é tocado.
</threat_model>

<verification>
- `grep -v '^#' docs/adequacao.md | grep -c "tabela temporária transacional"` → 0
- `grep -c "17,668 s" docs/adequacao.md` → 0
- `grep -c "mesma foto global" docs/adequacao.md` → >= 1
- `grep -v '^#' docs/adequacao.md | grep -c "tolerância padrão de 5%"` → 0
- `grep -c "OrcamentoPedido" docs/adequacao.md` → >= 1
- `grep -c "não se compensam" docs/adequacao.md` → >= 1
- Suíte completa via Docker: 825 passed, 16 skipped, 1 failed (baseline inalterado)
- `git show --stat HEAD` lista só `docs/adequacao.md`
</verification>

<success_criteria>
- `docs/adequacao.md` não descreve mais `sem_adequar` como um caminho de streaming/paginação separado de `adequar` — descreve o caminho global único compartilhado pelos dois modos.
- `docs/adequacao.md` descreve `adequar_grade_produto`/`conceder_adicao_pedido` via o ledger `OrcamentoPedido` (pedido completo, dois orçamentos independentes, duas passadas), não mais via tolerância `ceil`/`floor` por produto isolado.
- `docs/adequacao.md` explicita que `deferredCount`/`blockedCreditCount` valem para os dois modos desde o roteamento global da Fase 15.
- Suíte completa via Docker permanece no baseline conhecido (825 passed, 16 skipped, 1 failed) — prova de que a mudança é doc-only.
- Commit único, Conventional Commits, escopo `15-05`, sem coautoria de Claude.
</success_criteria>

## Artifacts this phase produces

- `docs/adequacao.md` § "Orquestração durável" (item 3 reescrito — caminho global único, sem streaming)
- `docs/adequacao.md` § "Regras puras — processar_pedidos" (item 5 ajustado — despacho por modo explícito)
- `docs/adequacao.md` § nova "Alocação por pedido — ledger e duas passadas" (substitui "Comparação pedido × estoque — adequar_grade_produto")
- `docs/adequacao.md` § "Ordenação de tamanhos" (referência de função corrigida para `conceder_adicao_pedido`)
- `docs/adequacao.md` § "Resultado" (contadores explicitados para os dois modos)

<output>
Create `.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-05-SUMMARY.md` when done
</output>
