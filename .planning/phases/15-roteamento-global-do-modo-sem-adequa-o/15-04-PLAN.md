---
phase: 15-roteamento-global-do-modo-sem-adequa-o
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - app/tests/test_pedidos_processing_sem_adequar_memory_024.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Rodar `pytest app/tests/test_pedidos_processing_sem_adequar_memory_024.py -q -s` produz um número REAL (não estimado) de pico de RSS incremental de `build_processing_plan` em modo SEM_ADEQUAR, sobre um dataset sintético de 45.000 pares / 112.500 itens gerado em memória (não lido do banco)."
    - "O teto usado na asserção (`_MEMORY_CEILING_MIB`) está documentado no próprio arquivo como calibração inicial (referência ~700 MiB, com margem sobre os ~600 MiB projetados em processing/domain.py), e o valor REAL medido na primeira execução deste plano está registrado no SUMMARY.md — nunca afrouxado silenciosamente até passar."
    - "A suíte completa do repositório continua em 825 passed, 16 skipped, 1 failed (a falha conhecida de teardown asyncpg) depois de adicionar este teste — nenhuma wave desta fase termina vermelha."
    - "O teste falha (não passa silenciosamente) se o dataset sintético não gerar o volume completo de 45.000 pares selecionados — protegendo contra uma medição de memória enganosa sobre um plano vazio ou parcial."
  artifacts:
    - path: "app/tests/test_pedidos_processing_sem_adequar_memory_024.py"
      provides: "Medição automatizada de pico de RSS (resource.getrusage) de build_processing_plan em modo SEM_ADEQUAR, com dataset sintético grande gerado programaticamente"
      contains: "resource.getrusage"
      min_lines: 70
  key_links:
    - from: "app/tests/test_pedidos_processing_sem_adequar_memory_024.py"
      to: "app/modules/pedidos/processing/domain.py::build_processing_plan"
      via: "chamada direta no teste com request.mode=ProcessingMode.SEM_ADEQUAR"
      pattern: "build_processing_plan\\("
    - from: "app/tests/test_pedidos_processing_sem_adequar_memory_024.py"
      to: "resource.getrusage(resource.RUSAGE_SELF).ru_maxrss"
      via: "medição antes/depois da chamada, delta convertido de KiB para MiB"
      pattern: "ru_maxrss"
---

<objective>
Instrumentar, pela primeira vez neste repositório, a medição automatizada de pico de memória (RSS) do modo `SEM_ADEQUAR` planejado pelo caminho global — critério de sucesso 4 do ROADMAP da Fase 15 ("O pico de memória medido ao processar um canal inteiro no modo sem adequação permanece dentro do orçamento já reservado para o worker dedicado").

**Pré-condição bloqueante, não decorativa:** este plano só pode ser executado depois do 15-03 fechado (roteamento de `SEM_ADEQUAR` pelo caminho global `build_processing_plan`/`processar_pedidos`, streaming removido). Em 2026-08-17, no momento em que este PLAN.md foi escrito, `app/modules/pedidos/processing/domain.py::build_processing_plan` AINDA usa, no ramo `else` (modo `SEM_ADEQUAR`), `selected = {pair: items for pair, items in sorted(eligible.items())}` — ou seja, ainda NÃO chama `processar_pedidos`. Antes de escrever qualquer linha de código deste plano, releia esse arquivo (função `build_processing_plan`) e confirme que o ramo `SEM_ADEQUAR` já chama `processar_pedidos(..., modo=...)`. **Se ainda mostrar a seleção direta sem motor, PARE — não escreva o teste.** Medir antes do 15-03 mediria o caminho antigo (seleção incondicional sem motor, que nunca existiu com hidratação global via streaming), não o que o critério 4 pede provar.

**Não existe hoje nenhuma instrumentação de memória automatizada neste repositório.** Os números de `docs/adequacao.md` (434 MiB) e do comentário em `processing/domain.py` (linhas 34-37) são medições manuais antigas de uma sessão de profiling passada para o modo `ADEQUAR`, não testes reprodutíveis. Nenhum pico real de `SEM_ADEQUAR` foi medido em produção — e o volume elegível desse modo pode ser **estruturalmente maior** que o do `ADEQUAR`, porque ele aceita todo par sem filtro de crédito antes desta fase (15-RESEARCH.md, Assumption A2). Trate o teto escolhido neste plano como **instrumentação a calibrar na primeira execução real**, não como um valor já validado — se a primeira rodada falhar, isso é sinal para medir e ajustar conscientemente (documentando o número real observado), nunca para afrouxar o número silenciosamente até passar.

Purpose: dar ao critério 4 do ROADMAP uma prova automatizada e reproduzível, em vez de uma medição manual antiga que nunca cobriu este caminho.

Output: `app/tests/test_pedidos_processing_sem_adequar_memory_024.py` (arquivo novo), com o número real de pico de memória observado registrado no SUMMARY.md deste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-RESEARCH.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-PATTERNS.md

# SUMMARY do plano do qual este depende — só existirá quando 15-03 estiver
# fechado; é a fonte de verdade de COMO o ramo SEM_ADEQUAR ficou implementado
# de fato (nomes de parâmetro podem ter sido decisão de discretion do 15-03).
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-03-SUMMARY.md

# Função sob medição e o comentário que documenta o teto/premissas atuais de memória
@app/modules/pedidos/processing/domain.py

# Assinatura real de processar_pedidos/aplicar_tudo_ou_nada e o filtro de crédito
# que passa a valer para SEM_ADEQUAR depois do roteamento
@app/modules/pedidos/domain/motor_adequacao.py

# Formato da chave de estoque (f"{cd_prod_cor}_{sg_tamanho}") usado por aplicar_tudo_ou_nada
@app/modules/pedidos/domain/value_objects.py

# Único análogo de teste "_024" no repositório — convenção de nome de arquivo,
# estilo de docstring de módulo, e confirmação de que nenhum teste hoje usa
# resource/psutil/tracemalloc
@app/tests/test_pedidos_motor_performance_024.py

# Convenção de chamada de build_processing_plan em modo SEM_ADEQUAR (helper _item,
# formato de `reference`) já usada pelos testes de domínio existentes
@app/tests/test_pedidos_processing_domain.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o teste de medição de pico de memória com dataset sintético grande</name>
  <files>app/tests/test_pedidos_processing_sem_adequar_memory_024.py</files>
  <read_first>
    - app/modules/pedidos/processing/domain.py — `build_processing_plan` (assinatura: `request`, `pending_items`, `reference`, `stock=None`, `criterion="valor"`, `tolerance=0.05`, `cancel_token=None`); confirmar que o ramo `SEM_ADEQUAR` já chama `processar_pedidos` (guarda bloqueante descrita no `<objective>`); `ProcessingMode`, `ProcessingChannel`, `ProcessingRequest` (dataclass `mode`+`channel`); `PLAN_PAIR_LIMIT=100_000`, `PLAN_ITEM_LIMIT=200_000`, `MAX_PLAN_TOTAL_BYTES=96*1024*1024` (linhas 33-43) — o dataset deste teste (45.000 pares / 112.500 itens) precisa ficar folgadamente abaixo dos três, não coladinho neles
    - app/modules/pedidos/domain/motor_adequacao.py — `processar_pedidos` (linhas 455-468, parâmetros `dados, estoque, ja_processados, criterio, tolerancia, *, modo, resultados_apenas_selecionados, cancel_token, total_original_por_pedido, consumido_previo_adicao_por_pedido, consumido_previo_corte_por_pedido`); `aplicar_tudo_ou_nada` (linhas 306-341, política tudo-ou-nada usada por `SEM_ADEQUAR`: grade completa disponível ou o par inteiro vai para stand-by); `is_sem_credito`/filtro de crédito em `_processar_pedidos_canal` (linhas 623-634) — todo item do dataset sintético precisa de `status_credito` que NÃO seja reconhecido como "sem crédito" (usar `"Com Crédito"`), senão o pedido cai em stand-by e o teste mede um cenário incompleto
    - app/modules/pedidos/domain/value_objects.py, `montar_chave_estoque` (linha 36-38): a chave do dict plano de estoque é `f"{cd_prod_cor}_{sg_tamanho}"` — o `stock` sintético precisa usar exatamente esse formato por canal
    - app/tests/test_pedidos_motor_performance_024.py — docstring de módulo (linha 1, estilo a replicar), convenção de nome de arquivo `_024`, e confirmação por leitura de que NENHUM teste ali usa `resource`/`psutil`/`tracemalloc` (não há padrão a copiar para a medição em si — é greenfield)
    - app/tests/test_pedidos_processing_domain.py — helper `_item()` (linhas 30-51, os 11 campos que um item de `pending_items` carrega: `nr_pedido, cd_prod_cor, sg_tamanho, ds_grupo, qt_liquida, vl_liquido, status_credito, client, canal, ds_produto, data`) e uso de `reference={"COD": {"36": 1, "37": 2}}` (linha 104, 130, 144) — formato `dict[str, dict[str,int]]` mapeando `cd_prod_cor -> {sg_tamanho: posicao_linx}`
    - app/modules/pedidos/domain/ordem_reserva_linx.py, `montar_linha_linx` (linhas 6-12, 74-86): a `reference` (`referencia_produto`) alimenta `converter_grade_para_posicoes`; posições pequenas (1, 2, 3) são válidas e já usadas pelos testes existentes
  </read_first>
  <action>
    Criar `app/tests/test_pedidos_processing_sem_adequar_memory_024.py` com docstring de módulo explicando: (1) o propósito (medir empiricamente o critério 4 do ROADMAP da Fase 15, pico de RSS de `build_processing_plan` em modo `SEM_ADEQUAR` sobre volume representativo de um canal inteiro); (2) que esta é a PRIMEIRA instrumentação de memória automatizada do repositório — nenhum precedente existe; (3) a limitação conhecida de `resource.getrusage(...).ru_maxrss`: é um pico monotônico desde o início do PROCESSO, não um snapshot pontual — por isso a leitura mais fiel vem de rodar este arquivo ISOLADO (não em meio à suíte completa), e o comando de verificação abaixo reflete isso; (4) que o teto usado na asserção é uma calibração inicial (referência ~700 MiB, com margem sobre os ~600 MiB projetados no comentário de `processing/domain.py` linhas 34-37 para o modo ADEQUAR) — não um valor validado por medição real de SEM_ADEQUAR ainda.

    Adicionar um guard `pytest.mark.skipif` (ou equivalente) condicionado a `sys.platform != "linux"`, com motivo citando que `resource.getrusage`/`ru_maxrss` é específico de Linux (mesmo ambiente do container Docker onde a suíte roda) — evita `ImportError`/`AttributeError` caso o arquivo seja coletado fora do container, sem afetar a contagem de "825 passed, 16 skipped, 1 failed" quando executado via Docker (a condição é falsa lá).

    Definir três constantes de módulo: `_SYNTHETIC_PAIR_COUNT = 45_000` (dentro da faixa 40-50k pedida); `_ITEMS_PER_PAIR_CYCLE = (2, 3)` (alterna 2 e 3 tamanhos por par, produzindo 112.500 itens totais — acima do piso de 100k+ pedido); `_MEMORY_CEILING_MIB = 700.0` (o teto de calibração, documentado no docstring conforme acima).

    Criar uma função helper `_construir_canal_sintetico(quantidade_pares: int) -> tuple[list[dict], dict[str, dict[str, int]], dict[str, dict[str, int]]]` que, para `i` em `range(quantidade_pares)`: gera `nr_pedido = 20_000_000 + i` (único por par, dentro do range válido `1..2_147_483_647`), `cd_prod_cor = f"SKU{i:06d}"` (único por par, mantendo cada produto de um único pedido — isso estressa o caminho de `_indexar_estoque_por_produto` com 45.000 produtos distintos, o cenário de complexidade que o teste existe para vigiar); alterna `sizes = ("36", "37")` para `i` par e `("36", "37", "38")` para `i` ímpar; para cada tamanho da lista, monta um item com os 11 campos do helper `_item()` de `test_pedidos_processing_domain.py` (`nr_pedido`, `cd_prod_cor`, `sg_tamanho=tamanho`, `ds_grupo="GRUPO_SINTETICO"`, `qt_liquida=2`, `vl_liquido=40.0`, `status_credito="Com Crédito"`, `client=f"Cliente Sintetico {i}"`, `canal="Franquia"`, `ds_produto=f"Produto Sintetico {i}"`, `data="2026-08-17"`) e acumula em uma lista plana `pending_items`. Em paralelo, para cada `(cd_prod_cor, tamanho)` usado, popula `stock["Franquia"][montar_chave_estoque(cd_prod_cor, tamanho)] = 10` (estoque abundante — 10 sempre >= `qt_liquida=2` — garantindo que TODOS os pares completem a grade e gerem OR, o cenário de pico de payload materializado, não o cenário de stand-by, que produziria menos memória e mediria um caso mais leniente que o pior caso real); `stock["Multimarca"] = {}` (canal vazio, nenhum item o usa). Popula também `reference[cd_prod_cor] = {"36": 1, "37": 2, "38": 3}` para cada produto (mapeamento tamanho->posição Linx, formato exigido por `montar_linha_linx`). Retornar `(pending_items, stock, reference)`.

    Criar a função de teste `test_sem_adequar_pico_de_memoria_do_canal_inteiro_fica_dentro_do_orcamento_do_worker()`: chamar `gc.collect()` e capturar `rss_antes_hidratacao = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss` (KiB no Linux); chamar `_construir_canal_sintetico(_SYNTHETIC_PAIR_COUNT)` para obter `pending_items, stock, reference`; assertar `len(pending_items) >= 100_000` (piso do dataset, per requisito "100k+ itens" — falha alto e cedo se o gerador estiver errado, antes de gastar tempo medindo memória de um dataset pequeno demais); capturar `rss_apos_hidratacao = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss`; chamar `build_processing_plan(request=ProcessingRequest(mode=ProcessingMode.SEM_ADEQUAR, channel=ProcessingChannel.FRANQUIA), pending_items=pending_items, reference=reference, stock=stock, criterion="valor", tolerance=0.05)`, guardando o retorno em `draft`; capturar `rss_apos_build = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss`. Calcular `delta_chamada_mib = (rss_apos_build - rss_apos_hidratacao) / 1024` (custo incremental só da chamada a `build_processing_plan`, a métrica literalmente pedida por 15-RESEARCH.md/15-VALIDATION.md) e `delta_total_mib = (rss_apos_build - rss_antes_hidratacao) / 1024` (custo do fluxo inteiro — foto do canal já hidratada em Python + planejamento — mais fiel ao texto do critério 4, "processar um canal inteiro"). Imprimir (via `print`, visível com `-s`) uma linha única reportando `delta_chamada_mib`, `delta_total_mib` e `_MEMORY_CEILING_MIB`, para que a Task 2 leia o número real sem precisar instrumentar de novo.

    Assertar, nesta ordem (falhas de volume ANTES da asserção de memória, para nunca mascarar um dataset incompleto como "memória OK"): `draft.candidate_count == _SYNTHETIC_PAIR_COUNT` (nenhum par foi descartado por canal/crédito antes de chegar ao motor); `len(draft.rows) == _SYNTHETIC_PAIR_COUNT` (com estoque abundante e crédito ok, todos os pares deveriam gerar OR — se um número menor aparecer, o dataset sintético tem um bug de fixture, não é o cenário de pico que o teste pretende medir); `draft.deferred_count == 0` e `draft.blocked_credit_count == 0` (mesma garantia, pela via dos contadores). Só então assertar `delta_total_mib < _MEMORY_CEILING_MIB`, com mensagem de asserção reportando os dois deltas e o teto para debug imediato em caso de falha.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_sem_adequar_memory_024.py -q -s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import resource" app/tests/test_pedidos_processing_sem_adequar_memory_024.py` retorna `1`.
    - `grep -v '^\s*#' app/tests/test_pedidos_processing_sem_adequar_memory_024.py | grep -c "ru_maxrss"` retorna pelo menos `3` (antes da hidratação, após hidratação, após a chamada).
    - `grep -c "^def test_" app/tests/test_pedidos_processing_sem_adequar_memory_024.py` retorna `1`.
    - `grep -c "_SYNTHETIC_PAIR_COUNT = 45_000" app/tests/test_pedidos_processing_sem_adequar_memory_024.py` retorna `1`.
    - `grep -c "len(pending_items) >= 100_000" app/tests/test_pedidos_processing_sem_adequar_memory_024.py` retorna `1`.
    - `grep -c "draft.candidate_count == _SYNTHETIC_PAIR_COUNT" app/tests/test_pedidos_processing_sem_adequar_memory_024.py` retorna `1`.
    - O comando de `<verify>` roda até o fim (passe ou falhe na asserção de memória — não em erro de fixture/import) e a saída com `-s` mostra a linha impressa com `delta_chamada_mib`/`delta_total_mib` reais.
  </acceptance_criteria>
  <done>O arquivo existe, contém o guard de plataforma, o dataset sintético de 45.000 pares/112.500 itens gerado programaticamente, as três capturas de `ru_maxrss`, as asserções de volume (candidate_count/rows/deferred/blocked) antes da asserção de memória, e a execução isolada via Docker produz um número real de pico de RSS (passando ou falhando de forma informativa).</done>
</task>

<task type="auto">
  <name>Task 2: Calibrar conscientemente o teto a partir do número real e fechar com a suíte completa verde</name>
  <files>app/tests/test_pedidos_processing_sem_adequar_memory_024.py</files>
  <read_first>
    - app/tests/test_pedidos_processing_sem_adequar_memory_024.py (o arquivo criado na Task 1 — reler o resultado real da execução, não assumir)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md, seção "Medição de memória (critério 4) — instrumentação nova" — repete explicitamente: "Tratar como instrumentação a calibrar na primeira execução, não como valor já validado (...) Se o teto escolhido falhar na primeira rodada, é sinal para medir e ajustar conscientemente, não para afrouxar o número até passar."
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-RESEARCH.md, Assumption A2 (o volume elegível de SEM_ADEQUAR pode ser estruturalmente maior que o de ADEQUAR por não filtrar crédito) — se o número real medido for muito mais alto que ~600 MiB, isso é o próprio A2 se manifestando, e deve ser sinalizado como achado real no SUMMARY.md, não só como ajuste técnico de teste
  </read_first>
  <action>
    Passo 1 — ler o resultado real da Task 1: a partir da saída de `pytest -s` já capturada (ou rodando de novo, isolado, com timeout generoso em primeiro plano — nunca em background), anotar os valores reais de `delta_chamada_mib` e `delta_total_mib`.

    Passo 2a — se a asserção `delta_total_mib < _MEMORY_CEILING_MIB` PASSOU: não alterar `_MEMORY_CEILING_MIB`. Editar o docstring do módulo, substituindo a frase "não um valor validado por medição real de SEM_ADEQUAR ainda" por uma frase registrando a calibração real: data de execução, `delta_total_mib` observado (com 1 casa decimal), `_SYNTHETIC_PAIR_COUNT`/quantidade de itens, e que o teto de `_MEMORY_CEILING_MIB` permanece com a margem observada acima do pico real.

    Passo 2b — se a asserção FALHOU: **não reduzir a exigência do teste** (não trocar `_SYNTHETIC_PAIR_COUNT` para um número menor, não remover a asserção de volume). Em vez disso, escolher conscientemente um novo `_MEMORY_CEILING_MIB` com margem real acima do `delta_total_mib` observado (ex.: arredondar para cima com folga de ~15-20% sobre o pico real, não coladinho nele), editar a constante e o docstring documentando: o pico real observado, a data, e que o teto foi recalibrado a partir de medição real (não estimativa). Rodar de novo o teste isolado para confirmar verde com o novo teto. Se o pico real observado sugerir que o volume elegível de SEM_ADEQUAR de fato excede folgadamente o orçamento do worker (1280-1536 MiB citado em `processing/domain.py`) mesmo depois da calibração — isto é um achado de produto, não só de teste — registrar isso com destaque no SUMMARY.md (não enterrar em um parágrafo técnico), citando a Open Question 2 de `15-RESEARCH.md` e a alternativa já documentada em ARCHITECTURE.md (processar por canal em vez de "Todos" de uma vez) como próximo passo recomendado, sem implementá-la neste plano (fora de escopo).

    Passo 3 — gate de suíte completa: rodar a suíte inteira via Docker e confirmar `825 passed, 16 skipped, 1 failed` (a falha conhecida de `test_pedidos_read_projection.py::test_read_projection_empty_contracts_execute_real_sql`, teardown asyncpg). Nenhuma wave desta fase pode terminar vermelha — se o número divergir da baseline por qualquer motivo além dessa falha conhecida, investigar antes de prosseguir (não commitar).

    Passo 4 — commit: `git add app/tests/test_pedidos_processing_sem_adequar_memory_024.py` (caminho explícito — o working tree é compartilhado com trabalho de SSO não commitado; nunca `git add -A`/`git add .`). Conferir `git status --short` antes do commit para confirmar que só este arquivo está staged. Mensagem em Conventional Commits, escopo `15-04`, corpo descrevendo o número real medido (ex. "mede pico de RSS de build_processing_plan em modo sem_adequar via dataset sintético de 45k pares; pico real observado: X MiB, teto Y MiB"), **sem** `Co-Authored-By` nem qualquer menção a Claude.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_sem_adequar_memory_024.py -q -s</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_sem_adequar_memory_024.py -q -s` termina em `1 passed`.
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) termina em exatamente `825 passed, 16 skipped, 1 failed` (a falha conhecida de teardown asyncpg — nenhuma falha nova).
    - `git status --short -- app/modules app/shared alembic` retorna vazio (nenhum arquivo de produção tocado por este plano).
    - `git log -1 --format=%B -- app/tests/test_pedidos_processing_sem_adequar_memory_024.py` não contém "Claude" nem "Co-Authored-By".
    - O docstring do módulo (`grep -v '^\s*#' app/tests/test_pedidos_processing_sem_adequar_memory_024.py | grep -c "MiB"`) reporta o número real medido, não mais o texto de "instrumentação não calibrada".
  </acceptance_criteria>
  <done>O teste passa isoladamente com o teto calibrado conscientemente a partir de uma medição real (documentada no docstring do arquivo e no SUMMARY.md); a suíte completa permanece em 825 passed/16 skipped/1 failed; commit criado só com o arquivo de teste, sem coautoria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dataset sintético (gerado em memória no teste) ↔ `build_processing_plan`/`processar_pedidos` | Não há fronteira de confiança real: o dataset é gerado por este mesmo teste, sem I/O externo, sem input de usuário. Listado aqui só para registrar explicitamente por que Spoofing/Tampering/Repudiation/Information Disclosure/Elevation of Privilege NÃO se aplicam a este plano — é um teste síncrono, sem rede, sem autenticação, sem dado de cliente real. |
| Volume elegível do canal (produção) ↔ orçamento de memória do worker dedicado | Esta é a fronteira que o critério 4 do ROADMAP realmente protege: o worker que roda `_plan_once`/`build_processing_plan` tem memória reservada finita (1280-1536 MiB, per comentário de `processing/domain.py`); um volume elegível real maior que o testado aqui poderia estourar esse orçamento em produção, mesmo que este teste passe. |

## STRIDE Threat Register

A única categoria STRIDE aplicável a este plano é **Denial of Service por complexidade algorítmica/crescimento de memória no volume elegível de SEM_ADEQUAR** — é literalmente o motivo deste teste existir. As demais cinco categorias (Spoofing, Tampering, Repudiation, Information Disclosure, Elevation of Privilege) não se aplicam: o artefato é um único arquivo de teste síncrono, sem I/O de rede/banco, sem autenticação, operando sobre dados sintéticos gerados no próprio processo — não há superfície de ataque real a mitigar além da que seria genérica de "qualquer teste" (falso verde, que é tratado no Passo 2b como resultado consciente, não como ameaça de segurança).

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|------------|-------------|------------------|
| T-15-04-01 | Denial of Service (memória do worker esgotada por volume elegível de SEM_ADEQUAR maior que o projetado, dado que o modo não filtra crédito antes desta fase — 15-RESEARCH.md Assumption A2) | HIGH | `app/modules/pedidos/processing/domain.py::build_processing_plan` em modo `SEM_ADEQUAR`, executado pelo worker dedicado (`processing/application/service.py::_plan_once`) | mitigate | Este teste mede empiricamente o pico de RSS incremental sobre um dataset representativo (45.000 pares/112.500 itens, dentro dos hard caps já existentes `PLAN_PAIR_LIMIT`/`PLAN_ITEM_LIMIT`/`MAX_PLAN_TOTAL_BYTES`) e falha se o crescimento não ficar dentro do teto calibrado; a Task 2 Passo 2b exige, se o pico real ultrapassar a folga esperada, registrar o achado com destaque no SUMMARY.md (não apenas ajustar o número), apontando a alternativa já documentada de processar por canal em vez de "Todos" de uma vez como próximo passo fora de escopo deste plano. |
| T-15-04-02 | Tampering (falso verde por teto de memória afrouxado silenciosamente até passar, mascarando um problema real de escala) | MEDIUM | `_MEMORY_CEILING_MIB` em `app/tests/test_pedidos_processing_sem_adequar_memory_024.py` | mitigate | Task 2 Passo 2b proíbe explicitamente reduzir `_SYNTHETIC_PAIR_COUNT` ou remover as asserções de volume para "fazer passar"; qualquer recalibração do teto exige documentar o número real observado e a data, tanto no arquivo quanto no SUMMARY.md — a diferença entre "calibrar conscientemente" e "afrouxar até passar" é o próprio critério de aceite deste plano. |
| T-15-04-SC | Tampering (supply chain — dependência nova instalada fora do escopo aprovado) | LOW | `pyproject.toml` | mitigate | Este plano usa apenas `resource` (stdlib) e não instala nenhum pacote novo — não há checkpoint de legitimidade de pacote a aplicar aqui. |

Nenhuma ameaça fica sem disposição: T-15-04-01 é a razão de existir deste plano (medição, não eliminação, do risco — a eliminação real, se necessária, é uma decisão de arquitetura fora de escopo); T-15-04-02 é o risco de processo que o próprio texto do Passo 2b da Task 2 existe para impedir.
</threat_model>

## Artifacts this phase produces

- `app/tests/test_pedidos_processing_sem_adequar_memory_024.py` (novo) — único arquivo tocado por este plano. Contém: guard de plataforma (`skipif` fora de Linux), gerador de dataset sintético `_construir_canal_sintetico` (45.000 pares / 112.500 itens, estoque abundante, crédito ok, referência Linx completa), e o teste `test_sem_adequar_pico_de_memoria_do_canal_inteiro_fica_dentro_do_orcamento_do_worker` que mede `resource.getrusage(resource.RUSAGE_SELF).ru_maxrss` antes/depois da hidratação e antes/depois da chamada a `build_processing_plan` em modo `SEM_ADEQUAR`, com asserções de volume (candidate_count, rows, deferred_count, blocked_credit_count) antes da asserção de memória, e o teto `_MEMORY_CEILING_MIB` calibrado a partir do número real observado na Task 2, documentado no docstring do módulo.
- Nenhum arquivo de produção é criado ou modificado.

<verification>
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_sem_adequar_memory_024.py -q -s` — verde, isolado, com o número real de pico de memória visível na saída.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) — `825 passed, 16 skipped, 1 failed` (a falha conhecida), sem regressão.
- `git status --short -- app/modules app/shared alembic` — vazio, prova de que nenhum arquivo de produção foi tocado.
</verification>

<success_criteria>
- O critério 4 do ROADMAP da Fase 15 tem, pela primeira vez, uma prova automatizada e reproduzível (não uma medição manual antiga) do pico de memória de `SEM_ADEQUAR` planejado pelo caminho global.
- O teto de memória usado na asserção reflete uma calibração consciente a partir de um número real medido nesta execução — documentado tanto no arquivo de teste quanto no SUMMARY.md — nunca um valor afrouxado silenciosamente até passar.
- Se o pico real sugerir que o volume elegível de SEM_ADEQUAR se aproxima ou excede o orçamento do worker, isso está registrado como achado explícito no SUMMARY.md, não escondido atrás de um teste verde.
- Suíte completa do repositório permanece em 825 passed / 16 skipped / 1 failed (a falha conhecida); nenhum arquivo de produção foi tocado por este plano.
</success_criteria>

<output>
Create `.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-04-SUMMARY.md` when done
</output>
