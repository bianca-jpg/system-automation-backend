---
phase: 15-roteamento-global-do-modo-sem-adequa-o
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - app/modules/pedidos/processing/infrastructure/repository.py
  - app/tests/test_pedidos_processing_repository.py
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Aplicar um plano tipo='sem' cuja quantidade solicitada excede o estoque disponível é rejeitado com ProcessingPlanConflict('processing_plan_stale'), e nenhuma OrdemReserva é gravada"
    - "Aplicar um plano tipo='sem' cuja quantidade solicitada cabe no estoque disponível continua funcionando exatamente como hoje (nenhuma regressão nos testes existentes que usam tipo='sem' com estoque suficiente)"
    - "Planos tipo='com' continuam sendo revalidados exatamente como antes desta mudança (comportamento pré-existente preservado)"
  artifacts:
    - path: "app/modules/pedidos/processing/infrastructure/repository.py"
      provides: "_assert_stock_capacity revalida capacidade de estoque para tipo in {'com', 'sem'} — sem o desvio anterior que só olhava 'com'"
      contains: "_assert_stock_capacity"
    - path: "app/tests/test_pedidos_processing_repository.py"
      provides: "test_sem_adequar_revalidates_stock_before_writes prova a rejeição para tipo='sem' acima do estoque disponível"
      contains: "test_sem_adequar_revalidates_stock_before_writes"
  key_links:
    - from: "app/modules/pedidos/processing/infrastructure/repository.py::apply_pairs"
      to: "app/modules/pedidos/processing/infrastructure/repository.py::_assert_stock_capacity"
      via: "await self._assert_stock_capacity(rows) chamado antes do INSERT em OrdemReserva, para qualquer tipo de par"
      pattern: "await self\\._assert_stock_capacity\\(rows\\)"
---

<objective>
Corrigir `_assert_stock_capacity` (`app/modules/pedidos/processing/infrastructure/repository.py:489-513`) para revalidar capacidade de estoque também quando `tipo == "sem"`, não apenas `tipo == "com"` como hoje. Fechar o gap com um teste novo que prova a rejeição — nenhum teste existente cobre isso hoje.

**Por que isto importa agora:** o modo `sem_adequar` ainda não reserva estoque compartilhado, então este bug é inofensivo neste instante. Ele vira um gap real assim que o plano 15-03 rotear `SEM_ADEQUAR` pelo caminho global e ele passar a planejar contra estoque — nesse momento, sem este fix, o `sem_adequar` aplicaria a escrita **sem** o double-check que o `adequar` já tem, e uma corrida entre planejar e aplicar poderia reservar acima do disponível. Este plano fecha o buraco antes que exista o que cair nele.

Purpose: FIX-01 — paridade de revalidação de estoque entre os dois modos na escrita, provada por teste, antes que 15-03 torne `sem_adequar` dependente de estoque.
Output: `_assert_stock_capacity` sem o desvio `if tipo != "com": continue`; `test_sem_adequar_revalidates_stock_before_writes` novo em `test_pedidos_processing_repository.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-CONTEXT.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-PATTERNS.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md

@app/modules/pedidos/processing/infrastructure/repository.py
@app/tests/test_pedidos_processing_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX-01 — revalidar capacidade de estoque também para tipo="sem"</name>
  <files>app/modules/pedidos/processing/infrastructure/repository.py, app/tests/test_pedidos_processing_repository.py</files>
  <read_first>
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-CONTEXT.md — seção `<decisions>` § "FIX-01": `_assert_stock_capacity` hoje pula a revalidação quando `tipo != "com"`; nenhum teste existente falharia se o fix fosse esquecido, por isso o teste novo é a entrega principal, não acessório.
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-PATTERNS.md — seção "5. Teste de revalidação de capacidade de estoque (FIX-01) — molde direto": o molde é `test_adequation_revalidates_stock_before_writes` (linhas 382-396 de `test_pedidos_processing_repository.py`); a única mudança de dado é `tipo="com"` → `tipo="sem"`; a linha `if tipo != "com": continue` é exatamente o que este fix remove — não copiá-la para o teste novo.
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md — critério de sucesso 3 e invariante J4: "Plano de `sem_adequar` que exceda o estoque na escrita é rejeitado, igual ao `adequar`".
    - app/modules/pedidos/processing/infrastructure/repository.py, linhas 489-513 (`_assert_stock_capacity` completa): note a linha 492 `tipo, items, _linx, _targets = self._payload(row)` e as linhas 493-494 `if tipo != "com": continue` — são as duas linhas centrais deste fix. `_payload` (linha 366-368) só aceita `tipo in {"com", "sem"}`; qualquer outro valor já levanta `ProcessingPlanConflict("processing_plan_payload_invalid")` antes de chegar aqui, então o `if` removido não precisa de substituto — passa a tratar os dois tipos válidos igualmente.
    - app/tests/test_pedidos_processing_repository.py, linhas 382-396 (`test_adequation_revalidates_stock_before_writes`, o molde) e os helpers que ele usa: `_seed_source` (linhas 211-235, semeia `pedidos` + `estoque` com `qt_disponivel=stock_qty`), `_planned_pair` (linhas 153-172, default `tipo="sem"`, `qty=2`), `_create_job_and_plan` (linhas 175-208, usa `ProcessingMode.SEM_ADEQUAR` internamente já hoje — não muda com este fix).
  </read_first>
  <behavior>
    - RED: adicionar `test_sem_adequar_revalidates_stock_before_writes` copiando `test_adequation_revalidates_stock_before_writes` linha a linha, trocando apenas `pair = _planned_pair(tipo="com", qty=2)` por `pair = _planned_pair(tipo="sem", qty=2)`. Contra o código atual (antes do fix), este teste falha com "DID NOT RAISE ProcessingPlanConflict": `_seed_source(db, stock_qty=1)` deixa só 1 unidade disponível, o par pede `qty=2`, mas `_assert_stock_capacity` hoje pula a checagem inteira para `tipo="sem"` (o `if tipo != "com": continue`), então `apply_pairs` grava a `OrdemReserva` sem erro — o oposto do que o teste espera. Confirmar essa falha antes de aplicar o fix (evidência de que o teste exercita o comportamento real, não um mock).
    - GREEN: remover as duas linhas `if tipo != "com": continue` de `_assert_stock_capacity`, e renomear a variável `tipo` (que passa a não ser lida) para `_tipo` na mesma linha de desestruturação, seguindo a convenção já usada para `_linx`/`_targets` na mesma linha. Rodar o teste novo e confirmar que passa; rodar `test_adequation_revalidates_stock_before_writes` (tipo="com") e confirmar que continua passando sem alteração de comportamento.
    - Verificar que os testes existentes deste mesmo arquivo que aplicam pares `tipo="sem"` com estoque suficiente (`test_writer_applies_pair_and_checkpoint_in_same_transaction` e `test_writer_rolls_back_all_outputs_before_checkpoint`, ambos com `_seed_source(db)` no default `stock_qty=20` e `_planned_pair()` no default `qty=2`) continuam passando — provam que o fix rejeita só o caso de estoque insuficiente, não todo write de `tipo="sem"`.
  </behavior>
  <action>
Em `app/modules/pedidos/processing/infrastructure/repository.py`, dentro de `_assert_stock_capacity` (linhas 489-502), remover as linhas `if tipo != "com": continue` (hoje 493-494), de modo que o laço acumule `requested[(row.cd_prod_cor, size, channel)] += qty` para QUALQUER par retornado por `self._payload(row)`, independente de `tipo`. Como `tipo` deixa de ser lido no corpo do laço após essa remoção, renomear o alvo de desestruturação de `tipo` para `_tipo` na linha `tipo, items, _linx, _targets = self._payload(row)` → `_tipo, items, _linx, _targets = self._payload(row)`. Não alterar mais nada nesta função: o chunking via `_chunks(targets, STOCK_TARGET_CHUNK_SIZE)`, a chamada a `estoque_repo.carregar_estoque_disponivel_alvos`, e a comparação final `any(requested[target] > available.get(target, 0) for target in targets)` permanecem byte-idênticos. Não tocar `apply_pairs`, `_assert_plan_still_current`, `_payload`, nem qualquer outro método do arquivo.

Em `app/tests/test_pedidos_processing_repository.py`, adicionar `test_sem_adequar_revalidates_stock_before_writes` logo após `test_adequation_revalidates_stock_before_writes` (após a linha 396), com o corpo idêntico ao molde exceto a troca de `tipo="com"` por `tipo="sem"` em `_planned_pair(tipo="sem", qty=2)` (explícito, mesmo sendo o default de `_planned_pair`, por legibilidade — per 15-PATTERNS.md). Manter idênticos: `await _seed_source(db, stock_qty=1)`, `repository, spec = await _create_job_and_plan(db, pair)`, `await db.commit()`, `chunk = await repository.load_next_chunk(job_id=spec.job_id, limit=250)`, o bloco `with pytest.raises(ProcessingPlanConflict, match="processing_plan_stale"): await SqlAlchemyProcessingWriter(db).apply_pairs(chunk)`, `await db.rollback()`, e o assert final `assert await db.scalar(select(OrdemReserva.nr_pedido)) is None` num segundo bloco `async with factory() as db:`. Nenhum import novo é necessário — `ProcessingPlanConflict`, `SqlAlchemyProcessingWriter`, `select`, `OrdemReserva` já estão importados no topo do arquivo.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py::test_sem_adequar_revalidates_stock_before_writes -q` termina com `1 passed` — prova direta de que aplicar `tipo="sem"` acima do estoque disponível é rejeitado.
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py::test_adequation_revalidates_stock_before_writes -q` continua com `1 passed` — o caso `tipo="com"` pré-existente não regrediu.
    - `grep -n 'if tipo != "com"' app/modules/pedidos/processing/infrastructure/repository.py | grep -v '^#'` não retorna nenhuma linha — o desvio foi removido, não apenas comentado.
    - `grep -c '_tipo, items, _linx, _targets = self._payload(row)' app/modules/pedidos/processing/infrastructure/repository.py` retorna `1` — a variável não lida foi renomeada com underscore, sem deixar `tipo` morto no escopo.
    - Suíte completa (`docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q`) termina em exatamente **825 passed, 16 skipped, 1 failed**, com a única falha sendo `app/tests/test_pedidos_read_projection.py::test_read_projection_empty_contracts_execute_real_sql` (teardown asyncpg pré-existente) — nenhuma regressão nova introduzida ao ampliar a revalidação para `tipo == "sem"`.
    - `test_writer_applies_pair_and_checkpoint_in_same_transaction` e `test_writer_rolls_back_all_outputs_before_checkpoint` (ambos usam `tipo="sem"` default com estoque suficiente) continuam `passed` — o fix rejeita só o caso de estoque insuficiente, não todo write de `sem_adequar`.
  </acceptance_criteria>
  <done>`_assert_stock_capacity` revalida capacidade de estoque para `tipo in {"com", "sem"}` sem exceção; `test_sem_adequar_revalidates_stock_before_writes` prova que um plano `tipo="sem"` acima do estoque disponível é rejeitado com `ProcessingPlanConflict("processing_plan_stale")` e não grava `OrdemReserva`; a suíte completa permanece na linha de base conhecida (825 passed, 16 skipped, 1 failed já catalogado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `apply_pairs` (grava `OrdemReserva`) → `_assert_stock_capacity` | Único ponto que impede reservar estoque acima do fisicamente disponível **na escrita** (não só no planejamento); até este fix, todo par `tipo="sem"` atravessava esse ponto sem checagem alguma |
| Estoque compartilhado entre pedidos concorrentes (mesmo `cd_prod_cor`/`sg_tamanho`/`canal`) | Dois jobs de processamento — ou um job e uma corrida planejar→aplicar dentro do mesmo job — podem disputar o mesmo alvo de estoque; a revalidação na escrita é a defesa contra essa janela, distinta da checagem feita no momento do planejamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | `_assert_stock_capacity` — integridade do estoque físico ao aplicar um plano `tipo="sem"` que exceda o disponível | mitigate | Remover o desvio `if tipo != "com": continue`; `test_sem_adequar_revalidates_stock_before_writes` prova, com `stock_qty=1` e `qty=2`, que `ProcessingPlanConflict("processing_plan_stale")` é levantado e nenhuma `OrdemReserva` é gravada. Efeito de negócio direto: reservar acima do disponível gera OR que o Linx/ERP não consegue atender — é justamente o que a revalidação existe para impedir, e o gap ficava invisível porque `sem_adequar` ainda não reserva estoque compartilhado (o plano 15-03 muda isso). |
| T-15-02-02 | Tampering | Regressão silenciosa no caminho `tipo="com"` já testado, ou nos testes existentes de `tipo="sem"` com estoque suficiente | mitigate | Acceptance criteria exige rodar `test_adequation_revalidates_stock_before_writes` (o caso `com` pré-existente) e os dois testes de writer que usam `sem_adequar` com estoque suficiente (`test_writer_applies_pair_and_checkpoint_in_same_transaction`, `test_writer_rolls_back_all_outputs_before_checkpoint`) e confirmar que continuam `passed`; suíte completa deve fechar na linha de base exata (825 passed / 16 skipped / 1 failed conhecido) — qualquer novo `failed` bloqueia a wave. |
| T-15-02-SC | Tampering | npm/pip/cargo installs | accept | Este plano não instala nenhum pacote novo — edita uma função existente e adiciona um teste reaproveitando helpers e imports já presentes no arquivo. |

Nenhuma ameaça de severidade relevante permanece sem mitigação testável. O escopo deste plano é estritamente a correção de `_assert_stock_capacity` — o efeito prático (bloquear reserva acima do estoque) só se torna observável em produção quando o plano 15-03 fizer `sem_adequar` planejar contra estoque; até lá, este fix é defesa em profundidade sem mudança de comportamento visível ao usuário.
</threat_model>

<verification>
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py -q` — verde, incluindo o teste novo (`tipo="sem"`) e o molde pré-existente (`tipo="com"`) lado a lado.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) — termina em exatamente 825 passed, 16 skipped, 1 failed (o teardown asyncpg já catalogado). Nenhuma wave desta fase pode terminar vermelha.
- `grep -n 'if tipo != "com"' app/modules/pedidos/processing/infrastructure/repository.py` não retorna nenhuma linha.
</verification>

<success_criteria>
- `_assert_stock_capacity` revalida capacidade de estoque para `tipo in {"com", "sem"}` — o desvio que só olhava `"com"` não existe mais.
- Teste novo (`test_sem_adequar_revalidates_stock_before_writes`) prova, por integração real com Postgres via `processing_database`, que aplicar um plano `tipo="sem"` acima do estoque disponível é rejeitado com `ProcessingPlanConflict("processing_plan_stale")` e não grava `OrdemReserva` — a invariante J4 do `15-VALIDATION.md` e o critério de sucesso 3 do ROADMAP.
- Nenhuma regressão: o caso `tipo="com"` pré-existente e os testes de `tipo="sem"` com estoque suficiente continuam verdes; suíte completa na linha de base exata (825 passed, 16 skipped, 1 failed conhecido).
- Nenhum arquivo fora de `app/modules/pedidos/processing/infrastructure/repository.py` e `app/tests/test_pedidos_processing_repository.py` foi tocado.
</success_criteria>

## Artifacts this phase produces

- `app/modules/pedidos/processing/infrastructure/repository.py` (modificado) — `_assert_stock_capacity` sem o desvio `if tipo != "com": continue`; variável de desestruturação renomeada para `_tipo` (não lida). Nenhuma outra linha da função muda.
- `app/tests/test_pedidos_processing_repository.py` (modificado) — `test_sem_adequar_revalidates_stock_before_writes` (novo), espelhando `test_adequation_revalidates_stock_before_writes` com `tipo="sem"` no lugar de `tipo="com"`, provando a rejeição por estoque insuficiente também neste modo.

<output>
Create `.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-02-SUMMARY.md` when done
</output>
