---
phase: 15-roteamento-global-do-modo-sem-adequa-o
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - app/modules/pedidos/processing/domain.py
  - app/modules/pedidos/processing/application/service.py
  - app/modules/pedidos/processing/application/ports.py
  - app/modules/pedidos/processing/infrastructure/adapters.py
  - app/modules/pedidos/processing/infrastructure/repository.py
  - app/tests/test_pedidos_processing_domain.py
  - app/tests/test_pedidos_processing_service.py
  - app/tests/test_pedidos_processing_repository.py
  - app/tests/test_pedidos_processing_stream_repository.py
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "Um par sem crédito NÃO entra em OR no modo sem adequação (J1), e um par com grade incompleta/estoque insuficiente NÃO entra em OR no modo sem adequação (J2) — os dois casos passam a existir porque o modo deixou de aceitar todo par elegível incondicionalmente"
    - "`deferred_count` e `blocked_credit_count` do plano deixam de ser 0 no modo sem adequação quando há pares recusados (J3) — vêm de `result['preteridos']`/`result['bloqueados_credito']` do motor, não de constantes"
    - "`_plan_once` tem UM caminho só: hidrata a foto inteira do canal e chama `build_processing_plan` nos dois modos; nenhuma decisão de prioridade ou estoque depende de em qual página de 250 pares um par caiu (critério 1 do ROADMAP)"
    - "Processar o mesmo pedido em duas execuções sucessivas não concede 5% novos na segunda: a segunda execução RECUSA o corte que o placeholder de execução única teria aceitado — provado no mesmo teste, comparando a chamada com orçamento real contra a chamada sem orçamento (critério 5 do ROADMAP)"
    - "O `logger.warning` citando ALOC-09 NÃO é emitido quando o caminho de produção (`_plan_once` → `build_processing_plan` → `processar_pedidos`) fornece os dados reais (J6)"
    - "Nenhum símbolo de streaming permanece no repositório (J7): `grep -rn --include='*.py' 'StreamingPlanBuilder\\|PLANNING_PAIR_PAGE_SIZE\\|prepare_pending_pair_stream\\|load_pending_pair_page\\|append_plan_rows\\|finalize_streamed_plan' app/modules app/tests` retorna vazio (critério 2 do ROADMAP)"
  artifacts:
    - path: "app/modules/pedidos/processing/domain.py"
      provides: "`build_processing_plan` com caminho único: os dois modos chamam `processar_pedidos` (com `modo=ModoAdequacao.SEM_ADEQUAR` ou `ADEQUAR`), recebem e repassam os 3 mappings de orçamento, e derivam `deferred_count`/`blocked_credit_count` do resultado real. `StreamingPlanBuilder`, `PLANNING_PAIR_PAGE_SIZE`, `build_pair_payload` e `PlanSummary` deixam de existir"
      contains: "ModoAdequacao.SEM_ADEQUAR"
    - path: "app/modules/pedidos/processing/application/service.py"
      provides: "`_plan_once` sem o ramo de streaming: hidratação global, `load_stock`/`load_adequation_config`/`load_pedido_budget` nos dois modos, plano persistido sempre por `store_plan`"
      contains: "load_pedido_budget"
    - path: "app/modules/pedidos/processing/application/ports.py"
      provides: "`ProcessingPlannerSource` sem `prepare_pending_pair_stream`/`load_pending_pair_page`; `ProcessingRepository` sem `append_plan_rows`/`finalize_streamed_plan`"
    - path: "app/modules/pedidos/processing/infrastructure/adapters.py"
      provides: "`SqlAlchemyProcessingPlannerSource` sem os dois métodos de paginação keyset e sem a TEMP TABLE `orders_processing_pending_pairs`"
    - path: "app/modules/pedidos/processing/infrastructure/repository.py"
      provides: "`SqlAlchemyProcessingRepository` sem `append_plan_rows`/`finalize_streamed_plan` — `_assert_stock_capacity` permanece INTOCADO (escopo do plano 15-02)"
    - path: "app/tests/test_pedidos_processing_service.py"
      provides: "Teste do caminho global de `sem_adequar` (substitui o teste de paginação) + teste de que o orçamento real é lido e passado, com `caplog` provando a ausência do aviso ALOC-09"
      contains: "ALOC-09"
    - path: "app/tests/test_pedidos_processing_repository.py"
      provides: "Teste de integração de duas execuções sucessivas provando o orçamento acumulado (ALOC-09 fechado); teste de paginação keyset removido"
      contains: "execucoes_sucessivas"
    - path: "app/tests/test_pedidos_processing_stream_repository.py"
      provides: "ARQUIVO DELETADO — testava só `append_plan_rows`/`finalize_streamed_plan`"
  key_links:
    - from: "app/modules/pedidos/processing/domain.py::build_processing_plan"
      to: "app/modules/pedidos/domain/motor_adequacao.py::processar_pedidos"
      via: "chamada única parametrizada por `modo`, substituindo o `selected = {pair: items ...}` do ramo else"
      pattern: "modo=ModoAdequacao"
    - from: "app/modules/pedidos/processing/application/service.py::_plan_once"
      to: "app/modules/pedidos/processing/application/ports.py::ProcessingPlannerSource.load_pedido_budget"
      via: "chamada com o conjunto de `nr_pedido` extraído de `pending_items`, nos dois modos"
      pattern: "await planner.load_pedido_budget"
    - from: "app/modules/pedidos/processing/application/service.py::_plan_once"
      to: "app/modules/pedidos/processing/domain.py::build_processing_plan"
      via: "os 3 dicts de orçamento repassados como argumentos nomeados"
      pattern: "total_original_por_pedido="
---

<objective>
Levar o modo `SEM_ADEQUAR` para o mesmo caminho global que já serve o `ADEQUAR` (`build_processing_plan` + `processar_pedidos`), ligar o orçamento real lido pelo plano 15-01 nos dois modos (fechando o ALOC-09), e **apagar o código de streaming que fica órfão na mesma entrega** — como uma sequência de tasks: implementar → trocar → apagar → verificar.

Purpose: hoje `sem_adequar` decide por página de 250 pares e aceita **todo par elegível incondicionalmente** (`selected = eligible`, sem crédito, sem estoque, sem furo de grade), com `deferred_count`/`blocked_credit_count` zerados por construção. Prioridade por valor e estoque compartilhado são decisões que uma página não consegue tomar — ela não sabe se existe, na página seguinte, um pedido de maior prioridade disputando o mesmo produto. E o orçamento ±5% da Phase 14 ainda roda com um placeholder de execução única que **não** respeita o acumulado entre execuções.

**Por que isto é UM plano e não três:** rotear numa entrega e remover o streaming em outra criaria uma janela real com dois caminhos concorrentes fazendo a mesma coisa — o anti-padrão que este projeto já pagou caro (uma trava de UI ficou órfã por um redesenho e foi apagada meses depois como "feature morta", sem que ninguém percebesse que a intenção de produto continuava válida). **Não divida este plano.**

Output: `SEM_ADEQUAR` planejado pelo caminho global com contadores reais; `processar_pedidos` recebendo total original e consumido prévio reais nos dois modos; zero símbolo de streaming no repositório.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-CONTEXT.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-RESEARCH.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-PATTERNS.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md
@.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-01-PLAN.md

**Antes de editar:** consultar o coding standards via MCP Backstage (`backstage_get_coding_standards`), per `.claude/CLAUDE.md`.

**Dependência de entrada (plano 15-01, já commitado):** `ProcessingPlannerSource.load_pedido_budget(nr_pedidos: set[int]) -> tuple[dict[int, int], dict[int, int], dict[int, int]]`, retornando **nesta ordem** `(total_original_por_pedido, consumido_previo_adicao_por_pedido, consumido_previo_corte_por_pedido)` — os mesmos nomes e a mesma ordem dos 3 parâmetros keyword-only opcionais que `processar_pedidos` aceita desde a Phase 14. O `SELECT` final parte de `pedido_scope`, então **todo** `nr_pedido` pedido aparece nos três dicts (com zeros quando não há OR anterior) — isso é o que faz o fallback de placeholder do motor nunca disparar. **Confirme a assinatura no código antes de usar**, não só no plano.

**Restrições desta fase (válidas para as 4 tasks):**
- Testes **sempre** via Docker: `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q`. `uv run pytest` local falha no Windows (pyicu).
- **Este plano NÃO tem exceção de wave vermelha.** Diferente da Phase 14, o critério de saída é suíte completa verde, **sem `--deselect`, sem `-k`**. A única falha permitida é `app/tests/test_pedidos_read_projection.py::test_read_projection_empty_contracts_execute_real_sql` (`RuntimeError: Event loop is closed`, teardown asyncpg, pré-existente). **Não conserte essa falha.**
- **Linha de base de entrada: 825 passed, 16 skipped, 1 failed.** Este plano **remove 7 testes** (5 do arquivo de streaming, 1 do `StreamingPlanBuilder`, 1 da paginação keyset) e **adiciona 3** (2 em `_service.py`, 1 em `_repository.py`), então a saída esperada é **821 passed, 16 skipped, 1 failed**. Um número diferente não é motivo para parar — é motivo para **reconciliar explicitamente** (qual teste apareceu/sumiu) e registrar no SUMMARY. Delta não explicado = pare.
- **NÃO tocar `_assert_stock_capacity`** em `repository.py` — é escopo do plano 15-02 (wave 2). Se o roteamento fizer `sem_adequar` reservar estoque antes do 15-02 aterrissar, um teste pode acusar falso-negativo: **documente a dependência de integração no SUMMARY, não corrija aqui.**
- **NÃO usar `gsd-tools query state.advance-plan`** (corrompe a frontmatter YAML de `.planning/STATE.md`). Atualizar com Edit.
- **NÃO iniciar comandos em background** para esperar a suíte terminar.
- Working tree compartilhado com trabalho de SSO Microsoft não commitado de outra frente. `git add` **sempre** com caminhos explícitos — nunca `git add -A`/`git add .`. Conferir com `git show --stat` depois de cada commit.
- Commits Conventional Commits, escopo `15-03`, **sem coautoria de Claude** (memória permanente do usuário para este projeto). Branch `develop`.
- **Não tocar `docs/adequacao.md`** — é o plano 15-05. Comentários desatualizados **dentro** de `domain.py` são escopo desta entrega.
- Chaves de retorno de `processar_pedidos` **não mudam de forma**.

**Mudança de comportamento esperada, não regressão:** depois desta entrega o volume de ORs geradas em `sem_adequar` **vai cair**, porque o modo passa a recusar por crédito (`is_sem_credito`), por grade incompleta (`tem_furo_de_grade`) e por estoque insuficiente (`aplicar_tudo_ou_nada`, tudo-ou-nada). Isso é o comportamento CORRETO. A diferença tem de ser explicada por `deferredCount` + `blockedCreditCount`; se a soma não fecha, aí sim há bug.
</context>

<tasks>

<task type="auto">
  <name>Task 1: IMPLEMENTAR — rotear SEM_ADEQUAR dentro de `build_processing_plan` (aditivo, sem tocar `service.py`)</name>
  <files>app/modules/pedidos/processing/domain.py, app/tests/test_pedidos_processing_domain.py</files>
  <read_first>
    - app/modules/pedidos/processing/domain.py (linhas 618-716: `build_processing_plan` — o ramo `ADEQUAR` em 641-666 é o molde exato; o ramo `else` em 667-669 é o que sai)
    - app/modules/pedidos/domain/motor_adequacao.py (linhas 455-502: assinatura de `processar_pedidos` e as 5 chaves de retorno; linhas 740-782: o ramo `SEM_ADEQUAR` de `_processar_pedidos_canal` — furo de grade + tudo-ou-nada, **nunca** toca o ledger nem chama `_resolver_orcamento_pedido`; linhas 306-341: `aplicar_tudo_ou_nada` — em caso de sucesso escreve `status_item="Gerar OR"`, `qt_solicitada`, `diff_valor` em cópias dos itens; linhas 710-728: pedidos sem crédito vão para `bloqueados_credito` e **não** para `preteridos`)
    - app/modules/pedidos/domain/politica_quantidade.py (linhas 25-27: `ModoAdequacao`, a StrEnum que `processar_pedidos` espera em `modo=`)
    - app/modules/pedidos/domain/value_objects.py (linhas 36-38: `montar_chave_estoque` = `f"{cd_prod_cor}_{sg_tamanho}"`, sem normalização — é o formato das chaves das fixtures de estoque)
    - app/tests/test_pedidos_processing_domain.py (linhas 30-51: helper `_item`; linhas 94-154 e 231-259: os 4 testes que chamam `build_processing_plan(mode=SEM_ADEQUAR, ...)` **sem `stock`**)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md § "Riscos de validação específicos desta fase", item 2 (fixture faltando ≠ regra mudada)
  </read_first>
  <action>
    Importe `ModoAdequacao` de `app.modules.pedidos.domain.politica_quantidade` em `processing/domain.py` (não há ciclo: `domain.py` já importa `motor_adequacao`, que já importa `politica_quantidade`). Não use `ProcessingMode` como valor de `modo=` — as duas StrEnum coincidem por acaso nos valores, e depender disso é acoplamento implícito.

    Estenda a assinatura de `build_processing_plan` com três parâmetros keyword-only opcionais, `total_original_por_pedido`, `consumido_previo_adicao_por_pedido` e `consumido_previo_corte_por_pedido`, todos `Mapping[int, int] | None = None` — **mesmos nomes** dos parâmetros de `processar_pedidos`, para que o repasse seja literal e o plano 15-03/Task 2 possa desempacotar o retorno de `load_pedido_budget` direto nos argumentos nomeados.

    Colapse o `if request.mode is ProcessingMode.ADEQUAR: ... else: ...` num caminho único:
    - Derive `modo = ModoAdequacao.ADEQUAR if request.mode is ProcessingMode.ADEQUAR else ModoAdequacao.SEM_ADEQUAR` e `tipo = "com" if request.mode is ProcessingMode.ADEQUAR else "sem"`.
    - **Mantenha a guarda `if stock is None: raise InvalidProcessingRequest("adequation_requires_stock_snapshot")` DEPOIS de `_eligible_pairs`** (onde ela já está hoje), nunca antes. Motivo concreto: `test_mixed_channel_pair_fails_closed_for_every_scope` chama sem `stock` e precisa continuar falhando com `pending_pair_channel_is_not_canonical` — validação de canal tem precedência sobre a de estoque. A guarda passa a valer para os **dois** modos (o modo sem adequação agora também precisa da foto de estoque); **preserve a string do erro** — nenhum teste ou rota depende dela hoje, e trocá-la no mesmo commit de uma mudança de comportamento seria churn desnecessário.
    - Achate `eligible` em `flat` na ordem `sorted(eligible)` (idêntico ao que o ramo `ADEQUAR` já faz, incluindo os `_ensure_not_cancelled`), chame `processar_pedidos(flat, {channel: dict(values) for channel, values in stock.items()}, set(), criterion, tolerance, modo=modo, resultados_apenas_selecionados=True, cancel_token=cancel_token, total_original_por_pedido=..., consumido_previo_adicao_por_pedido=..., consumido_previo_corte_por_pedido=...)` e mantenha o `except PlanningCancellationRequested` → `ProcessingPlanningCancelled` envolvendo a chamada nos dois modos.
    - `selected` vem de `result["resultados"]` nos pares de `sorted(result["pares_processados"])`; `deferred_count += len(result["preteridos"])`; `blocked_credit_count = len(set(result["bloqueados_credito"]))`. Nada de constante zerada para `sem_adequar`.

    O bloco que monta `rows`/`plan_hash`/`PlanDraft` (linhas 671-715) **não muda** — `_payload_for_pair` continua recebendo `items=selected[pair]` (agora os itens processados pelo motor, com `status_item`/`qt_solicitada`, exatamente como o `ADEQUAR` já entrega) e `source_items=eligible[pair]` (a foto original, que é o que o `sourceHash` congela e o writer revalida). Não altere essa distinção.

    **Repasse do orçamento nos dois modos, por decisão do usuário:** a pesquisa sugeria condicionar a leitura ao modo `ADEQUAR` (o `SEM_ADEQUAR` ignora os mappings porque `_resolver_orcamento_pedido` não é chamado no ramo tudo-ou-nada). A decisão travada é passar nos dois — o custo é uma query agregada por execução e o ganho é eliminar uma classe inteira de bug de wiring (nunca existe um caminho onde "esqueceram de ligar"). Registre esse contraste em uma linha de comentário junto da chamada.

    Nos testes de `test_pedidos_processing_domain.py`, **adicione fixtures de estoque sem enfraquecer nenhuma asserção existente** (todas as asserções de `candidate_count`, `deferred_count`, `rows`, `linx`, `stockTargets`, `payload_size` continuam como estão). As chaves são `f"{cd_prod_cor}_{sg_tamanho}"` por canal, com quantidade `>=` a pedida em **todos** os tamanhos do par (tudo-ou-nada reprova a grade inteira por falta em um único tamanho):
    - `test_exact_channel_excludes_other_channel_without_counting_deferred`: `stock={"Franquia": {"PROD_36": 2}}`.
    - `test_plan_preserves_numeric_sizes_and_freezes_linx_and_source_hash`: `stock={"Franquia": {"SKU|01_36": 2, "SKU|01_37": 3}}`.
    - `test_plan_aborts_during_build_when_cumulative_payload_exceeds_cap`: `stock={"Franquia": {"A_36": 2, "B_36": 2}}` nas **duas** chamadas a `build_processing_plan` do teste.
    - `test_mixed_channel_pair_fails_closed_for_every_scope`: **não toque** — continua sem `stock`, provando que a validação de canal precede a de estoque.

    Adicione **um** teste novo em `test_pedidos_processing_domain.py`, `test_sem_adequar_now_defers_by_stock_and_blocks_by_credit`, cobrindo J1/J2/J3 no nível puro, em canal `FRANQUIA`: pedido 1/produto "A" com estoque suficiente (entra em `rows`); pedido 2/produto "B" com estoque insuficiente para a grade pedida (cai em `preteridos`); pedido 3/produto "C" com `status_credito="Sem Crédito"` e estoque sobrando (cai em `bloqueados_credito`). Assert `candidate_count == 3`, `[(row.nr_pedido, row.cd_prod_cor) for row in draft.rows] == [(1, "A")]`, `draft.deferred_count == 1` e `draft.blocked_credit_count == 1` — os dois contadores que hoje seriam 0 por construção. `_item` já aceita `qty`/`size`; passe `status_credito` sobrescrevendo o dict retornado, ou estenda o helper com um parâmetro nomeado se preferir (helper local, sem consumidor externo).

    **Não toque `service.py` nesta task.** Ao fim dela o `sem_adequar` de produção ainda roda pelo streaming — é a janela deliberada de "implementar antes de trocar", fechada na Task 2.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_domain.py app/tests/test_pedidos_motor.py app/tests/test_pedidos_orcamento_pedido.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_processing_plan` não contém mais `selected = {pair: items for pair, items in sorted(eligible.items())}` — os dois modos passam por `processar_pedidos`.
    - `grep -n "modo=ModoAdequacao" app/modules/pedidos/processing/domain.py` encontra a chamada única; `ModoAdequacao` é importado de `politica_quantidade`.
    - `build_processing_plan` aceita e repassa `total_original_por_pedido`, `consumido_previo_adicao_por_pedido`, `consumido_previo_corte_por_pedido` sem renomear.
    - `test_mixed_channel_pair_fails_closed_for_every_scope` continua passando **sem alteração e sem `stock`** (validação de canal precede a de estoque).
    - Os 3 testes que precisavam de fixture passam com estoque adicionado e **asserções originais intactas** — nenhum `assert` foi afrouxado, removido ou trocado por `>=`.
    - `test_sem_adequar_now_defers_by_stock_and_blocks_by_credit` passa e prova `deferred_count == 1` e `blocked_credit_count == 1` no modo `SEM_ADEQUAR` (J1, J2, J3).
    - `app/tests/test_pedidos_motor.py` e `app/tests/test_pedidos_orcamento_pedido.py` passam sem alteração — o motor não foi tocado.
    - `service.py` não aparece no `git diff` desta task.
  </acceptance_criteria>
  <done>O caminho global já sabe planejar `SEM_ADEQUAR` com contadores reais e aceita os mappings de orçamento; nada em produção usa isso ainda.</done>
</task>

<task type="auto">
  <name>Task 2: TROCAR — `_plan_once` com caminho único e orçamento real nos dois modos (fecha ALOC-09)</name>
  <files>app/modules/pedidos/processing/application/service.py, app/tests/test_pedidos_processing_service.py</files>
  <read_first>
    - app/modules/pedidos/processing/application/service.py (linhas 189-354: `_plan_once` inteiro — o ramo de streaming em 211-290 sai; o corpo de 292-354 vira o caminho único; a condicional `if spec.mode is ProcessingMode.ADEQUAR` da linha 311 sai; linhas 22-42: o bloco de import de `domain` que precisa ser podado)
    - app/modules/pedidos/processing/application/ports.py (linhas 21-45: `ProcessingPlannerSource` — a assinatura de `load_pedido_budget` adicionada pelo plano 15-01)
    - app/modules/pedidos/domain/motor_adequacao.py (linhas 364-419: `_resolver_orcamento_pedido` — o placeholder dispara quando o mapping é `None` **ou** quando falta a chave daquele `nr_pedido`; linhas 913-927: o `logger.warning` de ALOC-09, emitido uma vez por canal)
    - app/tests/test_pedidos_processing_service.py (linhas 38-140: helpers `_job`, `_spec`, `_pending_spec`, `_Lock`, `_Lease`, `_Uow`; linhas 498-612: `test_sem_adequar_planning_uses_keyset_pages_without_global_hydration`, o teste que prova **o oposto** do que esta entrega implementa)
    - app/tests/test_pedidos_motor.py (linhas 290-327: os 2 testes de `caplog` do ALOC-09 da Phase 14 — **continuam válidos e não são tocados**; o teste novo desta task prova o caso "dados fornecidos" pelo caminho de produção real, não pela chamada direta ao motor)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md § "Riscos de validação específicos desta fase", item 1 (reescrever conscientemente, não "ajustar até passar")
  </read_first>
  <action>
    Em `service.py::_plan_once`, apague o bloco inteiro `if spec.mode is ProcessingMode.SEM_ADEQUAR: ... return spec` (o `StreamingPlanBuilder`, o laço de páginas keyset, o `processing_stream_cursor_did_not_advance`, o `finalize_streamed_plan` e o heartbeat duplicado que vive dentro dele). O que sobra — hidratação global, checagem de snapshot, `load_size_reference`, motor em thread, `store_plan`, heartbeat, commit — passa a servir os dois modos sem nenhuma condicional de modo.

    Remova a condicional `if spec.mode is ProcessingMode.ADEQUAR:` que hoje protege `load_stock`/`load_adequation_config`: `stock`, `criterion` e `tolerance` passam a ser carregados **sempre**. Com isso `stock` deixa de precisar do tipo `| None` e dos defaults locais `"valor"`/`0.05` — os valores vêm sempre do adapter.

    Ligue o orçamento real: depois da checagem de consistência do snapshot (`processing_snapshot_changed_during_planning`) e **antes** de `_run_cpu_bound_monitored` (é I/O; não pode entrar na lambda que roda em thread), extraia `order_ids = {int(item["nr_pedido"]) for item in pending_items}` e chame `load_pedido_budget(order_ids)` desempacotando em três variáveis na ordem documentada pelo port. Repasse as três a `build_processing_plan` como argumentos **nomeados**, dentro da lambda, junto de `stock`/`criterion`/`tolerance`. Nunca posicionalmente: a ordem dos três dicts é indistinguível para o type checker e trocar dois deles corromperia orçamento de dinheiro em silêncio.

    O conjunto `hydrated_pairs` já é derivado de `pending_items` logo acima; derive `order_ids` do mesmo `pending_items` (não de `hydrated_pairs`) para deixar explícito que o escopo do orçamento é o conjunto de pedidos da foto já validada pelo preflight, nunca um `nr_pedido` arbitrário vindo de fora.

    Pode os imports que ficaram órfãos em `service.py`: `PLANNING_PAIR_PAGE_SIZE`, `StreamingPlanBuilder`, `build_pair_payload` e `ProcessingMode` (nenhum uso restante depois das duas remoções acima), e `from collections import defaultdict` (usado só pelo agrupamento por página). Confirme com `grep -n` antes de remover cada um; `ProcessingRequest`, `PlanDraft` e `APPLY_CHUNK_SIZE` permanecem.

    Nos testes, **reescreva** `test_sem_adequar_planning_uses_keyset_pages_without_global_hydration` como `test_sem_adequar_planning_hydrates_globally_and_reports_real_counts`. Não é adaptação — o teste antigo prova a paginação, que é exatamente o que sai. O novo:
    - Fake `Planner` **sem** `prepare_pending_pair_stream`/`load_pending_pair_page` (os métodos deixam de existir no Protocol); `load_pending_items` agora registra o evento em vez de `raise AssertionError`; `load_stock` devolve estoque suficiente para o par (1, "A") e insuficiente para (2, "B"); `load_adequation_config` devolve `("valor", 0.05)`; `load_pedido_budget` registra o conjunto recebido e devolve três dicts cobrindo todos os pedidos.
    - Itens: pedido 1/produto "A" em dois tamanhos, pedido 2/produto "B" em um tamanho, pedido 3/produto "C" com `status_credito` sem crédito — os mesmos três casos da Task 1, agora atravessando `_plan_once`.
    - Fake `Processing` com `store_plan` (que captura o `draft`), **sem** `append_plan_rows`/`finalize_streamed_plan`.
    - Asserções: `load_pending_items` foi chamado; o `draft` capturado tem `planned_count == 1`, `deferred_count == 1`, `blocked_credit_count == 1`; a única row é `(1, "A")` com `payload["type"] == "sem"`; `load_pedido_budget` recebeu exatamente `{1, 2, 3}`; `events.count("commit") == 1`.

    Adicione o teste novo `test_plan_once_feeds_real_budget_and_silences_the_aloc09_warning`, em modo **`ADEQUAR`** (é o modo em que o ledger roda, e é onde o aviso de ALOC-09 nasce): estenda `_pending_spec` com um parâmetro nomeado opcional `mode` (default `ProcessingMode.SEM_ADEQUAR`, para não mexer no chamador existente). Use `caplog.at_level(logging.WARNING, logger="app.modules.pedidos.domain.motor_adequacao")` em torno de `_plan_once` e assert que **nenhum** registro contém `"ALOC-09"` (J6). `build_processing_plan` roda em thread via `_run_cpu_bound_monitored`, mas o handler do `caplog` é global do processo — os registros são capturados normalmente; se por algum motivo do ambiente não forem, **não afrouxe a asserção**: mova a verificação para uma chamada direta a `build_processing_plan` com os mesmos argumentos e registre o desvio no SUMMARY. Assert também que `load_pedido_budget` foi chamado com o conjunto de pedidos da foto e que os três dicts chegaram ao `draft` (o jeito mais direto: um pedido cujo `total_original` real é maior que a soma do snapshot, provando que o valor usado não é o placeholder).
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_service.py app/tests/test_pedidos_processing_domain.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_plan_once` não contém nenhuma condicional `spec.mode is ProcessingMode.*`; `grep -n "ProcessingMode" app/modules/pedidos/processing/application/service.py` retorna vazio.
    - `grep -n "load_pedido_budget" app/modules/pedidos/processing/application/service.py` encontra exatamente uma chamada, fora da lambda de `_run_cpu_bound_monitored`.
    - Os três dicts são passados a `build_processing_plan` por argumento nomeado (`total_original_por_pedido=`, `consumido_previo_adicao_por_pedido=`, `consumido_previo_corte_por_pedido=`), nunca posicional.
    - `test_sem_adequar_planning_hydrates_globally_and_reports_real_counts` passa e prova, no caminho de `_plan_once`, `planned_count == 1` / `deferred_count == 1` / `blocked_credit_count == 1` — o teste de paginação não existe mais e nenhuma asserção sua foi reciclada.
    - `test_plan_once_feeds_real_budget_and_silences_the_aloc09_warning` passa e prova ausência de `"ALOC-09"` no log com dados reais (J6).
    - Os 2 testes de `caplog` do ALOC-09 em `app/tests/test_pedidos_motor.py` continuam existindo e passando **sem alteração** (o fallback ruidoso segue provado para o caso "dados não fornecidos").
    - Nenhum import órfão restou em `service.py` (nem `defaultdict`, nem os 3 símbolos de streaming).
  </acceptance_criteria>
  <done>`sem_adequar` de produção passa pelo caminho global com orçamento real; o código de streaming está oficialmente inalcançável — e por isso mesmo tem de sair na próxima task.</done>
</task>

<task type="auto">
  <name>Task 3: APAGAR — remover o caminho de streaming inteiro do repositório</name>
  <files>app/modules/pedidos/processing/domain.py, app/modules/pedidos/processing/application/ports.py, app/modules/pedidos/processing/infrastructure/adapters.py, app/modules/pedidos/processing/infrastructure/repository.py, app/tests/test_pedidos_processing_domain.py, app/tests/test_pedidos_processing_repository.py, app/tests/test_pedidos_processing_stream_repository.py</files>
  <read_first>
    - app/modules/pedidos/processing/domain.py (linhas 33-48: os comentários que descrevem o streaming como estratégia viva — "Sem adequar usa páginas keyset", "`sem_adequar` não retém esse volume em Python porque monta e persiste páginas de até 250 pares"; linhas 298-412: `PlanSummary` e `StreamingPlanBuilder`; linhas 598-615: `build_pair_payload`, o wrapper público — a privada `_payload_for_pair` **permanece**; linhas 718-760: `__all__`)
    - app/modules/pedidos/processing/application/ports.py (linhas 28-35 e 69-82: os 4 métodos de streaming nos dois Protocols; linha 14: o import de `PlanSummary`)
    - app/modules/pedidos/processing/infrastructure/adapters.py (linhas 175-267: `prepare_pending_pair_stream` e `load_pending_pair_page` — a TEMP TABLE `orders_processing_pending_pairs` e o índice único saem junto)
    - app/modules/pedidos/processing/infrastructure/repository.py (linhas 110-208: `append_plan_rows` e `finalize_streamed_plan`; linha 41: o import `TYPE_CHECKING` de `PlanSummary`; linhas 210-262: `store_plan`, que **fica** e é o único caminho de persistência de plano)
    - app/tests/test_pedidos_processing_stream_repository.py (arquivo inteiro, 313 linhas, 5 testes — todos sobre `append_plan_rows`/`finalize_streamed_plan`)
    - app/tests/test_pedidos_processing_domain.py (linha 22: import de `StreamingPlanBuilder`; linhas 198-228: `test_streaming_builder_keeps_only_counts_hash_and_cumulative_cap`)
    - app/tests/test_pedidos_processing_repository.py (linhas 637-682: `test_planner_keyset_pages_cover_each_pair_once`)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md § "Riscos de validação", item 3 (remover os símbolos primeiro e ver o que quebra é mais confiável que prever consumidores)
  </read_first>
  <action>
    **Ordem obrigatória: remova os símbolos de produção primeiro, rode a suíte, e só então limpe os testes.** O que quebrar por `ImportError`/`AttributeError` é o raio de impacto real — mais confiável que qualquer grep prévio. Registre no SUMMARY a lista do que quebrou, mesmo que coincida com a previsão.

    Passo 1 — produção. Em `domain.py`: apague a classe `StreamingPlanBuilder`, a constante `PLANNING_PAIR_PAGE_SIZE`, a função pública `build_pair_payload` (mantenha `_payload_for_pair`, que é o caminho vivo) e as entradas correspondentes de `__all__`. Em `ports.py`: apague `prepare_pending_pair_stream` e `load_pending_pair_page` de `ProcessingPlannerSource`, e `append_plan_rows` e `finalize_streamed_plan` de `ProcessingRepository`. Em `adapters.py`: apague os dois métodos de paginação de `SqlAlchemyProcessingPlannerSource` (a TEMP TABLE e o índice único vão junto — nada mais os cria). Em `repository.py`: apague `append_plan_rows` e `finalize_streamed_plan`. **Não encoste em `_assert_stock_capacity`** (linhas ~489-513) — é o plano 15-02, rodando na wave anterior; um diff seu ali é vazamento de escopo.

    Passo 2 — o símbolo que a própria remoção deixa órfão. `PlanSummary` existe **apenas** para servir `StreamingPlanBuilder.finish()` e `finalize_streamed_plan`; `PlanDraft` valida suas próprias invariantes e é o que `store_plan` consome. Confirme com `grep -rn --include='*.py' "PlanSummary" app/` que, depois do passo 1, não sobra nenhum consumidor fora da própria definição, do `__all__`, do import em `ports.py` e do import `TYPE_CHECKING` em `repository.py` — e então remova os quatro. Deixar `PlanSummary` para trás recriaria exatamente o padrão de código órfão que esta fase existe para eliminar. **Registre essa decisão no SUMMARY**: não está na lista literal do CONTEXT.md, é consequência direta dela, e a evidência é o grep.

    Passo 3 — rode a suíte completa **antes** de tocar qualquer teste e anote quais falham e por quê (`ImportError` do `StreamingPlanBuilder`, `AttributeError` nos fakes, teste de keyset chamando método inexistente).

    Passo 4 — testes. Delete `app/tests/test_pedidos_processing_stream_repository.py` inteiro (`git rm`). Em `test_pedidos_processing_domain.py`, remova o import de `StreamingPlanBuilder` e o teste `test_streaming_builder_keeps_only_counts_hash_and_cumulative_cap` — o teto cumulativo de bytes que ele cobria continua coberto por `test_plan_aborts_during_build_when_cumulative_payload_exceeds_cap`, que exercita o mesmo `MAX_PLAN_TOTAL_BYTES` no caminho vivo. Em `test_pedidos_processing_repository.py`, remova `test_planner_keyset_pages_cover_each_pair_once` e quaisquer imports que fiquem sem uso.

    Passo 5 — cobertura que o arquivo deletado carregava. As duas garantias reais dele eram contiguidade de ordinais e conflito de header. Ambas seguem cobertas no caminho vivo: `store_plan` escreve todas as rows numa transação (contiguidade é intrínseca, não emergente de N chamadas) e já falha com `processing_plan_already_frozen`/`processing_plan_rows_without_header`, exercitados por `test_store_plan_materializes_at_most_one_insert_chunk` e `test_plan_checkpoint_is_sequential_and_resumable`. **Confirme lendo os dois testes** e registre a conclusão no SUMMARY. Se encontrar um buraco genuíno, **anote — não invente teste novo aqui**; vira item de gap para a verificação da fase.

    Passo 6 — comentários. Atualize os comentários de `domain.py` (linhas ~33-48) que descrevem o streaming como estratégia viva: os dois modos agora hidratam a foto global e o teto de memória do preflight (`PLAN_ITEM_LIMIT`, `MAX_PLAN_TOTAL_BYTES`) é a proteção real dos dois — não o streaming. Mantenha os números medidos (434 MiB / 132.405 itens, ~600 MiB projetados) e diga explicitamente que o pico do modo sem adequação ainda **não** foi medido (é o plano 15-04). **Não toque `docs/adequacao.md`** — plano 15-05.

    Portão final desta task: o grep dos 6 símbolos precisa retornar vazio, e a suíte completa precisa fechar com a única falha conhecida.
  </action>
  <verify>
    <automated>grep -rn --include='*.py' "StreamingPlanBuilder\|PLANNING_PAIR_PAGE_SIZE\|prepare_pending_pair_stream\|load_pending_pair_page\|append_plan_rows\|finalize_streamed_plan" app/modules app/tests; test $? -eq 1 && docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn --include='*.py' "StreamingPlanBuilder\|PLANNING_PAIR_PAGE_SIZE\|prepare_pending_pair_stream\|load_pending_pair_page\|append_plan_rows\|finalize_streamed_plan" app/modules app/tests` retorna **vazio** (J7, critério 2 do ROADMAP). O filtro `--include='*.py'` é obrigatório: sem ele o grep casa com `.pyc` de `__pycache__` e o portão dá falso-positivo.
    - `app/tests/test_pedidos_processing_stream_repository.py` não existe mais (removido com `git rm`, aparece como `D` no `git show --stat`).
    - `grep -rn --include='*.py' "PlanSummary" app/` retorna vazio, e o SUMMARY explica por que o símbolo saiu junto (consequência direta da remoção, com evidência de grep).
    - `_assert_stock_capacity` **não aparece** no `git diff` desta task.
    - `_payload_for_pair` e `store_plan` continuam existindo e sendo o caminho vivo.
    - Os comentários de `domain.py` não afirmam mais que `sem_adequar` usa páginas keyset.
    - Suíte completa: exatamente 1 falha, e é `test_read_projection_empty_contracts_execute_real_sql`.
  </acceptance_criteria>
  <done>Não existe mais um segundo caminho de planejamento no repositório — nem no código, nem nos testes, nem nos comentários.</done>
</task>

<task type="auto">
  <name>Task 4: VERIFICAR — prova de fechamento do ALOC-09 em duas execuções sucessivas + portões da fase</name>
  <files>app/tests/test_pedidos_processing_repository.py</files>
  <read_first>
    - app/tests/test_pedidos_processing_repository.py (linhas 63-91: fixture `processing_database` e a lista de tabelas que ela limpa; linhas 137-235: `_source_item`, `_planned_pair`, `_create_job_and_plan`, `_seed_source` — os moldes de seed e de persistência de plano; linhas 316-340: `test_writer_applies_pair_and_checkpoint_in_same_transaction`, o molde de `load_next_chunk` + `SqlAlchemyProcessingWriter.apply_pairs` + `checkpoint_chunk` numa transação; linhas 362-396: como o writer falha com `processing_plan_stale` quando a foto muda ou o estoque não cabe)
    - app/modules/pedidos/processing/infrastructure/repository.py (linhas 460-513: `_assert_plan_still_current` compara `fingerprint_pair_source` contra as linhas vivas de `pedidos`; `_assert_stock_capacity` revalida contra a projeção de estoque disponível — **leia, não edite**)
    - app/modules/pedidos/processing/infrastructure/adapters.py (o método `load_pedido_budget` entregue pelo plano 15-01, e `load_pending_items`, cujo SQL exclui pares com OR / já processados)
    - app/modules/pedidos/domain/orcamento_pedido.py (o ledger: dois orçamentos independentes, `floor(total_original * tolerancia)` para cada um; adição e corte não se compensam)
    - .planning/research/v1.3/PITFALLS.md § Pitfall 4 (nunca usar `total_grade < 20` em teste de orçamento — `floor(base * 0,05)` arredonda para 0 e o teste passa por acidente)
    - .planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-VALIDATION.md § "Critérios de sucesso do ROADMAP → como provar", linha do critério 5
  </read_first>
  <action>
    Adicione a `app/tests/test_pedidos_processing_repository.py` o teste `test_orcamento_persiste_entre_execucoes_sucessivas`, usando a fixture `processing_database`. Ele é a prova do critério 5 do ROADMAP e precisa ser **discriminante**: tem de falhar se alguém desligar o wiring do orçamento.

    Monte um pedido único com **dois produtos**, para que exista uma segunda execução com trabalho real a fazer:
    - `pedidos`: `nr_pedido=700`, canal Franquia, `status_credito` com crédito; produto `ORC|01` nos tamanhos 36 e 37 com `qt_entregar=10` cada; produto `ORC|02` nos tamanhos 36 e 37 com `qt_entregar=10` cada. Total original = **40** peças → orçamento de corte = `floor(40 * 0,05)` = **2** (acima do piso do Pitfall 4).
    - `estoque` (rodada 1): `ORC|01` 36 → 8 e 37 → 10 (falta exatamente 2, o orçamento inteiro); `ORC|02` sem linha ou com 0 (fica em stand-by nesta rodada).

    Rodada 1 — use os adapters reais para o que está sob teste e fixtures determinísticas para o resto: `pending = await SqlAlchemyProcessingPlannerSource(db).load_pending_items(ProcessingChannel.FRANQUIA)` e `budget = await ...load_pedido_budget({700})` são **reais** (é o comportamento sob teste); `reference` e `stock` podem ser dicts montados à mão (os adapters correspondentes já têm cobertura própria, e a projeção de estoque virtual introduziria variação irrelevante aqui) — mas as linhas de `estoque` precisam existir e bater com o dict, porque o writer revalida capacidade contra o banco. Chame `build_processing_plan(request=ProcessingRequest(mode=ProcessingMode.ADEQUAR, channel=ProcessingChannel.FRANQUIA), ...)` com os três dicts do orçamento, e persista o draft criando job + spec + `store_plan` (crie um helper irmão de `_create_job_and_plan`, por exemplo `_create_job_and_store_draft(db, draft, *, mode)` — **não altere `_create_job_and_plan`**, que é usado por 6 testes que passam hoje). Aplique com `load_next_chunk` + `SqlAlchemyProcessingWriter(db).apply_pairs(chunk)` + `checkpoint_chunk`, commitando como o teste-molde faz.

    Entre as rodadas, assert o estado intermediário: `load_pedido_budget({700})` agora devolve `total_original == 40`, `consumido_corte == 2` e `consumido_adicao == 0`. Isso prova o elo que o plano 15-01 **não** conseguiu provar: que o formato que o writer persiste em `ordens_reserva.itens` (`qt_solicitada`/`qt_liquida`) é exatamente o que o leitor de orçamento agrega.

    Rodada 2 — reponha estoque para `ORC|02` (36 → 9, 37 → 10: falta 1) e re-hidrate a foto (`load_pending_items` agora devolve só `ORC|02`, porque `ORC|01` tem OR e foi processado). Chame `build_processing_plan` **duas vezes** sobre os mesmos itens:
    - com o orçamento real relido do banco → o corte de 1 tem de ser **recusado** (orçamento restante = 2 − 2 = 0): `draft.rows == ()`, `planned_count == 0`, `deferred_count == 1`, e nenhuma `OrdemReserva` nova;
    - sem os mappings de orçamento (o placeholder de execução única) → o mesmo corte teria sido **aceito**, porque o denominador encolhido (só as 20 peças que sobraram na foto) concede um orçamento novo de `floor(20 * 0,05)` = 1: `planned_count == 1`.

    Essas duas asserções juntas são a prova de que o ALOC-09 fechou — uma sozinha não distingue "o orçamento está ligado" de "não havia orçamento a gastar". Envolva as duas chamadas em `caplog.at_level(logging.WARNING, logger="app.modules.pedidos.domain.motor_adequacao")` e assert que a chamada **com** orçamento não emite `"ALOC-09"` e a chamada **sem** emite (J6 nos dois sentidos).

    **Se os números do motor não se comportarem como descrito** (por exemplo, se ele cortar parcialmente em vez de recusar), ajuste as **quantidades da fixture** até restaurar a propriedade discriminante — nunca a asserção. A propriedade a preservar é: *com orçamento real a segunda rodada recusa; com placeholder ela aceitaria*. Registre no SUMMARY os números finais e por que mudaram.

    Portões finais da fase (registre os resultados no SUMMARY):
    1. `grep -rn --include='*.py'` dos 6 símbolos de streaming → vazio.
    2. Suíte completa via Docker → **821 passed, 16 skipped, 1 failed** esperados (825 − 7 removidos + 3 adicionados), com a única falha sendo a de teardown asyncpg conhecida. Delta diferente exige reconciliação nominal (qual teste apareceu/sumiu), não arredondamento.
    3. Anote no SUMMARY a **dependência de integração com o plano 15-02**: `sem_adequar` agora reserva estoque de verdade, então `_assert_stock_capacity` precisa da correção do FIX-01 para revalidar `tipo="sem"` na escrita. **`depends_on` deste plano inclui `"15-02"`** — o executor não deve rodar este plano antes daquele estar commitado. Confirme no início da execução que a correção do FIX-01 já está presente em `repository.py` (revalidação sem o `if tipo != "com": continue"`); se não estiver, pare e não prossiga.
    4. Anote também a verificação **manual** pendente (não automatizável): comparar, contra dado real, o volume de ORs de `sem_adequar` antes/depois, categorizando cada par que sumiu em `sem_credito` / `sem_estoque_grade` / `outro` — o bucket `outro` tem de estar vazio.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py -q && docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_orcamento_persiste_entre_execucoes_sucessivas` existe, passa, e contém as **duas** chamadas comparativas da rodada 2 (com orçamento real → `planned_count == 0`; sem orçamento → `planned_count == 1`). Um teste que só assert a recusa não satisfaz este critério.
    - O teste assert o estado intermediário `total_original == 40` / `consumido_corte == 2` / `consumido_adicao == 0` lido do banco **depois** do writer real ter gravado a OR.
    - O total do pedido no teste é `>= 20` peças (Pitfall 4 — abaixo disso `floor(base * 0,05)` zera e a prova vira acidente).
    - `caplog` prova ausência de `"ALOC-09"` na chamada com orçamento real e presença na chamada com placeholder.
    - `_create_job_and_plan` não foi modificado; o helper novo é irmão dele.
    - Portão J7: grep dos 6 símbolos vazio.
    - Suíte completa: exatamente 1 falha (a conhecida). Contagem esperada 821 passed / 16 skipped / 1 failed, ou delta reconciliado nominalmente no SUMMARY.
    - SUMMARY registra: a dependência de integração com 15-02, a decisão sobre `PlanSummary`, a verificação manual pendente do diff categorizado, e o resultado real de cada portão.
  </acceptance_criteria>
  <done>ALOC-09 está provado fechado pelo caminho de produção real, e os portões J1–J7 da fase têm evidência registrada.</done>
</task>

</tasks>

<verification>
## Mapa de verificação por task

| Task | Invariante / Critério | Comando |
|---|---|---|
| 1 | J1, J2, J3 no domínio puro | `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_domain.py app/tests/test_pedidos_motor.py app/tests/test_pedidos_orcamento_pedido.py -q` |
| 2 | Critério 1 (caminho global), J3 no plano, J6 | `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_service.py app/tests/test_pedidos_processing_domain.py -q` |
| 3 | Critério 2, J7 | `grep -rn --include='*.py' "StreamingPlanBuilder\|PLANNING_PAIR_PAGE_SIZE\|prepare_pending_pair_stream\|load_pending_pair_page\|append_plan_rows\|finalize_streamed_plan" app/modules app/tests` → vazio, + suíte completa |
| 4 | Critério 5, J6 | `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_processing_repository.py -q` + suíte completa |

## Portão da fase

```
docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q
```

Esperado: **821 passed, 16 skipped, 1 failed**. A única falha permitida é `app/tests/test_pedidos_read_projection.py::test_read_projection_empty_contracts_execute_real_sql` (teardown asyncpg, pré-existente, fora de escopo). **Sem `--deselect`, sem `-k`.**

Aritmética da contagem: 825 (base) − 5 (`test_pedidos_processing_stream_repository.py`) − 1 (`test_streaming_builder_keeps_only_counts_hash_and_cumulative_cap`) − 1 (`test_planner_keyset_pages_cover_each_pair_once`) + 1 (`test_sem_adequar_now_defers_by_stock_and_blocks_by_credit`) + 1 (`test_plan_once_feeds_real_budget_and_silences_the_aloc09_warning`) + 1 (`test_orcamento_persiste_entre_execucoes_sucessivas`) = 821. (`test_sem_adequar_planning_...` é reescrito, não somado.) Delta diferente = reconciliar nominalmente, nunca arredondar.

## Não coberto por automação (registrar no SUMMARY)

- **Queda no volume de ORs de `sem_adequar`** — só observável contra dado real. Uma queda é esperada e correta. Categorizar os pares que sumiram em `sem_credito` / `sem_estoque_grade` / `outro`; o bucket `outro` tem de estar vazio.
- **Pico de memória do modo sem adequação** — plano 15-04, wave 4. Nenhum número real existe hoje; não extrapole o de `ADEQUAR` (o volume elegível de `sem_adequar` pode ser estruturalmente maior porque, até esta entrega, ele aceitava todo par).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mirror Databricks (`pedidos`, `estoque`) → planejador | Dados de origem externa, full refresh, sem contrato de imutabilidade — um par pode sumir de `pedidos` sem que o app saiba (PREMISSA FALSA verificada em 2026-08-17). O `sourceHash` congelado no plano e a revalidação do writer são a fronteira que detecta mutação entre planejar e aplicar. |
| Worker de processamento → Postgres | `nr_pedidos` de `load_pedido_budget` nunca vem de input HTTP: deriva de `pending_items`, já limitado pelo preflight (`inspect_pending`) e escopado por canal. |
| Rota HTTP de submissão → job durável | Fora do escopo desta entrega (RBAC e idempotência já cobrem); nenhuma rota nova é criada aqui. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Denial of Service (complexidade/memória) | `_plan_once` + `build_processing_plan` no modo `SEM_ADEQUAR` — o modo passa a hidratar a foto global em Python e a rodar o motor `O(n log n)` sobre um volume elegível que pode ser **estruturalmente maior** que o de `ADEQUAR` (até aqui ele aceitava todo par, sem filtro de crédito) | mitigate | O preflight `inspect_pending` já é agnóstico de modo e barra antes da hidratação (`PLAN_PAIR_LIMIT` 100k pares, `PLAN_ITEM_LIMIT` 200k itens, `MAX_PLAN_TOTAL_BYTES` 96 MiB); o teto cumulativo de payload é rechecado dentro do laço de `rows`; `cancel_token` + `ProcessingLeaseKeeper` abortam a execução que estoura o lease em vez de segurar memória indefinidamente. **O pico real ainda não foi medido** — plano 15-04, wave 4; até lá o risco residual é conhecido e aceito com o teto do preflight como barreira |
| T-15-03-02 | Tampering (integridade financeira) | Wiring do orçamento em `_plan_once` → `build_processing_plan` → `processar_pedidos`: os três mappings agora valem para os **dois** modos, e um erro aqui altera quantidade reservada — efeito financeiro direto sobre o cliente | mitigate | Os três dicts trafegam **sempre por argumento nomeado**, com os mesmos nomes do port ao motor (nenhuma remapeação, nenhuma passagem posicional); `load_pedido_budget` devolve chave para todo `nr_pedido` do escopo, então o fallback de placeholder do motor nunca é silenciosamente parcial; `_resolver_orcamento_pedido` continua emitindo `logger.warning` de ALOC-09 se alguma chave faltar (detector residual vivo); `test_orcamento_persiste_entre_execucoes_sucessivas` prova o round-trip contra o banco real e falha se o wiring for desligado |
| T-15-03-03 | Tampering (reserva acima do estoque real) | `_assert_stock_capacity` ainda pula a revalidação para `tipo != "com"`, e este plano faz `sem_adequar` **passar a reservar estoque de verdade** | mitigate | Correção pertence ao plano 15-02 (FIX-01). Esta entrega **não** toca a função, mas `depends_on: ["15-01", "15-02"]` no frontmatter torna a ordem obrigatória — o executor não pode iniciar este plano sem o 15-02 já commitado, e a Task 1 confirma isso lendo `repository.py` antes de prosseguir. Deixa de ser risco meramente documentado para ser risco impedido por dependência formal |
| T-15-03-04 | Tampering (plano obsoleto entre planejar e aplicar) | Janela entre `store_plan` e `apply_pairs`, agora também exercitada por `sem_adequar` com quantidades decididas pelo motor | mitigate | `_assert_plan_still_current` compara `fingerprint_pair_source` da foto congelada contra as linhas vivas de `pedidos` e falha com `processing_plan_stale`; o `sourceHash` continua sendo calculado sobre `eligible[pair]` (foto original), não sobre os itens já processados pelo motor — preservar essa distinção é parte do critério de aceite da Task 1 |
| T-15-03-05 | Denial of Service (custo de I/O adicional) | `load_pedido_budget` passa a rodar em **toda** execução, inclusive no modo que não usa o ledger (decisão do usuário; a pesquisa sugeria condicionar a `ADEQUAR`) | accept | Uma query agregada, chunkeada em 5.000 `nr_pedido`, sempre bounded por `pedido_scope` e apoiada nos índices existentes (`ix_pedidos_*`, PK composta de `ordens_reserva`) — custo desprezível diante da hidratação da foto que já acontece na mesma transação. Aceito em troca de eliminar a classe de bug "esqueceram de ligar o orçamento neste caminho" |
| T-15-03-06 | Information Disclosure | Leitura agregada de orçamento por pedido | accept | Sistema não tem multi-tenancy; `nr_pedidos` deriva exclusivamente da foto já validada pelo preflight, nunca de valor arbitrário externo; nenhuma rota HTTP nova é exposta |
| T-15-03-SC | Tampering | Supply chain (npm/pip/cargo installs) | accept | Não aplicável — este plano **não instala nenhuma dependência nova**; só remove código e religa símbolos já existentes no repositório |
</threat_model>

<success_criteria>
- `SEM_ADEQUAR` é planejado por `build_processing_plan` → `processar_pedidos`, com `deferred_count`/`blocked_credit_count` reais (critério 1 do ROADMAP; J1, J2, J3).
- `_plan_once` tem um único caminho; `stock`, `criterion`, `tolerance` e o orçamento são carregados nos dois modos.
- Os três mappings de orçamento chegam ao motor pelo caminho de produção, e o `logger.warning` de ALOC-09 deixa de ser emitido (critério 5 do ROADMAP; J6).
- Duas execuções sucessivas sobre o mesmo pedido não concedem 5% novos — provado por comparação direta contra o comportamento do placeholder no mesmo teste.
- Nenhum símbolo de streaming permanece no repositório; o grep dos 6 símbolos retorna vazio (critério 2 do ROADMAP; J7).
- `_assert_stock_capacity` permanece intocado (escopo do plano 15-02).
- Suíte completa via Docker: 821 passed, 16 skipped, 1 failed (a falha conhecida de teardown asyncpg), sem `--deselect` e sem `-k`.
</success_criteria>

## Artifacts this phase produces

- `app/modules/pedidos/processing/domain.py` — `build_processing_plan` com caminho único parametrizado por modo (chama `processar_pedidos` nos dois), recebendo e repassando `total_original_por_pedido`/`consumido_previo_adicao_por_pedido`/`consumido_previo_corte_por_pedido`; `StreamingPlanBuilder`, `PlanSummary`, `PLANNING_PAIR_PAGE_SIZE` e `build_pair_payload` removidos; comentários de memória atualizados para o caminho único.
- `app/modules/pedidos/processing/application/service.py` — `_plan_once` sem o ramo de streaming, com hidratação global, `load_stock`/`load_adequation_config`/`load_pedido_budget` incondicionais e persistência sempre por `store_plan`.
- `app/modules/pedidos/processing/application/ports.py` — `ProcessingPlannerSource` sem os 2 métodos de paginação; `ProcessingRepository` sem `append_plan_rows`/`finalize_streamed_plan`.
- `app/modules/pedidos/processing/infrastructure/adapters.py` — `prepare_pending_pair_stream` e `load_pending_pair_page` removidos (a TEMP TABLE `orders_processing_pending_pairs` deixa de existir).
- `app/modules/pedidos/processing/infrastructure/repository.py` — `append_plan_rows` e `finalize_streamed_plan` removidos; `store_plan` e `_assert_stock_capacity` intocados.
- `app/tests/test_pedidos_processing_domain.py` — 4 testes com fixture de estoque (asserções originais preservadas), teste do `StreamingPlanBuilder` removido, teste novo `test_sem_adequar_now_defers_by_stock_and_blocks_by_credit` (J1/J2/J3).
- `app/tests/test_pedidos_processing_service.py` — teste de paginação keyset reescrito como prova de hidratação global e contadores reais; teste novo de orçamento real + `caplog` sem ALOC-09 (J6).
- `app/tests/test_pedidos_processing_repository.py` — teste de paginação keyset removido; teste novo `test_orcamento_persiste_entre_execucoes_sucessivas` (critério 5, com a comparação discriminante contra o placeholder).
- `app/tests/test_pedidos_processing_stream_repository.py` — **deletado** (313 linhas, 5 testes, exclusivos do caminho removido).

<output>
Create `.planning/phases/15-roteamento-global-do-modo-sem-adequa-o/15-03-SUMMARY.md` when done
</output>
