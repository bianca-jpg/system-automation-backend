---
phase: 16-persist-ncia-do-motivo-de-stand-by
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - alembic/versions/032_pedido_standby_motivo.py
  - app/modules/pedidos/infrastructure/models.py
  - alembic/env.py
  - app/tests/test_migration_032_pedido_standby_motivo.py
autonomous: true
requirements: [STANDBY-01]

must_haves:
  truths:
    - "A migration alembic 032 aplica com sucesso e cria a tabela pedido_standby_motivo com CHECK aceitando os 3 motivos (sem_credito, sem_estoque, furo_grade) desde a criação — nunca só 2"
    - "Inserir uma linha com um 4º valor de motivo (ex. 'invalido') é rejeitado pelo Postgres com CheckViolation/IntegrityError — nunca aceito silenciosamente"
    - "A migration é reversível: downgrade() remove a tabela sem erro, e um upgrade seguinte a reconstrói vazia"
    - "O model ORM PedidoStandbyMotivo (mesmo arquivo do EstoqueVirtual) espelha exatamente as colunas/tipos/PK da migration — alembic check não detecta nenhuma operação pendente"
    - "alembic/env.py continua registrando todos os models existentes (nenhum # noqa: F401 removido) e passa a importar também PedidoStandbyMotivo"
    - "A suíte de testes completa permanece na linha de base confirmada no início da execução (mesma 1 falha conhecida, sem nenhuma regressão nova), com os deltas esperados desta plan: +2 passed (test_schema_guard.py parametrizado para a tabela nova) e +1 skipped (o teste de migration novo, opt-in via MIGRATION_TEST_DATABASE_URL)"
  artifacts:
    - path: "alembic/versions/032_pedido_standby_motivo.py"
      provides: "DDL da tabela pedido_standby_motivo com CHECK de 3 motivos e downgrade reversível"
      contains: "motivo IN ('sem_credito', 'sem_estoque', 'furo_grade')"
    - path: "app/modules/pedidos/infrastructure/models.py"
      provides: "Model ORM PedidoStandbyMotivo, PK composta (nr_pedido, cd_prod_cor)"
      contains: "class PedidoStandbyMotivo"
    - path: "alembic/env.py"
      provides: "Import registrado do model novo (autogenerate/alembic check enxergam a tabela)"
      contains: "PedidoStandbyMotivo"
    - path: "app/tests/test_migration_032_pedido_standby_motivo.py"
      provides: "Teste de round-trip upgrade -> CHECK (3 valores, rejeita 4º) -> downgrade -> re-upgrade"
      contains: "_URL_ENV = \"MIGRATION_TEST_DATABASE_URL\""
  key_links:
    - from: "alembic/versions/032_pedido_standby_motivo.py"
      to: "app/modules/pedidos/infrastructure/models.py::PedidoStandbyMotivo"
      via: "mesmas colunas/tipos/PK, verificado por 'alembic check' sem detectar diff"
      pattern: "motivo IN \\('sem_credito', 'sem_estoque', 'furo_grade'\\)"
    - from: "alembic/env.py"
      to: "app/modules/pedidos/infrastructure/models.py"
      via: "import na tupla existente de app.modules.pedidos.infrastructure.models, mesmo noqa: F401 da linha"
      pattern: "PedidoStandbyMotivo"
    - from: "app/tests/test_migration_032_pedido_standby_motivo.py"
      to: "alembic/versions/032_pedido_standby_motivo.py"
      via: "_alembic(database_url, \"upgrade\"/\"downgrade\", \"032\"/\"031\") via subprocess"
      pattern: "\"032\""
---

<objective>
Criar a migration Alembic isolada (schema puro) que dá lugar à tabela `pedido_standby_motivo` — grão `(nr_pedido, cd_prod_cor)`, com o CHECK constraint de **3** motivos (`sem_credito`, `sem_estoque`, `furo_grade`) desde a criação, nunca 2 — mais o model ORM correspondente `PedidoStandbyMotivo`, no mesmo arquivo/estilo do `EstoqueVirtual`. Este plano só entrega o schema: nenhuma escrita/leitura de aplicação é conectada a ele nesta plan (isso é 16-04/16-05).

**Fonte do DDL: `16-RESEARCH.md` § "Gap de desenho resolvido" e § "Code Examples" — NUNCA `.planning/research/v1.3/ARCHITECTURE.md` § 3.2, que está desatualizado e lista só 2 valores (`sem_credito`, `sem_estoque`). Copiar aquele DDL reproduziria exatamente o bug que a Fase 16 existe para resolver: furo de grade ficaria indistinguível de falta de estoque genérica, e a Phase 17 nunca teria dado para cumprir STANDBY-04.**

Purpose: STANDBY-01 (schema) — base de persistência sobre a qual 16-03 (wiring de domínio) e 16-04 (persistência real, upsert com `execucoes_consecutivas`) constroem.
Output: `alembic/versions/032_pedido_standby_motivo.py` (nova), `PedidoStandbyMotivo` em `app/modules/pedidos/infrastructure/models.py`, import registrado em `alembic/env.py`, `app/tests/test_migration_032_pedido_standby_motivo.py` (novo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-CONTEXT.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-RESEARCH.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-PATTERNS.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-VALIDATION.md

@alembic/env.py
@app/modules/pedidos/infrastructure/models.py
@alembic/versions/020_pedido_produto_read.py
@alembic/versions/031_auth_users_sso_provider.py
@app/tests/test_migration_030_auth_otp_code_hash.py
@app/tests/test_pipeline_safety.py
@app/tests/test_schema_guard.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration `032_pedido_standby_motivo` — CHECK com os 3 motivos, desde a criação</name>
  <files>alembic/versions/032_pedido_standby_motivo.py</files>
  <read_first>
    - 16-RESEARCH.md linhas 320-403 (§ "Code Examples" — migration completa já rascunhada: cabeçalho, DDL de 3 valores, nota de estilo do import `postgresql.UUID`) — fonte primária do DDL desta task.
    - 16-RESEARCH.md linhas 41-60 e 298-317 (§ "Gap de desenho resolvido" e "Pitfall 1: CHECK constraint com só 2 valores") — por que o DDL de `ARCHITECTURE.md` §3.2 está desatualizado e NUNCA deve ser copiado.
    - 16-VALIDATION.md linhas 30-34 (linha de base a reconfirmar) e linhas 59-70 (invariantes K5 — CHECK com 3 valores desde a criação — e K7 — migration reversível).
    - 16-PATTERNS.md linhas 26-75 (§ "1. Migration Alembic" — molde de cabeçalho, import do dialeto Postgres, `create_table`/`CheckConstraint`, `downgrade()` de 1 linha, o que replicar/adaptar de `020`/`031`).
    - alembic/versions/020_pedido_produto_read.py linhas 34-42 (import `from sqlalchemy.dialects import postgresql`) e linhas 358-433 (`op.create_table` com `CheckConstraint`s nomeados + `PrimaryKeyConstraint` + `downgrade()` de 1 linha — molde estrutural exato a seguir).
    - alembic/versions/031_auth_users_sso_provider.py linhas 1-29 (cabeçalho enxuto: docstring de bloco explicando o porquê, sem preflight SQL — mais próximo do tamanho desta migration do que `020`, que tem backfill).
  </read_first>
  <action>
Confirmar a head real ANTES de codar: `docker compose -f .docker/docker-compose.yml exec -T api uv run alembic heads` (confirmado nesta sessão de planejamento como `031 (head)` — reconfirmar no início da execução, pois pode ter mudado). Criar `alembic/versions/032_pedido_standby_motivo.py` com `revision: str = "032"`, `down_revision: str | None = "031"`, `branch_labels`/`depends_on` como `None`, seguindo literalmente o padrão de tipos/imports de `031_auth_users_sso_provider.py` (`from collections.abc import Sequence`, `import sqlalchemy as sa`, `from alembic import op`) mais `from sqlalchemy.dialects import postgresql` (necessário para `postgresql.UUID(as_uuid=True)` da coluna `job_id` — usar este import direto do módulo, NÃO `from sqlalchemy.dialects.postgresql import UUID`, que é o estilo de `models.py`, não de migration).

Docstring de bloco no topo do arquivo (título curto + corpo explicando o PORQUÊ, não o QUE): projeção recalculável no mesmo padrão de `estoque_virtual`/`pedido_produto_read`, grão `(nr_pedido, cd_prod_cor)`, fora do ciclo de vida do job durável (`pedido_processamentos`/`pedido_processamento_plan` são efêmeros, com `ON DELETE CASCADE` a partir de `durable_jobs` e sujeitos a expurgo — por isso `job_id` aqui é informativo, sem FK). Registrar explicitamente que os 3 motivos (`sem_credito`, `sem_estoque`, `furo_grade`) entram desde a criação, citando `16-RESEARCH.md` § "Gap de desenho resolvido" como fonte do DDL — nunca `ARCHITECTURE.md` §3.2, que está desatualizado com só 2 valores — porque mudar o CHECK depois de dados em produção seria uma migration extra evitável. Mencionar `execucoes_consecutivas` (contador agnóstico ao motivo, incrementado em upserts futuros — wiring real é 16-04) e que `downgrade()` só remove esta projeção, nenhuma tabela fonte é tocada.

`upgrade()`: um único `op.create_table("pedido_standby_motivo", ...)` com, nesta ordem: `nr_pedido` (`sa.Integer()`, `nullable=False`), `cd_prod_cor` (`sa.String(length=64)`, `nullable=False`), `canal` (`sa.String(length=16)`, `nullable=False`), `motivo` (`sa.String(length=16)`, `nullable=False`), `execucoes_consecutivas` (`sa.Integer()`, `nullable=False`, `server_default=sa.text("1")`), `job_id` (`postgresql.UUID(as_uuid=True)`, `nullable=False`, sem FK), `atualizado_em` (`sa.DateTime(timezone=True)`, `server_default=sa.text("now()")`, `nullable=False`); depois `sa.PrimaryKeyConstraint("nr_pedido", "cd_prod_cor")`; depois três `sa.CheckConstraint`s nomeados: `ck_psm_canal` com `"canal IN ('Franquia', 'Multimarca')"`, `ck_psm_motivo` com `"motivo IN ('sem_credito', 'sem_estoque', 'furo_grade')"` (os TRÊS valores — este é o núcleo do risco desta fase, ver K5), `ck_psm_execucoes_consecutivas` com `"execucoes_consecutivas >= 1"`. Sem preflight SQL, sem `LOCK TABLE` (tabela nasce vazia, sem backfill de dado existente).

`downgrade()`: só `op.drop_table("pedido_standby_motivo")` — molde 1:1 de `020_pedido_produto_read.py::downgrade`.

Depois de escrever o arquivo, aplicar e provar em AMBOS os bancos usados pela suíte deste repositório (não só o de desenvolvimento): `system_automation` (dev, usado por `alembic check` e pela CLI padrão) e `system_automation_test` (o banco que a suíte pytest geral usa via `TEST_DATABASE_URL`/`DATABASE_URL` herdado — `test_schema_guard.py` compara `Base.metadata` contra este banco a cada run, então ele precisa estar no head assim que o model existir, na Task 2). Rodar `alembic upgrade head` nos dois; inspecionar via `psql` a definição do `ck_psm_motivo` (deve conter literalmente os 3 valores); tentar um INSERT com `motivo='invalido'` (par `nr_pedido`/`cd_prod_cor` fictício, `canal='Franquia'`, `job_id`=um UUID literal, `atualizado_em`=`now()`) e confirmar que o Postgres rejeita com erro de `check constraint "ck_psm_motivo"` — prova ad hoc do risco central antes mesmo do teste dedicado da Task 3; depois `alembic downgrade 031` (tabela deve sumir, `to_regclass` retorna NULL) e `alembic upgrade head` de novo (deve recriar vazia) — round-trip rápido via CLI no banco `system_automation`. Deixar **ambos** os bancos (`system_automation` e `system_automation_test`) no head `032` ao final desta task, prontos para a Task 2 usar `alembic check` e para a suíte pytest geral enxergar a tabela.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run alembic heads</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run alembic upgrade head</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T -e DATABASE_URL=postgresql+asyncpg://automation:automation@dev_db:5432/system_automation_test api uv run alembic upgrade head</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T dev_db psql -U automation -d system_automation -c "SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'ck_psm_motivo'"</automated>
  </verify>
  <acceptance_criteria>
    - Suíte completa RODADA ANTES de qualquer edição (`docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q`) confirma a baseline real — se divergir de "826 passed, 16 skipped, 1 failed" (a única falha sendo `test_read_projection_empty_contracts_execute_real_sql`), registrar o número real encontrado e usá-lo como base de comparação para as próximas tasks, não assumir o número documentado cegamente.
    - `alembic heads` reporta `032 (head)` depois do arquivo criado.
    - `alembic upgrade head` sucede tanto contra `system_automation` quanto contra `system_automation_test` (via `-e DATABASE_URL=...system_automation_test`), sem erro.
    - `pg_get_constraintdef` de `ck_psm_motivo` contém literalmente as 3 substrings `sem_credito`, `sem_estoque` e `furo_grade` — nunca só 2.
    - Um INSERT manual via `psql` com `motivo='invalido'` (par fictício) retorna erro de violação do `ck_psm_motivo` — não é aceito silenciosamente.
    - `alembic downgrade 031` seguido de `SELECT to_regclass('public.pedido_standby_motivo')` via `psql` retorna `NULL`; `alembic upgrade head` em seguida recria a tabela (round-trip CLI ad hoc, complementar ao teste automatizado da Task 3).
    - Ao final da task, `system_automation` e `system_automation_test` estão AMBOS no head `032` (nenhum dos dois fica para trás, para não quebrar a Task 2/`test_schema_guard.py`).
  </acceptance_criteria>
  <done>Migration `032_pedido_standby_motivo.py` criada, aplicada com sucesso nos dois bancos (`system_automation`, `system_automation_test`), com CHECK de 3 motivos provado por inspeção direta e por tentativa de INSERT inválido, e reversibilidade provada por um round-trip CLI ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Model ORM `PedidoStandbyMotivo` + registro em `alembic/env.py`</name>
  <files>app/modules/pedidos/infrastructure/models.py, alembic/env.py</files>
  <read_first>
    - app/modules/pedidos/infrastructure/models.py linhas 1-27 (imports do arquivo, especialmente `from sqlalchemy.dialects.postgresql import JSONB` na linha 25 — precisa ganhar `UUID` nesta mesma linha de import) e linhas 82-129 (`EstoqueVirtual` — molde exato de estilo `Column(...)` explícito, docstring de bloco).
    - 16-PATTERNS.md linhas 78-136 (§ "2. Model ORM" — esqueleto de classe já fechado, "o que replicar" (estilo `Column`, docstring) vs "o que adaptar" (PK composta em vez de `id` autoincrement, sem FK para `durable_jobs`)).
    - 16-RESEARCH.md linhas 204-225 (§ "Recommended Project Structure" — por que este model vai neste arquivo e não em `processing/infrastructure/models.py`: projeções recalculáveis independentes do job durável usam estilo `Column(...)`, não `Mapped[...]`).
    - alembic/env.py linhas 27-33 (bloco de import `from app.modules.pedidos.infrastructure.models import (...)` — único ponto a editar neste arquivo; NUNCA remover o comentário `# noqa: F401` desta ou de qualquer outra linha do arquivo).
    - app/tests/test_schema_guard.py linhas 1-58 (imports de módulos de model para popular `Base.metadata`) e linhas 71-104/107-139 (os dois testes parametrizados sobre `sorted(Base.metadata.tables)` que vão automaticamente ganhar uma instância nova para `pedido_standby_motivo` assim que o model existir — **não precisa editar este arquivo**: `conftest.py` já importa `app.main`, que carrega `pedidos.infrastructure.models` por inteiro, registrando a tabela nova em `Base.metadata` mesmo sem este arquivo citar `PedidoStandbyMotivo` pelo nome).
  </read_first>
  <action>
Em `app/modules/pedidos/infrastructure/models.py`, alterar a linha `from sqlalchemy.dialects.postgresql import JSONB` (linha 25) para `from sqlalchemy.dialects.postgresql import JSONB, UUID`. Adicionar a classe `PedidoStandbyMotivo(Base)` entre a classe `EstoqueVirtual` (termina na linha 129) e a classe `PedidoModificacao` (começa na linha 132) — mesma vizinhança de projeções recalculáveis fora do job durável. Docstring de bloco no mesmo tom de `EstoqueVirtual`: explicar que a linha registra por que um par `(nr_pedido, cd_prod_cor)` está fora de uma OR nesta rodada de processamento; que o grão é o par, com upsert (nunca delete-then-insert) porque o motivo pode mudar entre rodadas (ex. estoque→crédito) sem a linha duplicar; que os 3 valores canônicos de `motivo` são `sem_credito`/`sem_estoque`/`furo_grade` (escrever os 3 valores literalmente no comentário — este arquivo não importa `domain/standby_motivo.py` para não criar acoplamento de infraestrutura para domínio; o CHECK constraint da migration é quem realmente impõe o vocabulário); que `job_id` é só informativo/auditoria, sem `ForeignKey` (job pode ser expurgado); e que a linha é apagada quando o par finalmente vira OR ou some do recorte de pendentes (wiring real fica para 16-04/16-05, fora do escopo desta plan).

`__tablename__ = "pedido_standby_motivo"`. Colunas, na MESMA ordem da migration (para leitura lado a lado): `nr_pedido = Column(Integer, primary_key=True)`; `cd_prod_cor = Column(String(64), primary_key=True)` (PK composta — NÃO usar `id` autoincrement + `UniqueConstraint`, diferente de `EstoqueVirtual`); `canal = Column(String(16), nullable=False)`; `motivo = Column(String(16), nullable=False)`; `execucoes_consecutivas = Column(Integer, nullable=False, default=1, server_default="1")`; `job_id = Column(UUID(as_uuid=True), nullable=False)`; `atualizado_em = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)`. Sem `__table_args__` extra — os `CheckConstraint`s vivem só na migration (mesmo padrão já usado por `EstoqueVirtual`/`OrdemReserva`/`PedidoModificacao`, nenhum dos quais replica CHECK no lado do model).

Em `alembic/env.py`, editar SOMENTE o bloco de import existente `from app.modules.pedidos.infrastructure.models import (...)` (linhas 27-33): adicionar a linha nova `PedidoStandbyMotivo,` à tupla de nomes importados, em ordem alfabética logo após `PedidoProcessado,` (ordem correta: `PedidoModificacao`, `PedidoProcessado`, `PedidoStandbyMotivo` — M < P < S). Não tocar o comentário `# noqa: F401` da primeira linha do bloco, nem nenhuma outra linha do arquivo (nenhum outro import, nenhuma lógica de lock/migration).
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run alembic check</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_schema_guard.py -k pedido_standby_motivo -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic check` reporta `No new upgrade operations detected.` — prova que o model espelha exatamente a migration (nenhum diff de coluna/tipo/PK detectado pelo autogenerate contra `system_automation`, já no head `032` desde a Task 1).
    - `pytest app/tests/test_schema_guard.py -k pedido_standby_motivo -q` reporta `2 passed` — as duas instâncias parametrizadas novas (`test_colunas_do_model_existem_no_banco[pedido_standby_motivo]`, `test_chave_primaria_do_model_bate_com_o_banco[pedido_standby_motivo]`) confirmam, contra `system_automation_test`, que colunas/nullability e a PK composta batem exatamente — sem precisar editar `test_schema_guard.py`.
    - `grep -c '# noqa: F401' alembic/env.py` retorna `8` (o mesmo número de antes desta task — nenhum comentário existente foi removido).
    - `grep -c 'PedidoStandbyMotivo' alembic/env.py` retorna `1` (import novo presente).
    - Suíte completa (`pytest -q`) mostra exatamente **+2 passed** em relação à baseline confirmada na Task 1, mesma contagem de `skipped` e `failed` da baseline (a única falha continua sendo `test_read_projection_empty_contracts_execute_real_sql`) — nenhuma regressão introduzida.
  </acceptance_criteria>
  <done>`PedidoStandbyMotivo` existe em `app/modules/pedidos/infrastructure/models.py` com PK composta e colunas idênticas à migration 032 (`alembic check` sem diff); `alembic/env.py` importa o model novo sem remover nenhum `noqa` existente; `test_schema_guard.py` confirma columns/PK batendo contra `system_automation_test` sem precisar de nenhuma edição naquele arquivo.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de migration — round-trip upgrade/CHECK/downgrade/re-upgrade</name>
  <files>app/tests/test_migration_032_pedido_standby_motivo.py</files>
  <read_first>
    - app/tests/test_migration_030_auth_otp_code_hash.py (arquivo inteiro — harness a copiar 1:1: `_BACKEND_ROOT`, `_URL_ENV`, `_DATABASE_PREFIX`, `_LOCAL_HOSTS`, `_isolated_database_url`, `_execute_async`, `_execute`, `_reset`, `_alembic`, fixture `migration_database_url` — nenhum helper reescrito, só o docstring do módulo e o corpo do teste mudam).
    - app/tests/test_pipeline_safety.py (arquivo inteiro — `test_todos_os_testes_destrutivos_exigem_prefixo_scratch_comum` falha para QUALQUER `test_migration_*.py` que não contenha literalmente `_URL_ENV = "MIGRATION_TEST_DATABASE_URL"` e `_DATABASE_PREFIX = "automation_migration_test"` — copiar o harness do molde já satisfaz isso automaticamente).
    - 16-PATTERNS.md linhas 261-281 (§ "6. Teste de migration" — convenção confirmada, o que replicar/adaptar: asserts de schema específicos da tabela nova em vez de `character_maximum_length`).
    - 16-VALIDATION.md linhas 59-70 (K5 — CHECK aceita os 3 valores desde a criação — e K7 — migration reversível) e linhas 104-109 (riscos de validação: DDL com só 2 motivos é o risco central desta fase).
    - README.md linhas 51-64 (comando de banco descartável dedicado a `test_migration_*.py`: `createdb`, prefixo `automation_migration_test`, host de loopback, variável `MIGRATION_TEST_DATABASE_URL`).
  </read_first>
  <action>
Criar `app/tests/test_migration_032_pedido_standby_motivo.py` copiando integralmente a infraestrutura de `test_migration_030_auth_otp_code_hash.py` (`_BACKEND_ROOT`, `_URL_ENV = "MIGRATION_TEST_DATABASE_URL"`, `_DATABASE_PREFIX = "automation_migration_test"`, `_LOCAL_HOSTS`, `_isolated_database_url`, `_execute_async`, `_execute`, `_reset`, `_alembic`, fixture `migration_database_url`) — trocar só o docstring do módulo para "Ciclo destrutivo isolado da migration 032 (tabela pedido_standby_motivo)." ou equivalente.

Escrever um único teste, `test_upgrade_cria_tabela_com_check_de_tres_motivos_e_downgrade_reverte`, com esta sequência: (1) `_alembic(migration_database_url, "upgrade", "032")`; assert que `SELECT version_num FROM alembic_version` retorna `[("032",)]`. (2) Assert de schema via `information_schema.columns` (`WHERE table_name = 'pedido_standby_motivo'`) confirmando que as 7 colunas esperadas existem: `nr_pedido`, `cd_prod_cor`, `canal`, `motivo`, `execucoes_consecutivas`, `job_id`, `atualizado_em`. (3) Inserir uma linha com `motivo='furo_grade'` via `_execute` com parâmetros nomeados (`nr_pedido` e `cd_prod_cor` de exemplo, `canal='Franquia'`, `job_id`=um UUID literal de teste, `atualizado_em=now()`) e confirmar por SELECT que a linha volta com `motivo='furo_grade'` — prova direta de que o TERCEIRO valor (o que o DDL desatualizado do `ARCHITECTURE.md` não previa) é aceito desde a criação (K5). (4) Tentar inserir uma SEGUNDA linha, com um par `(nr_pedido, cd_prod_cor)` diferente do passo 3 e `motivo='invalido'`, dentro de um bloco `pytest.raises` capturando a exceção do driver (`sqlalchemy.exc.IntegrityError`, que embrulha o `asyncpg.exceptions.CheckViolationError` do Postgres) — confirma que o Postgres rejeita qualquer motivo fora do vocabulário, nunca aceitando silenciosamente. (5) `_alembic(migration_database_url, "downgrade", "031")`; assert que `SELECT to_regclass('public.pedido_standby_motivo')` retorna `[(None,)]` — tabela sumiu (K7). (6) `_alembic(migration_database_url, "upgrade", "032")` de novo; assert que `to_regclass` volta a apontar para a tabela E que `SELECT count(*) FROM pedido_standby_motivo` retorna `[(0,)]` — reconstruída vazia, sem nenhum resíduo dos dados inseridos no passo 3 (prova de round-trip completo, mesmo padrão do molde de `test_migration_030`).
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec dev_db createdb -U automation automation_migration_test_local_test</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T -e MIGRATION_TEST_DATABASE_URL=postgresql+asyncpg://automation:automation@dev_db:5432/automation_migration_test_local_test api uv run pytest app/tests/test_migration_032_pedido_standby_motivo.py -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T dev_db dropdb -U automation automation_migration_test_local_test</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pipeline_safety.py -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Se `automation_migration_test_local_test` já existir de uma execução anterior não limpa, usar `dropdb`/`createdb` para garantir um banco vazio antes do teste — o teste é destrutivo por natureza (`DROP SCHEMA public CASCADE` no setup/teardown da fixture).
    - `pytest app/tests/test_migration_032_pedido_standby_motivo.py -q` com `MIGRATION_TEST_DATABASE_URL` apontando para `automation_migration_test_local_test` reporta `1 passed`.
    - Dentro do teste: o INSERT com `motivo='furo_grade'` sucede (passo 3) e o INSERT com `motivo='invalido'` levanta `IntegrityError`/`CheckViolation` (passo 4) — as duas pontas do vocabulário de 3 valores, provadas na mesma execução.
    - Após `downgrade "031"`, `to_regclass('public.pedido_standby_motivo')` é `NULL`; após `upgrade "032"` de novo, a tabela existe e está vazia (`count(*) = 0`) — round-trip completo, sem dado residual.
    - Banco descartável (`automation_migration_test_local_test`) removido ao final (`dropdb`) — nenhum artefato específico de ambiente deixado para trás.
    - `pytest app/tests/test_pipeline_safety.py -q` continua `2 passed` — o arquivo novo satisfaz o contrato de `_URL_ENV`/`_DATABASE_PREFIX` sem precisar de nenhuma edição adicional.
    - Suíte completa geral (sem `MIGRATION_TEST_DATABASE_URL`) mostra a mesma contagem de `passed`/`failed` da Task 2 (nenhuma regressão) e exatamente **+1 skipped** em relação à baseline (o teste novo se auto-descarta corretamente via `pytest.skip` quando a variável de ambiente não está definida — o mesmo comportamento já estabelecido pelos outros 8 arquivos `test_migration_*.py`).
  </acceptance_criteria>
  <done>`test_migration_032_pedido_standby_motivo.py` prova, com um banco descartável real, o round-trip completo: upgrade cria a tabela com o CHECK de 3 motivos (aceitando `furo_grade`, rejeitando um 4º valor), downgrade remove a tabela, e um novo upgrade a reconstrói vazia. `test_pipeline_safety.py` e a suíte completa permanecem verdes, com os deltas esperados desta plan já confirmados nas Tasks 2/3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Código interno (domínio/processing, waves futuras 16-03/16-04) → tabela `pedido_standby_motivo` | Toda escrita nesta tabela vem de código Python confiável, sem input de usuário HTTP nesta fase — o `CHECK` constraint é a última linha de defesa contra um bug de aplicação que tente persistir um motivo ou canal fora do vocabulário canônico |
| Alembic autogenerate (dev/CI) → schema real do Postgres | Se o model ORM divergir da migration, um autogenerate futuro pode propor DDL destrutivo (`DROP`/`ALTER`) tentando "corrigir" a divergência às custas de dado real — `alembic check` nesta plan existe justamente para fechar essa divergência antes que aconteça |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | `ck_psm_motivo` — CHECK constraint incompleto (2 valores em vez de 3, o bug documentado e literalmente presente em `ARCHITECTURE.md` §3.2) permitiria que `furo_grade` fosse rejeitado na escrita ou nunca existisse como valor válido, colapsando-o silenciosamente em `sem_estoque` numa wave futura | mitigate | DDL desta migration cita `16-RESEARCH.md` como fonte única (nunca `ARCHITECTURE.md`); `test_migration_032_pedido_standby_motivo.py` insere `motivo='furo_grade'` (deve suceder) e um 4º valor arbitrário `'invalido'` (deve levantar `IntegrityError`) na mesma execução — prova positiva e negativa lado a lado (K5) |
| T-16-02-02 | Tampering | `ck_psm_canal` — canal fora do vocabulário `Franquia`/`Multimarca` persistido por um bug de fan-out futuro | mitigate | `CheckConstraint` nomeado `ck_psm_canal` desde a criação, mesmo vocabulário já usado em `pedido_produto_read` (`ck_ppr_canal`) |
| T-16-02-03 | Tampering | `execucoes_consecutivas` chegando a zero ou negativo por um bug de upsert em wave futura (16-04) | mitigate | `CheckConstraint` `ck_psm_execucoes_consecutivas >= 1` desde a criação — defesa em profundidade antes mesmo do upsert existir |
| T-16-02-04 | Tampering / Repudiation | Migration irreversível deixaria o schema preso em caso de rollback manual de deploy, sem caminho de volta documentado | mitigate | `downgrade()` testado explicitamente em `test_migration_032_pedido_standby_motivo.py` — drop + re-upgrade round-trip completo, provando reconstrução vazia sem resíduo (K7) |
| T-16-02-05 | Tampering | Remover acidentalmente um `# noqa: F401` existente em `alembic/env.py` ao adicionar o import novo faria o autogenerate futuro propor `DROP TABLE` de tabelas já em produção (o próprio `CLAUDE.md` do repo marca isso como proibição explícita) | mitigate | Task 2 restrita a SOMENTE adicionar `PedidoStandbyMotivo` à tupla de import já existente, nunca reescrever as linhas ao redor; acceptance criteria com `grep -c '# noqa: F401'` confirmando contagem inalterada (8 antes e depois) |
| T-16-02-06 | Tampering | Model ORM divergir silenciosamente da migration (tipo/nullability/PK diferente) — reproduz a classe de bug documentada em `test_schema_guard.py` (PK composta perdida em 2026-07-23, `IntegrityError` em produção) | mitigate | `alembic check` (sem diff) e `test_schema_guard.py -k pedido_standby_motivo` (2 passed, contra `system_automation_test` real) como gates automatizados da Task 2 — nenhuma confiança em leitura visual |
| T-16-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova instalada nesta plan (Package Legitimacy Audit do `16-RESEARCH.md` confirma N/A) — só SQLAlchemy/Alembic/pytest já presentes |

Nenhuma ameaça de severidade relevante permanece sem mitigação testável. O escopo desta plan é estritamente schema (migration + model) — nenhuma escrita/leitura de aplicação real está conectada ainda, então o raio de impacto de qualquer regressão aqui fica contido a "a tabela não existe/está errada", nunca a corromper dado de negócio já em produção.
</threat_model>

<verification>
- `docker compose -f .docker/docker-compose.yml exec -T api uv run alembic heads` reporta `032 (head)`.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run alembic check` reporta `No new upgrade operations detected.` — model e migration em paridade exata.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_schema_guard.py app/tests/test_pipeline_safety.py -q` — verde.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa, sem `MIGRATION_TEST_DATABASE_URL`) — mesma 1 falha conhecida, +2 passed (schema guard) e +1 skipped (teste de migration novo) em relação à baseline confirmada no início da execução. Nenhuma wave desta fase pode terminar vermelha.
- Com `MIGRATION_TEST_DATABASE_URL` apontando para um banco `automation_migration_test*` descartável: `pytest app/tests/test_migration_032_pedido_standby_motivo.py -q` — `1 passed`, provando o round-trip completo (upgrade, CHECK de 3 valores nos dois sentidos, downgrade, re-upgrade).
</verification>

<success_criteria>
- Tabela `pedido_standby_motivo` existe no Postgres (dev e test) com `PRIMARY KEY (nr_pedido, cd_prod_cor)` e os 3 `CheckConstraint`s (`ck_psm_canal`, `ck_psm_motivo` com os 3 valores, `ck_psm_execucoes_consecutivas`).
- Model ORM `PedidoStandbyMotivo` existe em `app/modules/pedidos/infrastructure/models.py`, espelhando a migration byte a byte na visão do SQLAlchemy (`alembic check` sem diff, `test_schema_guard.py` verde).
- `alembic/env.py` importa o model novo sem remover nenhum `# noqa: F401` existente.
- Migration é reversível, provado por teste automatizado dedicado (`test_migration_032_pedido_standby_motivo.py`), não só por inspeção manual.
- Nenhum arquivo fora do escopo desta plan foi tocado (`alembic/versions/` — só o arquivo novo; `app/modules/pedidos/infrastructure/models.py`; `alembic/env.py` — só o import; o teste de migration novo). Nenhum wiring de aplicação (`processing/`, `motor_adequacao.py`) foi alterado.
- Suíte completa permanece na linha de base confirmada no início da execução, com os deltas esperados desta plan e nenhuma regressão nova.
</success_criteria>

## Artifacts this phase produces

- `alembic/versions/032_pedido_standby_motivo.py` (novo) — DDL da tabela `pedido_standby_motivo`, CHECK de 3 motivos desde a criação, `downgrade()` de 1 linha (projeção recalculável, nenhuma tabela fonte tocada).
- `app/modules/pedidos/infrastructure/models.py` (modificado) — `+ class PedidoStandbyMotivo`, `+ UUID` no import de `sqlalchemy.dialects.postgresql`.
- `alembic/env.py` (modificado) — `+ PedidoStandbyMotivo` na tupla de import existente do módulo `pedidos.infrastructure.models`, nenhum `noqa` removido.
- `app/tests/test_migration_032_pedido_standby_motivo.py` (novo) — teste de round-trip upgrade/CHECK (aceita 3 valores, rejeita um 4º)/downgrade/re-upgrade, usando o harness já estabelecido de banco descartável.

<output>
Create `.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-02-SUMMARY.md` when done
</output>
