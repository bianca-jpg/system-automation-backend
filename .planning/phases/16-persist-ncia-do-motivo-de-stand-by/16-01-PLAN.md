---
phase: 16-persist-ncia-do-motivo-de-stand-by
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/modules/pedidos/domain/standby_motivo.py
  - app/modules/pedidos/domain/motor_adequacao.py
  - app/tests/test_pedidos_motor.py
  - app/tests/test_pedidos_motor_furo_grade.py
autonomous: true
requirements: [STANDBY-01]

must_haves:
  truths:
    - "Um par preterido por furo de grade tem preteridos_motivo[par] == FURO_GRADE ('furo_grade'), distinto de um par preterido por falta de estoque genérica (SEM_ESTOQUE, 'sem_estoque') — provado nos dois modos, ADEQUAR e SEM_ADEQUAR, no mesmo teste"
    - "Orçamento de corte excedido no modo ADEQUAR (ledger ±5%) grava SEM_ESTOQUE — mesmo bucket de falta de estoque simples, não um motivo à parte"
    - "Todo par em preteridos tem uma entrada correspondente em preteridos_motivo, sempre — len(preteridos) == len(preteridos_motivo) em qualquer cenário, incluindo o ramo defensivo de canal não identificado"
    - "As 5 chaves de retorno de processar_pedidos que já existiam (resultados, selecionados, preteridos, bloqueados_credito, pares_processados) continuam com a mesma forma; preteridos_motivo é uma 6ª chave puramente aditiva — nenhum teste existente quebra"
    - "A chamada hoje existente em processing/domain.py (build_processing_plan -> processar_pedidos, que não lê preteridos_motivo ainda) continua funcionando sem alteração — este plano não toca processing/"
  artifacts:
    - path: "app/modules/pedidos/domain/standby_motivo.py"
      provides: "Vocabulário canônico curto de motivo de stand-by (STANDBY-01/04): FURO_GRADE, SEM_ESTOQUE, SEM_CREDITO + MOTIVOS_VALIDOS"
      exports: ["FURO_GRADE", "SEM_ESTOQUE", "SEM_CREDITO", "MOTIVOS_VALIDOS"]
    - path: "app/modules/pedidos/domain/motor_adequacao.py"
      provides: "preteridos_motivo populado nos 4 pontos reais de escrita em preteridos (2 furo, 1 via _commitar_par compartilhado pelos dois modos, 1 ramo defensivo de canal não identificado) e mesclado no retorno de processar_pedidos/_processar_pedidos_canal"
      contains: "preteridos_motivo"
  key_links:
    - from: "_processar_pedidos_canal (motor_adequacao.py, linhas ~750-763 e ~821-834)"
      to: "app/modules/pedidos/domain/standby_motivo.py"
      via: "import direto de FURO_GRADE/SEM_ESTOQUE, um por chamada de tem_furo_de_grade"
      pattern: "from app\\.modules\\.pedidos\\.domain\\.standby_motivo import"
    - from: "_commitar_par (motor_adequacao.py, ramo else de gerou_or)"
      to: "preteridos_motivo[par] = SEM_ESTOQUE"
      via: "escrita compartilhada pelos dois modos (SEM_ADEQUAR e ADEQUAR chamam a mesma função)"
      pattern: "preteridos_motivo\\[par\\] = SEM_ESTOQUE"
    - from: "processar_pedidos (merged, linha ~513-519 e ~563-567)"
      to: "_processar_pedidos_canal (retorno, linha ~939-948)"
      via: "merge de parcial[\"preteridos_motivo\"] por canal, mais o fallback do ramo pares_sem_canal"
      pattern: "preteridos_motivo"
---

<objective>
Criar o vocabulário canônico de motivo de stand-by (`domain/standby_motivo.py`) e fechar o gap de desenho identificado em `16-CONTEXT.md`: hoje o motor já sabe diferenciar furo de grade de falta de estoque genérica (`tem_furo_de_grade` devolve um `motivo_furo` próprio), mas essa distinção morre em `resultados[par]` e nunca chega a `processing/` porque `build_processing_plan` chama `processar_pedidos(..., resultados_apenas_selecionados=True)`. Este plano adiciona `preteridos_motivo: dict[tuple[int, str], str]` como uma 6ª chave **puramente aditiva** ao retorno de `processar_pedidos`/`_processar_pedidos_canal`, populada nos 4 pontos reais onde um par entra em `preteridos` — 2 pontos de furo de grade (um por modo, `FURO_GRADE`), 1 ponto compartilhado pelos dois modos via `_commitar_par` (`SEM_ESTOQUE`, cobrindo tanto falta de estoque simples quanto orçamento de corte excedido no modo ADEQUAR) e 1 ramo defensivo de canal não identificado (`SEM_ESTOQUE` como fallback, ver decisão do achado A1 abaixo).

**Decisão do achado A1 (código morto, `16-RESEARCH.md` § "Achado adicional não previsto"):** o ramo `pares_sem_canal` (linhas ~524-546) é código morto no caminho real de `build_processing_plan` — `_eligible_pairs` já garante canal canônico por par antes do motor rodar, então nenhum item chega a este motor com `canal=None` quando disparado por aquele caminho. Mesmo assim, este plano popula `preteridos_motivo` ali também (fallback `SEM_ESTOQUE`), por defensividade: a alternativa seria deixar a invariante `len(preteridos) == len(preteridos_motivo)` quebrável por um chamador futuro que não seja `build_processing_plan` (ex. um teste direto do motor, como os que esta fase escreve). Custa 1 linha e fecha a invariante universalmente. Não é criado um 4º valor (ex. `"canal_invalido"`) porque a coluna `motivo` da tabela nova (Phase 16, plano 16-02) terá `CHECK` fechado em exatamente 3 valores desde a criação (K5 de `16-VALIDATION.md`) — introduzir um 4º valor aqui obrigaria uma migration extra.

**Nota descoberta durante o planejamento, fora do escopo deste plano:** `app/tests/invariantes_motor_adequacao.py::assert_contrato_chaves_resultado` (I9, reutilizado pelos testes de propriedade da Phase 19, hoje não chamado contra saída real de `processar_pedidos` em nenhum teste da suíte) verifica `set(resultado.keys()) == {5 chaves antigas}` — um conjunto FECHADO que rejeita chave extra (há inclusive um teste, `test_assert_contrato_chaves_resultado_falha_com_chave_extra`, provando que a função falha com chave a mais). Este plano **não toca** `invariantes_motor_adequacao.py` nem `test_invariantes_motor_adequacao.py` — não estão na lista de arquivos autorizados. Como nada na suíte atual chama I9 contra a saída real do motor, a suíte completa continua verde. Mas o conjunto `_CHAVES_CONTRATO_RESULTADO` (linha 13-19) ficará desatualizado (5 chaves, deveria ser 6) a partir deste plano — registrar esta descoberta no SUMMARY para que a Phase 19 (ou um plano futuro que precise dela antes) atualize esse conjunto ao ligar I9 contra saída real.

Purpose: sem esta distinção nascendo no motor, a Phase 17 (STANDBY-04, rótulo próprio de furo de grade na UI) nunca teria dado para renderizar — ela é só UI, não pode inventar um dado que não existe.

Output: `app/modules/pedidos/domain/standby_motivo.py` (novo), `app/modules/pedidos/domain/motor_adequacao.py` (editado, `preteridos_motivo` aditivo), testes cobrindo os 4 pontos de escrita + a distinção furo/estoque nos dois modos + o caráter aditivo do campo, em `app/tests/test_pedidos_motor.py`/`test_pedidos_motor_furo_grade.py`, suíte completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-CONTEXT.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-VALIDATION.md
@.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-PATTERNS.md

# 16-RESEARCH.md já fechou a assinatura exata (§ "Gap de desenho resolvido:
# motivo do furo de grade") com os 7 pontos de edição linha a linha em
# motor_adequacao.py — ler só essa seção (não o documento inteiro).

@app/modules/pedidos/domain/motor_adequacao.py
@app/modules/pedidos/domain/furo_de_grade.py
@app/tests/test_pedidos_motor_furo_grade.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline da suíte + vocabulário canônico (standby_motivo.py)</name>
  <files>app/modules/pedidos/domain/standby_motivo.py, app/tests/test_pedidos_motor.py</files>
  <read_first>
    - app/modules/pedidos/domain/furo_de_grade.py (arquivo inteiro, já em contexto) — molde de módulo-folha do domínio: docstring de módulo curta explicando o porquê, só stdlib/tipos, `__all__` explícito no fim.
    - .planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-RESEARCH.md, § "Novo vocabulário canônico — arquivo dedicado" (as 3 constantes + `MOTIVOS_VALIDOS = frozenset(...)` já fechadas: `SEM_CREDITO = "sem_credito"`, `SEM_ESTOQUE = "sem_estoque"`, `FURO_GRADE = "furo_grade"`).
    - .planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-VALIDATION.md, invariante K5 (o `CHECK` da tabela nova, plano 16-02, precisa aceitar exatamente estes 3 valores desde a criação — os literais aqui são a fonte da verdade que a migration vai citar depois, nunca redigitar solto).
  </read_first>
  <action>
Primeiro, ANTES de qualquer edição, rodar a suíte completa via Docker para confirmar a linha de base real deste momento (não assumir os 826 passed/16 skipped/1 failed anotados em `16-VALIDATION.md` — o tempo pode ter passado entre o planejamento e agora). Registrar os números exatos observados; se divergirem do que `16-VALIDATION.md` documenta, isso é um achado a registrar no SUMMARY, não um bloqueio (a única falha aceitável continua sendo `test_pedidos_read_projection.py::test_read_projection_empty_contracts_execute_real_sql`, teardown asyncpg pré-existente).

Criar `app/modules/pedidos/domain/standby_motivo.py`, módulo-folha sem nenhum import (nem stdlib além do que for estritamente necessário — neste caso, nenhum). Docstring de módulo de 4-6 linhas explicando: (1) este é o vocabulário CURTO e CANÔNICO que atravessa a fronteira do motor até a tabela `pedido_standby_motivo` (Phase 16, plano 16-02) e dali até a UI (Phase 17, STANDBY-04); (2) contraste explícito com `motivo_stand_by` (`motor_adequacao.marcar_stand_by`), que carrega uma frase longa e dinâmica (ex. com o tamanho específico do furo) para log/depuração — os dois propósitos são deliberadamente distintos, a frase pode mudar livremente sem quebrar nada a jusante, estas constantes não podem (viram `CHECK constraint` em banco); (3) um único lugar para as 3 strings evita que motor, `processing/domain.py` (plano 16-03) e a migration (plano 16-02) divirjam entre si — nunca redigitar os literais soltos em outro arquivo. Definir exatamente três constantes de módulo, nesta ordem: `SEM_CREDITO = "sem_credito"`, `SEM_ESTOQUE = "sem_estoque"`, `FURO_GRADE = "furo_grade"`. Logo abaixo, `MOTIVOS_VALIDOS = frozenset({SEM_CREDITO, SEM_ESTOQUE, FURO_GRADE})` (usado por validações defensivas em planos futuros — não usado ainda por este plano, mas parte do vocabulário canônico fechado pela pesquisa). Terminar com `__all__ = ["SEM_CREDITO", "SEM_ESTOQUE", "FURO_GRADE", "MOTIVOS_VALIDOS"]`.

Em `app/tests/test_pedidos_motor.py`, adicionar uma seção nova `# standby_motivo — vocabulário canônico` (posição: logo após o bloco de imports, antes da seção `# canal_bucket / is_sem_credito / get_tamanho_idx`) com um teste `test_standby_motivo_valores_canonicos_e_distintos`: importar `FURO_GRADE, SEM_CREDITO, SEM_ESTOQUE, MOTIVOS_VALIDOS` de `app.modules.pedidos.domain.standby_motivo` e assertar os 3 valores literais exatos (`"furo_grade"`, `"sem_credito"`, `"sem_estoque"`), que os 3 são mutuamente distintos (`len({FURO_GRADE, SEM_CREDITO, SEM_ESTOQUE}) == 3`), e que `MOTIVOS_VALIDOS == {FURO_GRADE, SEM_CREDITO, SEM_ESTOQUE}`.

Rodar a suíte completa de novo ao final da task para confirmar que nada regrediu (nenhuma função existente foi tocada; o módulo novo não é importado por ninguém ainda além do teste novo).
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -k standby_motivo -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -k standby_motivo -q` termina com `1 passed`.
    - `grep -c "^from\|^import" app/modules/pedidos/domain/standby_motivo.py` retorna `0` (módulo-folha, zero imports).
    - `grep -n "FURO_GRADE = \"furo_grade\"" app/modules/pedidos/domain/standby_motivo.py` e `grep -n "SEM_ESTOQUE = \"sem_estoque\"" app/modules/pedidos/domain/standby_motivo.py` e `grep -n "SEM_CREDITO = \"sem_credito\"" app/modules/pedidos/domain/standby_motivo.py` mostram, cada um, exatamente 1 linha com o literal correto.
    - `grep -n "__all__" app/modules/pedidos/domain/standby_motivo.py` mostra os 4 nomes exportados.
    - Suíte completa (`pytest -q`) termina com a mesma contagem de falhas observada na linha de base desta execução (só a falha conhecida de teardown asyncpg), e com exatamente +1 passed em relação à linha de base (o teste novo desta task).
  </acceptance_criteria>
  <done>standby_motivo.py existe com as 3 constantes + MOTIVOS_VALIDOS, testado isoladamente, suíte completa sem regressão em relação à linha de base confirmada no início desta task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: preteridos_motivo — furo de grade vs falta de estoque, aditivo, nos dois modos</name>
  <files>app/modules/pedidos/domain/motor_adequacao.py, app/tests/test_pedidos_motor.py, app/tests/test_pedidos_motor_furo_grade.py</files>
  <read_first>
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 1-28 (bloco de imports do topo) e linha 17 (`from app.modules.pedidos.domain.furo_de_grade import tem_furo_de_grade`) — ponto de inserção do novo import de `standby_motivo`.
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 422-452 (`_commitar_par` completa) — assinatura atual (8 parâmetros posicionais + `cancel_token` keyword-only) e o ramo `else` (linhas 449-452, quando `not gerou_or`) onde `preteridos.append(par)` acontece hoje.
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 455-501 (assinatura e docstring "Retorna:" de `processar_pedidos`) — atualizar a lista de chaves documentadas.
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 503-572 (`processar_pedidos` corpo: `merged` init linha 513-519, laço `pares_sem_canal` linha 524-546 com o append em preteridos na linha 538, chamada a `_processar_pedidos_canal` e merge do `parcial` linhas 548-567).
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 575-948 (`_processar_pedidos_canal` completa): early-return vazio linha 604-610; declaração local `preteridos: list[...] = []` linha 614 (ponto de inserção da nova variável local ao lado); ramo SEM_ADEQUAR com o call site de `tem_furo_de_grade` linha 750-763 e o call site de `_commitar_par` linha 771-781; ramo ADEQUAR com o call site de `tem_furo_de_grade` no laço de `produtos_elegiveis` linha 821-834 e o call site de `_commitar_par` linha 901-911; retorno final do dict linha 939-948.
    - app/modules/pedidos/domain/orcamento_pedido.py (arquivo inteiro, já em contexto) — confirma que `consumir_corte` é tudo-ou-nada e que a falha (`concedido=False`) delega inteiramente a `marcar_stand_by(motivo=_MOTIVO_ORCAMENTO_CORTE, ...)` dentro de `adequar_grade_produto` (linhas 184-189 de motor_adequacao.py) — por isso este caminho SEMPRE cai no ramo `else` de `_commitar_par` (nenhum item fica "Gerar OR"), nunca precisando de um motivo à parte.
    - app/tests/test_pedidos_motor.py, linhas 634-667 (`test_processar_pedidos_fatura_um_produto_e_deixa_o_outro_em_stand_by`) e linhas 92-133 (`test_processar_pedidos_orcamento_pedido_corte_atravessa_produtos_respeita_teto`) — cenários de referência de "sem estoque simples" (ADEQUAR, tamanho único) e "orçamento de corte excedido" (ambos devem virar SEM_ESTOQUE).
    - app/tests/test_pedidos_motor.py, linhas 511-559 (`test_processar_pedidos_sem_canal_vai_para_stand_by_sem_consumir_franquia`) — cenário de referência do ramo defensivo (canal=None), a estender com a nova asserção de `preteridos_motivo`.
    - app/tests/test_pedidos_motor_furo_grade.py, linhas 98-131 (`test_processar_pedidos_furo_de_grade_isolado_bloqueia_par_unico`) — cenário de referência de furo isolado em modo ADEQUAR (default), a usar como base para o novo teste desta task.
    - app/tests/test_pedidos_motor_politica_quantidade.py, linhas 156-187 (`test_processar_pedidos_sem_adequar_rejeicao_e_por_par_nao_por_pedido`) — sintaxe de chamada `processar_pedidos(dados, estoque, set(), modo=ModoAdequacao.SEM_ADEQUAR)` a replicar no teste SEM_ADEQUAR desta task.
    - app/tests/factories_pedidos_motor.py (arquivo inteiro, 63 linhas) — helpers `item_pedido`/`estoque_produto`/`estoque_por_canal`, já importados em `test_pedidos_motor_furo_grade.py`, a reusar nos dois testes novos desse arquivo.
  </read_first>
  <behavior>
    - Teste 1 (`test_pedidos_motor_furo_grade.py::test_processar_pedidos_preteridos_motivo_distingue_furo_de_sem_estoque_modo_adequar`): pedido único `nr_pedido=1` com dois produtos, modo ADEQUAR (default, sem passar `modo`). Produto `"FURO"`: 3 tamanhos `PP=1,M=1,G=1` (todos `vl_liquido=100.0`), estoque `FURO_PP=5, FURO_M=0, FURO_G=5` (furo no meio, M). Produto `"CORTE"`: tamanho único `M=100` (`vl_liquido=1000.0`), estoque `CORTE_M=0` (falta total = 100, muito acima do orçamento de corte do pedido — `total_original` do placeholder soma TODOS os itens do pedido, 3 do produto FURO + 100 do CORTE = 103, `limite_corte = floor(103*5/100) = 5`; `consumir_corte(100)` falha, `_MOTIVO_ORCAMENTO_CORTE`). Chamar `processar_pedidos(itens, estoque, ja_processados=set())`. Assertar: `resultado["preteridos"] == [(1, "CORTE"), (1, "FURO")]` (ordem alfabética); `resultado["preteridos_motivo"][(1, "FURO")] == FURO_GRADE`; `resultado["preteridos_motivo"][(1, "CORTE")] == SEM_ESTOQUE`; os dois valores são diferentes entre si; `len(resultado["preteridos"]) == len(resultado["preteridos_motivo"]) == 2`.
    - Teste 2 (`test_pedidos_motor_furo_grade.py::test_processar_pedidos_preteridos_motivo_distingue_furo_de_sem_estoque_modo_sem_adequar`): mesmo `nr_pedido=1`, modo `ModoAdequacao.SEM_ADEQUAR` explícito. Produto `"FURO"`: mesma grade de 3 tamanhos com furo no meio (idêntico ao Teste 1). Produto `"SEMESTOQUE"`: tamanho único `M=5`, estoque `SEMESTOQUE_M=0` (falta total 5 > 0, único tamanho então nunca é furo — vai direto para `aplicar_tudo_ou_nada` -> stand-by "Sem adequação: grade completa indisponível" -> `_commitar_par`). Chamar `processar_pedidos(itens, estoque, ja_processados=set(), modo=ModoAdequacao.SEM_ADEQUAR)`. Assertar: `resultado["preteridos"] == [(1, "FURO"), (1, "SEMESTOQUE")]`; `resultado["preteridos_motivo"][(1, "FURO")] == FURO_GRADE`; `resultado["preteridos_motivo"][(1, "SEMESTOQUE")] == SEM_ESTOQUE`; os dois valores diferentes; `len(resultado["preteridos"]) == len(resultado["preteridos_motivo"]) == 2`.
    - Teste 3 (`test_pedidos_motor.py::test_processar_pedidos_preteridos_motivo_e_campo_aditivo_chaves_existentes_preservadas`): reusar o cenário de `test_processar_pedidos_fatura_um_produto_e_deixa_o_outro_em_stand_by` (produto `"COM_ESTOQUE"` gera OR, produto `"SEM_ESTOQUE"` fica em stand-by, ambos tamanho único `M`, modo ADEQUAR default — falta de estoque simples, sem furo possível). Chamar `processar_pedidos(...)`. Assertar `set(resultado.keys()) == {"resultados", "selecionados", "preteridos", "bloqueados_credito", "pares_processados", "preteridos_motivo"}` (exatamente 6 chaves — prova que nenhuma das 5 antigas foi renomeada/removida e que só uma nova foi adicionada); `resultado["preteridos"] == [(1, "SEM_ESTOQUE")]` (comportamento antigo intacto); `resultado["preteridos_motivo"] == {(1, "SEM_ESTOQUE"): SEM_ESTOQUE}`; `len(resultado["preteridos"]) == len(resultado["preteridos_motivo"])`.
    - Teste 4 (`test_pedidos_motor.py::test_processar_pedidos_sem_canal_grava_preteridos_motivo_fallback_defensivo`): reusar o cenário de `test_processar_pedidos_sem_canal_vai_para_stand_by_sem_consumir_franquia` (nr_pedido 20 e 30, canal `None`/`"CANAL_NOVO"` respectivamente, produto `"X"`, ambos vão para `preteridos` no ramo `pares_sem_canal`). Chamar `processar_pedidos(...)`. Além das asserções já existentes naquele padrão, assertar `resultado["preteridos_motivo"][(20, "X")] == SEM_ESTOQUE` e `resultado["preteridos_motivo"][(30, "X")] == SEM_ESTOQUE` (fallback defensivo do achado A1 — documentar no corpo do teste, via comentário, que este ramo é código morto no caminho real de `build_processing_plan`, mas a invariante `len(preteridos) == len(preteridos_motivo)` precisa valer sempre); `len(resultado["preteridos"]) == len(resultado["preteridos_motivo"]) == 2`.
    - RED antes da implementação: rodar os quatro testes filtrados (`-k preteridos_motivo`) contra o código ainda sem a chave `preteridos_motivo` — devem falhar com `KeyError: 'preteridos_motivo'`, confirmando que o campo ainda não existe.
  </behavior>
  <action>
Escrever os quatro testes do `<behavior>` acima nos dois arquivos indicados (dois em `test_pedidos_motor_furo_grade.py`, na seção "testes de integração — processar_pedidos" já existente, logo após `test_processar_pedidos_furo_sensivel_a_ordem_prova_checagem_dentro_do_laco"; dois em `test_pedidos_motor.py`, cada um logo após seu cenário de referência citado no `<read_first>`). Adicionar os imports necessários: em `test_pedidos_motor_furo_grade.py`, `from app.modules.pedidos.domain.politica_quantidade import ModoAdequacao` e `from app.modules.pedidos.domain.standby_motivo import FURO_GRADE, SEM_ESTOQUE`; em `test_pedidos_motor.py`, adicionar `SEM_ESTOQUE` ao import já existente de `app.modules.pedidos.domain.standby_motivo` (criado na Task 1) — reaproveitar, não duplicar linha de import. Confirmar RED rodando `pytest -k preteridos_motivo -q` nos dois arquivos.

Implementar as 7 edições em `app/modules/pedidos/domain/motor_adequacao.py`, exatamente como fechado em `16-RESEARCH.md` § "Mudanças exatas em motor_adequacao.py":
(1) adicionar `from app.modules.pedidos.domain.standby_motivo import FURO_GRADE, SEM_ESTOQUE` ao bloco de imports do topo, logo após o import de `tem_furo_de_grade`;
(2) em `_commitar_par`, inserir o parâmetro `preteridos_motivo: dict[tuple[int, str], str]` na assinatura, posicionado logo após `preteridos: list` e antes de `resultados: dict`; no ramo `else` do corpo (quando `not gerou_or`), logo após `preteridos.append(par)`, adicionar `preteridos_motivo[par] = SEM_ESTOQUE`;
(3) atualizar os dois call sites de `_commitar_par` (dentro do ramo SEM_ADEQUAR e dentro do ramo ADEQUAR) passando `preteridos_motivo` na mesma posição posicional (logo após o argumento `preteridos`, antes de `resultados`);
(4) em `_processar_pedidos_canal`, declarar `preteridos_motivo: dict[tuple[int, str], str] = {}` imediatamente ao lado de `preteridos: list[tuple[int, str]] = []`;
(5) nos dois call sites de `tem_furo_de_grade` (ramo SEM_ADEQUAR e ramo ADEQUAR, dentro do laço de `produtos_elegiveis`), logo após `preteridos.append(par)`, adicionar `preteridos_motivo[par] = FURO_GRADE`;
(6) no early-return de `_processar_pedidos_canal` (quando `estrutura` está vazia) e no dict de retorno final, adicionar a chave `"preteridos_motivo": preteridos_motivo,` (early-return: dict vazio `{}`; retorno final: a variável populada) — posicionar logo após a chave `"preteridos"` em ambos os dicts, mantendo a ordem de leitura já usada pelas outras chaves;
(7) em `processar_pedidos`: no dict `merged`, adicionar `"preteridos_motivo": {}` logo após `"preteridos": []`; no laço `pares_sem_canal`, logo após `merged["preteridos"].append(par)`, adicionar `merged["preteridos_motivo"][par] = SEM_ESTOQUE` (comentário inline citando o achado A1: ramo defensivo, código morto no caminho real, mantém a invariante universal); após a chamada a `_processar_pedidos_canal` dentro do laço de canais, adicionar `merged["preteridos_motivo"].update(parcial["preteridos_motivo"])` logo após a linha que já faz `merged["pares_processados"] |= parcial["pares_processados"]`.

Atualizar a docstring "Retorna:" de `processar_pedidos` (linhas ~495-501) acrescentando uma linha descrevendo `preteridos_motivo` como `{ (nr_pedido, cd_prod_cor): motivo }` com o motivo canônico curto (`FURO_GRADE` ou `SEM_ESTOQUE`) de cada par em `preteridos`, campo aditivo com sempre as mesmas chaves de `preteridos` (mesmo tamanho).

Rodar os quatro testes filtrados para confirmar GREEN, depois a suíte completa para confirmar zero regressão em relação à linha de base da Task 1.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor_furo_grade.py app/tests/test_pedidos_motor.py -k preteridos_motivo -q</automated>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Os quatro testes novos falharam antes da mudança (RED: `KeyError: 'preteridos_motivo'`) e passam depois (GREEN).
    - `grep -n "preteridos_motivo\[par\] = FURO_GRADE" app/modules/pedidos/domain/motor_adequacao.py` mostra exatamente 2 ocorrências (uma por modo, ambas dentro dos call sites de `tem_furo_de_grade`).
    - `grep -n "preteridos_motivo\[par\] = SEM_ESTOQUE" app/modules/pedidos/domain/motor_adequacao.py` mostra exatamente 2 ocorrências (uma dentro de `_commitar_par`, uma dentro do ramo `pares_sem_canal`).
    - `grep -n "def _commitar_par" app/modules/pedidos/domain/motor_adequacao.py` mostra a assinatura com o parâmetro `preteridos_motivo` presente.
    - `grep -c "\"preteridos_motivo\":" app/modules/pedidos/domain/motor_adequacao.py` retorna pelo menos `4` (merged init, early-return de `_processar_pedidos_canal`, retorno final de `_processar_pedidos_canal`).
    - Cenário obrigatório (K1 de `16-VALIDATION.md`): no mesmo teste, um par furo de grade e um par sem estoque genérico gravam motivos DIFERENTES, nos dois modos (Testes 1 e 2 do `<behavior>`).
    - Orçamento de corte excedido (modo ADEQUAR) grava `SEM_ESTOQUE`, não um motivo à parte (Teste 1).
    - Nenhuma das 5 chaves antigas de retorno de `processar_pedidos` mudou de forma; `preteridos_motivo` é a 6ª chave, puramente aditiva (Teste 3) — `set(resultado.keys())` tem exatamente 6 elementos.
    - Ramo defensivo de canal não identificado também grava `preteridos_motivo` (Teste 4), mantendo `len(preteridos) == len(preteridos_motivo)` mesmo nesse caminho morto.
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) termina com a MESMA falha conhecida (teardown asyncpg) e nenhuma outra, com a contagem de passed da linha de base da Task 1 + 4 (os quatro testes novos desta task).
  </acceptance_criteria>
  <done>preteridos_motivo existe, distingue furo de grade de falta de estoque genérica (incluindo orçamento de corte excedido) nos dois modos, é 100% aditivo às 5 chaves antigas, cobre os 4 pontos reais de escrita em preteridos (incluindo o ramo defensivo de canal não identificado), e a suíte completa permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este plano é domínio puro (`app/modules/pedidos/domain/`): sem I/O, sem rede, sem entrada de usuário direta, sem autenticação/autorização envolvida. `itens`/`estoque` chegam como estruturas Python já montadas por quem chama (hoje, testes; em produção, `build_processing_plan`, fora deste plano). Não há fronteira de confiança HTTP/rede cruzada aqui — apenas a fronteira interna "o que o motor calcula" -> "o que sobrevive até `processing/`", que é exatamente o gap que este plano fecha.

| Boundary | Description |
|----------|-------------|
| `processar_pedidos`/`_processar_pedidos_canal` -> chamador (hoje só testes; em produção, `build_processing_plan` no plano 16-03) | O RESULTADO da classificação de motivo (furo vs estoque) tem efeito indireto em decisões futuras de UI/priorização (Phase 17), mas nenhum efeito financeiro/logístico direto é criado por ESTE plano — ele só anota um motivo a mais sobre uma decisão de stand-by que o motor já tomava antes |

## STRIDE Threat Register

Análise honesta: Spoofing e Elevation of Privilege não se aplicam — não há identidade nem autorização neste domínio puro. Repudiation e Denial of Service também não se aplicam de forma significativa (nenhuma chamada de rede, nenhum log novo com dado sensível, função continua O(1) por par, sem loop novo custoso). O risco real e único desta mudança é de **integridade do dado classificado** (Tampering) — gravar o motivo errado, ou deixar um par sem motivo.

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | HIGH | Colapsar furo de grade e falta de estoque genérica no mesmo valor (`SEM_ESTOQUE` para os dois), tornando a distinção inútil para a Phase 17 (STANDBY-04) | mitigate | Task 2, Testes 1 e 2: cenário único com um par de cada tipo, nos dois modos, asserção explícita de que os dois valores gravados são DIFERENTES — não apenas que cada um bate isoladamente com o esperado (`16-VALIDATION.md`, "Caso obrigatório: o motivo do furo de grade não pode se perder") |
| T-16-01-02 | Tampering | MEDIUM | Par entra em `preteridos` mas fica sem entrada correspondente em `preteridos_motivo` (invariante quebrada silenciosamente), deixando um "buraco" que a Phase 16-02/17 não consegue explicar | mitigate | Os 4 pontos reais de escrita em `preteridos` (2 furo, 1 `_commitar_par` compartilhado, 1 ramo defensivo de canal) recebem a escrita simétrica em `preteridos_motivo` na mesma implementação; todos os 4 testes da Task 2 assertam `len(resultado["preteridos"]) == len(resultado["preteridos_motivo"])` explicitamente |
| T-16-01-03 | Tampering | LOW | Mudar a forma de uma das 5 chaves antigas de retorno ao adicionar a 6ª, quebrando consumidores existentes (hoje nenhum, mas `processing/domain.py` já lê `result["preteridos"]`/`result["bloqueados_credito"]` sem esperar a chave nova) | mitigate | Teste 3 da Task 2 assert `set(resultado.keys())` com exatamente 6 elementos (as 5 antigas + a nova), mais suíte completa verde ao final — nenhum teste existente (que só lê chaves específicas, nunca o dict inteiro) quebra |
| T-16-01-SC | Tampering (supply chain) | LOW | Instalação de pacote npm/pip | accept | Nenhum pacote novo instalado — `standby_motivo.py` é um módulo-folha só com constantes de string, sem nenhum import |

Nenhuma ameaça de severidade alta permanece sem mitigação testável.
</threat_model>

## Artifacts this phase produces

- `app/modules/pedidos/domain/standby_motivo.py` (novo) — vocabulário canônico curto: `SEM_CREDITO`, `SEM_ESTOQUE`, `FURO_GRADE`, `MOTIVOS_VALIDOS`; módulo-folha, zero imports.
- `app/modules/pedidos/domain/motor_adequacao.py` (editado) — `preteridos_motivo: dict[tuple[int, str], str]` como 6ª chave aditiva do retorno de `processar_pedidos`/`_processar_pedidos_canal`; `_commitar_par` ganha o parâmetro `preteridos_motivo`; os 2 call sites de `tem_furo_de_grade` gravam `FURO_GRADE`; `_commitar_par` grava `SEM_ESTOQUE` (cobre estoque insuficiente simples e orçamento de corte excedido, mesmo bucket); o ramo defensivo `pares_sem_canal` grava `SEM_ESTOQUE` como fallback.
- `app/tests/test_pedidos_motor.py` (editado) — `test_standby_motivo_valores_canonicos_e_distintos` (Task 1); `test_processar_pedidos_preteridos_motivo_e_campo_aditivo_chaves_existentes_preservadas` e `test_processar_pedidos_sem_canal_grava_preteridos_motivo_fallback_defensivo` (Task 2).
- `app/tests/test_pedidos_motor_furo_grade.py` (editado) — `test_processar_pedidos_preteridos_motivo_distingue_furo_de_sem_estoque_modo_adequar` e `test_processar_pedidos_preteridos_motivo_distingue_furo_de_sem_estoque_modo_sem_adequar`.
- Nota para o SUMMARY (não é arquivo, é descoberta a registrar): `app/tests/invariantes_motor_adequacao.py::_CHAVES_CONTRATO_RESULTADO` (I9) ficará desatualizado (5 chaves fechadas, deveriam ser 6) a partir deste plano — fora do escopo de arquivos autorizados aqui; atualizar quando algum plano futuro (Phase 19, testes de propriedade, ou antes se necessário) ligar `assert_contrato_chaves_resultado` contra saída real de `processar_pedidos`.

<verification>
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -k standby_motivo -q` — verde, 1 teste (Task 1).
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor_furo_grade.py app/tests/test_pedidos_motor.py -k preteridos_motivo -q` — verde, 4 testes (Task 2).
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) — verde ao final de cada task, com exatamente a mesma falha conhecida (teardown asyncpg) e nenhuma outra; contagem de passed = linha de base confirmada no início da Task 1 + 5 (1 da Task 1 + 4 da Task 2).
- `grep -n "preteridos_motivo\[par\] = FURO_GRADE" app/modules/pedidos/domain/motor_adequacao.py` — exatamente 2 ocorrências.
- `grep -n "preteridos_motivo\[par\] = SEM_ESTOQUE" app/modules/pedidos/domain/motor_adequacao.py` — exatamente 2 ocorrências.
</verification>

<success_criteria>
- `standby_motivo.py` existe com `FURO_GRADE`/`SEM_ESTOQUE`/`SEM_CREDITO`/`MOTIVOS_VALIDOS`, módulo-folha, testado.
- `preteridos_motivo` existe como 6ª chave aditiva do retorno de `processar_pedidos`; as 5 chaves antigas não mudaram de forma.
- Furo de grade (`FURO_GRADE`) e falta de estoque genérica, incluindo orçamento de corte excedido (`SEM_ESTOQUE`), gravam valores DIFERENTES entre si, provado nos dois modos (ADEQUAR e SEM_ADEQUAR) no mesmo teste.
- Os 4 pontos reais de escrita em `preteridos` (2 furo, 1 `_commitar_par`, 1 ramo defensivo de canal) têm a escrita simétrica em `preteridos_motivo`; `len(preteridos) == len(preteridos_motivo)` provado em todos os cenários testados.
- `processing/` não foi tocado — este plano é escopo exclusivo do motor (`domain/`).
- Suíte completa 100% verde ao final, com exatamente a mesma falha conhecida pré-existente (teardown asyncpg) e nenhuma outra.
</success_criteria>

<output>
Create `.planning/phases/16-persist-ncia-do-motivo-de-stand-by/16-01-SUMMARY.md` when done
</output>
