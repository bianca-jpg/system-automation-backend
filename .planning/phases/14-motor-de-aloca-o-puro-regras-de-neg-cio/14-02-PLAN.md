---
phase: 14-motor-de-aloca-o-puro-regras-de-neg-cio
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/tests/factories_pedidos_motor.py
  - app/tests/test_factories_pedidos_motor.py
  - app/tests/cenarios_motor_adequacao.py
  - app/tests/test_cenario_disputa_mantenedora.py
  - app/tests/invariantes_motor_adequacao.py
  - app/tests/test_invariantes_motor_adequacao.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Um executor dos planos 14-03 a 14-07 monta um item de pedido válido para o motor chamando uma única função (item_pedido), sem montar o dict à mão"
    - "Um executor dos planos 14-03 a 14-07 monta o estoque por canal no formato exigido por processar_pedidos (estoque_produto + estoque_por_canal), sem formatar a chave de estoque manualmente"
    - "O cenário numérico ditado pela mantenedora (cliente A atendido, cliente B em stand by, estoque=20) está codificado como fixture importável e comprovadamente correto contra o motor ATUAL (modo adequação)"
    - "Cada uma das invariantes I1-I9 do 14-VALIDATION.md tem uma função de asserção correspondente, cada uma comprovada com pelo menos um caso que passa e um caso que falha (pytest.raises)"
    - "Nenhum arquivo de produção foi tocado nesta plano — só app/tests/ novo, e test_pedidos_motor.py não foi editado"
  artifacts:
    - path: "app/tests/factories_pedidos_motor.py"
      provides: "Factories compartilhadas de item de pedido e estoque por canal"
      exports: ["item_pedido", "estoque_produto", "estoque_por_canal"]
      min_lines: 25
    - path: "app/tests/test_factories_pedidos_motor.py"
      provides: "Teste próprio das factories (defaults + overrides + validação de canal)"
      min_lines: 15
    - path: "app/tests/cenarios_motor_adequacao.py"
      provides: "Fixture do cenário nomeado 'disputa da mantenedora'"
      exports: ["cenario_disputa_mantenedora", "NR_PEDIDO_CLIENTE_A", "NR_PEDIDO_CLIENTE_B", "PRODUTO_DISPUTADO"]
      min_lines: 30
    - path: "app/tests/test_cenario_disputa_mantenedora.py"
      provides: "Prova de que a fixture produz A atendido / B stand by contra o motor atual"
      min_lines: 10
    - path: "app/tests/invariantes_motor_adequacao.py"
      provides: "Helpers de asserção para as invariantes I1-I9 + oráculo de furo de grade (I4), reutilizáveis pela Phase 19"
      exports:
        - "assert_estoque_nunca_excedido"
        - "assert_sem_credito_nunca_alocado"
        - "assert_tudo_ou_nada_preserva_quantidade"
        - "calcular_furo_de_grade"
        - "assert_furo_de_grade_nao_selecionado"
        - "assert_orcamento_nao_excedido"
        - "assert_execucoes_identicas"
        - "assert_rateio_sem_drift"
        - "assert_minimo_viavel_antes_de_extra"
        - "assert_contrato_chaves_resultado"
      min_lines: 70
    - path: "app/tests/test_invariantes_motor_adequacao.py"
      provides: "Self-teste de cada helper de invariante (caso que passa + caso que falha)"
      min_lines: 80
  key_links:
    - from: "app/tests/cenarios_motor_adequacao.py"
      to: "app/tests/factories_pedidos_motor.py"
      via: "import direto das factories"
      pattern: "from app\\.tests\\.factories_pedidos_motor import"
    - from: "app/tests/test_cenario_disputa_mantenedora.py"
      to: "app.modules.pedidos.service.processar_pedidos"
      via: "import direto do shim (mesmo padrão de test_pedidos_motor.py)"
      pattern: "from app\\.modules\\.pedidos\\.service import"
    - from: "app/tests/invariantes_motor_adequacao.py"
      to: "app/modules/pedidos/domain/value_objects.py e domain/motor_adequacao.py"
      via: "import direto de domain (não passa pelo shim service.py, para não herdar as dependências de I/O do módulo)"
      pattern: "from app\\.modules\\.pedidos\\.domain\\."
---

<objective>
Criar, em `app/tests/`, a infraestrutura de prova que os planos 14-03 a 14-07 (e os testes de propriedade da Phase 19) vão reutilizar: uma factory de item de pedido, uma factory de estoque por canal, a fixture do cenário numérico ditado pela mantenedora (disputa entre cliente A e cliente B), e os helpers de asserção que codificam as invariantes I1-I9 do domínio de alocação.

Este plano **não** implementa nenhuma regra de negócio nova do motor — é Wave 0, paralela ao plano `14-01` (que promove o rateio Hamilton). Nenhum arquivo de produção é tocado; `test_pedidos_motor.py` (que o plano `14-07` reescreve) também não é tocado.

Purpose: dar aos planos seguintes (que editam `motor_adequacao.py` em sequência estrita, ondas 1-4) dados de teste prontos e critérios de invariante já codificados, para que cada um só precise ESCREVER a regra e CHAMAR os helpers — não reinventar fixtures a cada plano.
Output: `app/tests/factories_pedidos_motor.py`, `app/tests/cenarios_motor_adequacao.py`, `app/tests/invariantes_motor_adequacao.py` (módulos de suporte, não coletados como teste pelo pytest por não começarem com `test_`) + três arquivos `test_*.py` que provam que cada um funciona.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-PATTERNS.md
@app/modules/pedidos/domain/value_objects.py
@app/tests/test_pedidos_motor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Factory de item de pedido e de estoque por canal</name>
  <files>app/tests/factories_pedidos_motor.py, app/tests/test_factories_pedidos_motor.py</files>
  <read_first>
    - app/tests/test_pedidos_motor.py, linhas 15-25: a factory `_item(sg_tamanho, qt_liquida, vl_liquido, **extra)` já existente e PRIVADA daquele arquivo — o molde de forma a replicar (função solta com `**extra` sobrescrevendo um dict base), mas NÃO editar aquele arquivo nem importar dele; a nova factory é a versão pública e compartilhada para os planos 14-03 a 14-07.
    - app/modules/pedidos/domain/value_objects.py, linhas 1-3 e 36-38: `CANAIS = ("Franquia", "Multimarca")` e `montar_chave_estoque(cd_prod_cor, sg_tamanho) -> str` — a chave composta a reusar, nunca reformatar manualmente com f-string solta.
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md, linhas 96-102 ("Wave 0 Requirements"): a lista exata de 7 campos que a factory de item precisa cobrir e o formato exato `{canal: {f"{cd}_{tam}": qt}}` da factory de estoque.
  </read_first>
  <action>
Criar `app/tests/factories_pedidos_motor.py` com uma docstring de módulo de 2-3 linhas explicando que é a factory COMPARTILHADA do motor de adequação, usada pelos planos 14-03 a 14-07 desta fase (distinta da `_item` privada em `test_pedidos_motor.py`, que continua existindo até o plano 14-07 a substituir).

Definir `item_pedido(sg_tamanho: str, qt_liquida: int, vl_liquido: float, *, nr_pedido: int = 1, cd_prod_cor: str = "PROD1", canal: str = "Franquia", status_credito: str = "Com Credito", **extra) -> dict`. Monta um dict base com exatamente as sete chaves `nr_pedido`, `cd_prod_cor`, `sg_tamanho`, `qt_liquida`, `vl_liquido`, `status_credito`, `canal` a partir dos parâmetros nomeados, depois aplica `base.update(extra)` (o mesmo padrão de override pontual de `_item`) e retorna o dict. `sg_tamanho`, `qt_liquida` e `vl_liquido` são posicionais e obrigatórios (sem default); os demais têm default e podem ser sobrescritos por nome ou via `**extra`.

Definir `estoque_produto(cd_prod_cor: str, quantidades_por_tamanho: dict[str, int]) -> dict[str, int]`. Para cada par `(tamanho, quantidade)` de `quantidades_por_tamanho`, monta a chave com `montar_chave_estoque(cd_prod_cor, tamanho)` (importado de `app.modules.pedidos.domain.value_objects`) e retorna o dict achatado `{chave: quantidade}` — nunca concatenar `f"{cd_prod_cor}_{tamanho}"` manualmente.

Definir `estoque_por_canal(**por_canal: dict[str, int]) -> dict[str, dict[str, int]]`. Cada kwarg é o nome de um canal (ex.: `Franquia=...`, `Multimarca=...`) mapeado para um dict já achatado (tipicamente construído com `estoque_produto`, e mesclado pelo chamador com `|` quando há mais de um produto no mesmo canal). Para cada nome de canal recebido, validar contra `CANAIS` (importado do mesmo `value_objects.py`) e, se o nome não estiver em `CANAIS`, levantar `ValueError` com mensagem descritiva citando o nome inválido e os canais aceitos — seguindo o precedente local de `edicao_grade.py` de usar `ValueError` cru para validação de entrada (não criar hierarquia de exceção nova). Se todos os nomes forem válidos, retornar o dict `{canal: quantidades}` recebido, sem transformação adicional.

Terminar o arquivo com `__all__ = ["item_pedido", "estoque_produto", "estoque_por_canal"]`, replicando o padrão de `__all__` explícito já usado em `edicao_grade.py`/no `rateio.py` do plano 14-01.

Criar `app/tests/test_factories_pedidos_motor.py` com docstring de módulo de 1-2 linhas (self-teste das factories, sem I/O). Três testes, nomeados no padrão `test_{funcao}_{comportamento}` do repositório: um que chama `item_pedido("M", 10, 500.0)` sem overrides e confere que os sete campos batem com os defaults documentados acima; um que chama `item_pedido` passando overrides nomeados (`nr_pedido`, `canal`, `status_credito`) e via `**extra` (um campo extra qualquer, ex. `ds_grupo="Camisas"`) e confere que os overrides prevalecem sem afetar `sg_tamanho`/`qt_liquida`/`vl_liquido`; um que chama `estoque_produto` com dois tamanhos e confere que as chaves do dict retornado batem exatamente com `montar_chave_estoque` chamado diretamente para cada tamanho, e que chamar `estoque_por_canal` com um nome de canal fora de `CANAIS` levanta `ValueError` (usar `pytest.raises(ValueError)`), enquanto um nome válido retorna o dict esperado sem alteração.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_factories_pedidos_motor.py -q</automated>
  </verify>
  <done>Os três testes de test_factories_pedidos_motor.py passam; item_pedido, estoque_produto e estoque_por_canal existem e são importáveis de app.tests.factories_pedidos_motor.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture do cenário nomeado — disputa da mantenedora</name>
  <files>app/tests/cenarios_motor_adequacao.py, app/tests/test_cenario_disputa_mantenedora.py</files>
  <read_first>
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md, seção "Specific Ideas" ("Cenário numérico ditado pela mantenedora"): os números exatos — cliente A quer 10 peças de um produto, dentro de um pedido global de 1.000 peças/R$ 50.000; cliente B quer 15 peças, dentro de um pedido de 50 peças/R$ 1.000; estoque do produto disputado = 20; resultado esperado A atendido, B em stand by.
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 382-400 (`_prioridade` dentro de `_processar_pedidos_canal`): a prioridade de um nr_pedido é a soma de vl_liquido de TODOS os itens daquele pedido cujo cd_prod_cor tenha algum estoque disponível — não só do item do produto disputado. É por isso que a fixture precisa de uma linha "de preenchimento" (outro produto, mesmo pedido) para materializar o total do pedido global ditado pela mantenedora, e não só a linha do produto disputado.
    - app/tests/test_pedidos_motor.py, linhas 169-210 (test_processar_pedidos_prioriza_maior_valor_e_isola_sem_credito): precedente direto de como chamar processar_pedidos e ler selecionados/preteridos/resultados no mesmo formato que este teste vai usar.
  </read_first>
  <action>
Criar `app/tests/cenarios_motor_adequacao.py`, importando `item_pedido`, `estoque_produto` e `estoque_por_canal` de `app.tests.factories_pedidos_motor` (task 1 deste mesmo plano). Docstring de módulo explicando que é o cenário obrigatório do 14-VALIDATION.md ("Disputa da mantenedora"), válido hoje contra o motor ATUAL no modo adequação, e que o plano 14-05 (quando o modo sem-adequação existir) deve reexecutar o mesmo cenário nesse segundo modo.

Definir as constantes de módulo NR_PEDIDO_CLIENTE_A = 101, NR_PEDIDO_CLIENTE_B = 202, PRODUTO_DISPUTADO = "PRODX_DISPUTADO", TAMANHO_DISPUTADO = "M", CANAL_CENARIO = "Franquia", ESTOQUE_DISPUTADO = 20.

Definir `cenario_disputa_mantenedora() -> tuple[list[dict], dict[str, dict[str, int]]]`. Monta quatro itens com item_pedido, todos com canal=CANAL_CENARIO e status_credito="Com Credito": (1) linha disputada do cliente A — cd_prod_cor=PRODUTO_DISPUTADO, sg_tamanho=TAMANHO_DISPUTADO, qt_liquida=10, vl_liquido=500.0, nr_pedido=NR_PEDIDO_CLIENTE_A; (2) linha de preenchimento do cliente A — outro cd_prod_cor (ex. "PRODX_FILLER_A"), qt_liquida=990, vl_liquido=49500.0, mesmo nr_pedido, de forma que a soma das duas linhas do cliente A totalize exatamente 1000 peças e R$ 50.000,00 (o pedido global ditado); (3) linha disputada do cliente B — mesmo produto e tamanho disputados, qt_liquida=15, vl_liquido=300.0, nr_pedido=NR_PEDIDO_CLIENTE_B; (4) linha de preenchimento do cliente B — outro cd_prod_cor (ex. "PRODX_FILLER_B"), qt_liquida=35, vl_liquido=700.0, mesmo nr_pedido, totalizando exatamente 50 peças e R$ 1.000,00 para o cliente B. Montar o estoque com estoque_produto/estoque_por_canal: o produto disputado recebe ESTOQUE_DISPUTADO (20) no tamanho TAMANHO_DISPUTADO; cada produto de preenchimento recebe estoque exatamente igual à quantidade pedida por seu respectivo cliente (990 e 35), para que nenhum deles compita pelo estoque disputado nem fique ele mesmo em stand-by (o que inflaria ou desinflaria artificialmente o valor do pedido usado na priorização). Retornar a tupla (dados, estoque) pronta para ser passada como os dois primeiros argumentos posicionais de processar_pedidos.

Terminar com __all__ listando a função e as seis constantes.

Criar `app/tests/test_cenario_disputa_mantenedora.py`, importando processar_pedidos de app.modules.pedidos.service (mesmo padrão de shim usado em test_pedidos_motor.py) e a fixture + constantes de app.tests.cenarios_motor_adequacao. Um teste test_cenario_disputa_mantenedora_cliente_a_atendido_cliente_b_stand_by: chama cenario_disputa_mantenedora(), roda processar_pedidos(dados, estoque, ja_processados=set(), criterio="valor", tolerancia=0.05), e confere que (NR_PEDIDO_CLIENTE_A, PRODUTO_DISPUTADO) está em resultado["selecionados"]; que (NR_PEDIDO_CLIENTE_B, PRODUTO_DISPUTADO) está em resultado["preteridos"] e NÃO em resultado["selecionados"]; que o item do tamanho TAMANHO_DISPUTADO no resultado do par de A tem status_item == "Gerar OR" e qt_liquida == 10; que o item correspondente do par de B tem status_item == "Pedido em Stand By". Docstring do teste registrando que esta prova vale para o motor pré-Phase-14 (modo adequação) e que o plano 14-05 deve adicionar a variante sem-adequação quando essa política existir.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_cenario_disputa_mantenedora.py -q</automated>
  </verify>
  <done>O teste do cenário passa: cliente A atendido (Gerar OR, qt_liquida=10) e cliente B em stand by, usando exatamente os números ditados pela mantenedora (1000pçs/R$50.000 vs 50pçs/R$1.000, estoque=20).</done>
</task>

<task type="auto">
  <name>Task 3: Helpers de asserção para as invariantes I1-I9</name>
  <files>app/tests/invariantes_motor_adequacao.py, app/tests/test_invariantes_motor_adequacao.py</files>
  <read_first>
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md, linhas 60-74 (tabela de invariantes I1-I9): a fonte de verdade de CADA regra a codificar — ler as 9 linhas com atenção, cada helper implementa exatamente uma.
    - app/modules/pedidos/domain/motor_adequacao.py, linhas 200-234 (docstring de processar_pedidos): as cinco chaves de contrato exatas (resultados, selecionados, preteridos, bloqueados_credito, pares_processados) que o helper de I9 confere, e a semântica de "par" (nr_pedido, cd_prod_cor) usada pelos helpers de I2/I4.
    - app/modules/pedidos/domain/value_objects.py, linhas 36-38 e 98-109 (montar_chave_estoque, get_tamanho_idx): reusar diretamente, não recriar a lógica de índice de tamanho nem a montagem de chave.
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md, seção "Furo de grade (ALOC-04)": os 4 casos de borda decididos (1 tamanho só, 2 adjacentes, PP+GG sem M, tamanho desconhecido idx 999) que o oráculo calcular_furo_de_grade precisa acertar.
  </read_first>
  <action>
Criar `app/tests/invariantes_motor_adequacao.py`, importando is_sem_credito de app.modules.pedidos.domain.motor_adequacao (direto do domínio, não do shim service.py, para este módulo de apoio não herdar a cadeia pesada de imports de infraestrutura que service.py carrega) e get_tamanho_idx, montar_chave_estoque de app.modules.pedidos.domain.value_objects, além de from decimal import Decimal. Docstring de módulo de 2-3 linhas: helpers de asserção para as invariantes I1-I9 de 14-VALIDATION.md, reutilizados pelos testes de propriedade da Phase 19 — cada função levanta AssertionError com mensagem prefixada "I{n} violada: " quando a invariante correspondente não vale, e não retorna nada quando vale.

Definir exatamente estas dez funções públicas (nove invariantes + um oráculo auxiliar para I4), cada uma com docstring de 2-4 linhas citando o ID da invariante e a regra de negócio por trás:

1. assert_estoque_nunca_excedido(estoque_inicial: dict[str, int], resultado: dict) -> None (I1). Soma, por chave de estoque (via montar_chave_estoque(item["cd_prod_cor"], item["sg_tamanho"])), o qt_liquida de todo item com status_item == "Gerar OR" em resultado["resultados"].values(); para cada chave com reserva > 0, confere que o total reservado não excede estoque_inicial.get(chave, 0).

2. assert_sem_credito_nunca_alocado(dados: list[dict], resultado: dict) -> None (I2). Calcula o conjunto de nr_pedido cujo status_credito (via is_sem_credito) indica ausência de crédito; confere que nenhum desses nr_pedido aparece em resultado["selecionados"] nem em resultado["pares_processados"], e que nenhum item de resultado["resultados"] cujo par pertença a esse conjunto tem status_item == "Gerar OR".

3. assert_tudo_ou_nada_preserva_quantidade(itens_par: list[dict]) -> None (I3). Se nenhum item de itens_par tem status_item == "Gerar OR", retorna sem verificar nada (par não selecionado, a invariante não se aplica). Caso contrário, confere que TODOS os itens do par têm qt_liquida igual ao valor pedido original (usar item.get("qt_solicitada", item["qt_liquida"]) como a quantidade pedida, para funcionar tanto em itens já processados quanto crus).

4. calcular_furo_de_grade(itens_pedido_produto: list[dict], estoque_local: dict[str, int], cd_prod_cor: str) -> tuple[bool, str | None] (oráculo de referência para I4, independente da futura implementação de produção do plano 14-04). Calcula o conjunto de índices de tamanho via get_tamanho_idx, DESCARTANDO qualquer índice igual a 999 (tamanho desconhecido nunca define extremo nem conta como interior). Se restarem menos de 2 índices distintos, retorna (False, None) (tamanho único, ou só tamanhos desconhecidos, nunca é furo). Caso contrário, toma o menor e o maior índice restantes; para cada tamanho PEDIDO cujo índice esteja ESTRITAMENTE entre esses dois extremos, confere estoque_local.get(montar_chave_estoque(cd_prod_cor, tamanho), 0); se algum desses tamanhos do meio tiver estoque reservável <= 0, retorna (True, "Furo de grade: tamanho {tamanho} sem estoque reservável"). Se nenhum tamanho do meio zerar, retorna (False, None). Importante: tamanhos que o cliente não pediu para aquele produto nunca entram no cálculo (nem como candidato a "meio", nem como extremo) — a lista de tamanhos vem exclusivamente de itens_pedido_produto.

5. assert_furo_de_grade_nao_selecionado(par: tuple[int, str], itens_pedido_produto: list[dict], estoque_no_momento: dict[str, int], resultado: dict) -> None (I4). Chama calcular_furo_de_grade(itens_pedido_produto, estoque_no_momento, par[1]); se o resultado indicar furo, confere que par NÃO está em resultado["selecionados"] (levantando AssertionError citando o motivo do furo, caso contrário).

6. assert_orcamento_nao_excedido(total_original: int, adicionado_execucoes: list[int], cortado_execucoes: list[int]) -> None (I5). Calcula limite = (total_original * 5) // 100 (floor inteiro, igual nos dois lados — nunca ceil, nunca float). Confere SEPARADAMENTE que sum(adicionado_execucoes) <= limite e que sum(cortado_execucoes) <= limite — os dois orçamentos não se compensam entre si, cada violação gera sua própria mensagem de erro.

7. assert_execucoes_identicas(resultado_1: dict, resultado_2: dict) -> None (I6). Confere igualdade estrita, campo a campo, de selecionados, preteridos, bloqueados_credito, pares_processados, e que resultado_1["resultados"] e resultado_2["resultados"] têm exatamente o mesmo conjunto de chaves com o mesmo conteúdo de itens por chave — rodar o mesmo cenário duas vezes tem que produzir o mesmo plano, inclusive em empate exato de prioridade.

8. assert_rateio_sem_drift(itens: list[dict], valor_total_esperado: Decimal) -> None (I7 / FIX-02). Soma vl_liquido de todos os itens convertendo CADA valor via Decimal(str(item["vl_liquido"])) (nunca Decimal(float) direto, para não herdar imprecisão binária do float) e confere que a soma bate EXATAMENTE com valor_total_esperado — sem tolerância de centavo.

9. assert_minimo_viavel_antes_de_extra(itens_por_produto: dict[str, list[dict]]) -> None (I8). Recebe os itens de UM pedido já agrupados por cd_prod_cor. Um produto é "zerado" quando NENHUM de seus itens tem status_item == "Gerar OR". Um produto "recebeu extra" quando algum de seus itens tem qt_liquida maior que a quantidade pedida original (item.get("qt_solicitada", item["qt_liquida"])). Confere que não existe simultaneamente pelo menos um produto zerado E pelo menos um produto com extra dentro do mesmo pedido — nenhum produto pode ficar zerado enquanto outro do mesmo pedido já recebeu peça extra.

10. assert_contrato_chaves_resultado(resultado: dict) -> None (I9). Confere que set(resultado.keys()) é EXATAMENTE {"resultados", "selecionados", "preteridos", "bloqueados_credito", "pares_processados"} — nem mais, nem menos.

Terminar o arquivo com __all__ listando as dez funções.

Criar `app/tests/test_invariantes_motor_adequacao.py`. Docstring de módulo explicando que é o self-teste dos helpers de invariante — sem hypothesis (a dependência só entra na Phase 19); aqui cada invariante vira um caso de exemplo que passa e um caso de exemplo que falha. Para CADA uma das 9 invariantes (I1-I9), escrever no mínimo dois testes seguindo o padrão test_{nome_da_funcao}_{cenario}: um caso onde a invariante VALE (a função retorna None sem levantar) e um caso onde ela é violada (a chamada dentro de pytest.raises(AssertionError) levanta). Para calcular_furo_de_grade, cobrir explicitamente os 4 casos de borda do 14-CONTEXT.md (tamanho único nunca é furo; dois tamanhos adjacentes nunca é furo; cliente pediu PP e GG sem pedir M, com M zerado, nunca é furo; tamanho desconhecido índice 999 nunca define extremo nem conta como interior) MAIS o caso positivo (tamanho do meio zerado é furo) — totalizando pelo menos 5 testes só para essa função, além do teste de assert_furo_de_grade_nao_selecionado. Reaproveitar item_pedido de app.tests.factories_pedidos_motor para montar os itens de exemplo em vez de dicts literais, quando isso não adicionar ruído ao teste.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_invariantes_motor_adequacao.py -q</automated>
  </verify>
  <done>Todos os testes de test_invariantes_motor_adequacao.py passam; o arquivo contém pelo menos 18 funções test_ (contagem via linha começando com "def test_"), cobrindo caso-que-passa e caso-que-falha para cada uma das 9 invariantes mais os 5 casos de borda do oráculo de furo de grade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Nenhuma nesta plano | Todo o código criado vive em `app/tests/`, roda em memória, sem I/O de rede/disco/banco, e não é importado por nenhum módulo de produção (`app/main.py`, `app/modules/*/routes.py`, `app/workers/*`). Não atravessa nenhuma fronteira de confiança: não recebe input de usuário, não serializa para fora do processo de teste, não é empacotado no artefato deployado. |

## STRIDE Threat Register

Esta plano cria apenas infraestrutura de teste pura (factories, fixtures e helpers de asserção em memória, sem I/O). ASVS L1 pressupõe superfície que recebe input externo, persiste dado ou expõe endpoint — nenhuma das três condições existe aqui. Não há pacote novo instalado (nenhuma linha de `pyproject.toml` muda), então o gate de legitimidade de pacotes (npm/pip/cargo) também não se aplica.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | (nenhuma categoria STRIDE aplicável) | app/tests/factories_pedidos_motor.py, app/tests/cenarios_motor_adequacao.py, app/tests/invariantes_motor_adequacao.py | accept | Código de teste puro (sem I/O, sem rede, sem persistência, sem input externo), nunca importado por código de produção, não compõe o artefato deployado. Registrado apenas para manter o formato exigido do plano — nenhuma ameaça real foi identificada e aceita por conveniência. |

</threat_model>

## Artifacts this phase produces

- item_pedido(sg_tamanho: str, qt_liquida: int, vl_liquido: float, *, nr_pedido: int = 1, cd_prod_cor: str = "PROD1", canal: str = "Franquia", status_credito: str = "Com Credito", **extra) -> dict — app/tests/factories_pedidos_motor.py
- estoque_produto(cd_prod_cor: str, quantidades_por_tamanho: dict[str, int]) -> dict[str, int] — app/tests/factories_pedidos_motor.py
- estoque_por_canal(**por_canal: dict[str, int]) -> dict[str, dict[str, int]] — app/tests/factories_pedidos_motor.py
- cenario_disputa_mantenedora() -> tuple[list[dict], dict[str, dict[str, int]]] (+ constantes NR_PEDIDO_CLIENTE_A, NR_PEDIDO_CLIENTE_B, PRODUTO_DISPUTADO, TAMANHO_DISPUTADO, CANAL_CENARIO, ESTOQUE_DISPUTADO) — app/tests/cenarios_motor_adequacao.py
- assert_estoque_nunca_excedido(estoque_inicial: dict[str, int], resultado: dict) -> None (I1) — app/tests/invariantes_motor_adequacao.py
- assert_sem_credito_nunca_alocado(dados: list[dict], resultado: dict) -> None (I2) — app/tests/invariantes_motor_adequacao.py
- assert_tudo_ou_nada_preserva_quantidade(itens_par: list[dict]) -> None (I3) — app/tests/invariantes_motor_adequacao.py
- calcular_furo_de_grade(itens_pedido_produto: list[dict], estoque_local: dict[str, int], cd_prod_cor: str) -> tuple[bool, str | None] (oráculo I4) — app/tests/invariantes_motor_adequacao.py
- assert_furo_de_grade_nao_selecionado(par: tuple[int, str], itens_pedido_produto: list[dict], estoque_no_momento: dict[str, int], resultado: dict) -> None (I4) — app/tests/invariantes_motor_adequacao.py
- assert_orcamento_nao_excedido(total_original: int, adicionado_execucoes: list[int], cortado_execucoes: list[int]) -> None (I5) — app/tests/invariantes_motor_adequacao.py
- assert_execucoes_identicas(resultado_1: dict, resultado_2: dict) -> None (I6) — app/tests/invariantes_motor_adequacao.py
- assert_rateio_sem_drift(itens: list[dict], valor_total_esperado: Decimal) -> None (I7) — app/tests/invariantes_motor_adequacao.py
- assert_minimo_viavel_antes_de_extra(itens_por_produto: dict[str, list[dict]]) -> None (I8) — app/tests/invariantes_motor_adequacao.py
- assert_contrato_chaves_resultado(resultado: dict) -> None (I9) — app/tests/invariantes_motor_adequacao.py

<verification>
Rodar, nesta ordem, dentro do container (todos devem passar):
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_factories_pedidos_motor.py app/tests/test_cenario_disputa_mantenedora.py app/tests/test_invariantes_motor_adequacao.py -q`
2. Suíte completa da fase, per 14-VALIDATION.md (obrigatório mesmo neste plano de infraestrutura, para confirmar que nada em app/tests/ colidiu com os arquivos existentes): `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q`
3. Confirmar que nenhum arquivo de produção mudou: `git status --short app/modules` deve retornar vazio (ou só mostrar o trabalho pré-existente de SSO não relacionado a este plano).
</verification>

<success_criteria>
- app/tests/factories_pedidos_motor.py, app/tests/cenarios_motor_adequacao.py e app/tests/invariantes_motor_adequacao.py existem, não começam com "test_" (não são coletados como teste) e são importáveis por outros arquivos de teste via app.tests.<modulo>.
- Os três arquivos test_*.py desta plano passam 100% (zero failures, zero skips) rodando isoladamente e dentro da suíte completa.
- Nenhum arquivo em app/modules/ ou app/tests/test_pedidos_motor.py foi alterado.
- Os planos 14-03 a 14-07 podem importar item_pedido, estoque_por_canal, cenario_disputa_mantenedora e qualquer helper de invariantes_motor_adequacao.py sem precisar recriar nenhum deles.
</success_criteria>

<output>
Create `.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-02-SUMMARY.md` when done
</output>
