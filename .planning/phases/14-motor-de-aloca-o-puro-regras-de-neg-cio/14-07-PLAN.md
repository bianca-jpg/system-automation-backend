---
phase: 14-motor-de-aloca-o-puro-regras-de-neg-cio
plan: 07
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - app/tests/test_pedidos_motor.py
  - app/tests/test_pedidos_motor_performance_024.py
  - app/tests/test_pedidos_processing_cancellation_024.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "A suíte completa (`pytest -q` via Docker) passa integralmente, incluindo os dois arquivos que fixam a assinatura de adequar_grade_produto via monkeypatch"
    - "Nenhum teste em test_pedidos_motor.py afirma mais a regra antiga de orçamento por produto isolado com ceil no corte; os testes de orçamento afirmam a regra nova: base = pedido completo, floor nos dois lados, dois orçamentos (adição/corte) que não se compensam entre si nem entre execuções"
    - "Os 11 cenários nomeados de 14-VALIDATION.md têm, cada um, pelo menos um teste automatizado que os exercita com os números daquele cenário"
    - "Nenhum arquivo de produção (fora de app/tests/) foi alterado por este plano; se um teste falhou por comportamento de produção divergente da regra documentada em 14-CONTEXT.md/14-VALIDATION.md, isso foi reportado como gap de um plano anterior, nunca mascarado ajustando a asserção"
    - "O helper de asserção de invariantes I1-I9 criado em 14-02 é reutilizado por pelo menos dois testes reescritos nesta fase, evidenciando que o domínio está pronto para os testes de propriedade da Phase 19 sem precisar adicionar hypothesis agora"
  artifacts:
    - path: "app/tests/test_pedidos_motor.py"
      provides: "Suíte de negócio do motor migrada para a semântica pós-14-06: testes de regra mudada reescritos conscientemente, testes mecânicos ajustados, 11 cenários nomeados cobertos"
    - path: "app/tests/test_pedidos_motor_performance_024.py"
      provides: "Fake de monkeypatch (_adequar_observando_subset) com assinatura espelhando exatamente adequar_grade_produto pós-14-06"
    - path: "app/tests/test_pedidos_processing_cancellation_024.py"
      provides: "Fake de monkeypatch (_slow_grade) com assinatura espelhando exatamente adequar_grade_produto pós-14-06, cancel_token preservado como keyword-only"
  key_links:
    - from: "app/tests/test_pedidos_motor_performance_024.py::_adequar_observando_subset"
      to: "app/modules/pedidos/domain/motor_adequacao.py::adequar_grade_produto"
      via: "monkeypatch.setattr(motor, \"adequar_grade_produto\", ...) com assinatura idêntica à real"
      pattern: "monkeypatch\\.setattr\\(motor, \"adequar_grade_produto\""
    - from: "app/tests/test_pedidos_processing_cancellation_024.py::_slow_grade"
      to: "app/modules/pedidos/domain/motor_adequacao.py::adequar_grade_produto"
      via: "monkeypatch.setattr(motor, \"adequar_grade_produto\", ...) com assinatura idêntica à real"
      pattern: "monkeypatch\\.setattr\\(motor, \"adequar_grade_produto\""
    - from: "app/tests/test_pedidos_motor.py"
      to: "app/modules/pedidos/service.py"
      via: "import direto dos símbolos re-exportados do shim de domínio"
      pattern: "from app\\.modules\\.pedidos\\.service import"
---

<objective>
Fechar a Phase 14 migrando `app/tests/test_pedidos_motor.py`, `app/tests/test_pedidos_motor_performance_024.py` e `app/tests/test_pedidos_processing_cancellation_024.py` para a semântica implementada pelos planos 14-01 a 14-06, e provar isso com a suíte completa do repositório verde.

Este plano **não implementa nenhuma regra de negócio nova** — ele é a prova (via teste) dos requirements ALOC-01 a ALOC-10 e FIX-02 já implementados. Ele **não toca nenhum arquivo de produção**: se um teste falhar por divergência de comportamento de produção (não de asserção desatualizada), a causa é um plano anterior (14-03 a 14-06) incompleto, e este plano deve reportar isso explicitamente, nunca "consertar" a asserção para bater com o comportamento errado.

Dois grupos de testes precisam de tratamento **diferente**, e este plano os trata separadamente:
- **Testes cuja regra mudou de propósito** (o orçamento ±5% deixou de ser por produto isolado com `ceil`/`floor` assimétricos e virou um ledger por pedido completo, `floor` nos dois lados, dois orçamentos que não se compensam) — são **reescritos conscientemente** como asserção da regra nova, nunca ajustados até passar.
- **Testes que quebram só por detalhe de implementação** (nova assinatura obrigatória, novo parâmetro de orçamento) — recebem **ajuste mecânico** de chamada, sem tocar a asserção de negócio.

Purpose: garantir que a reescrita do motor de adequação (14-01 a 14-06) tem prova automatizada completa, incluindo os dois arquivos que fixam a assinatura de `adequar_grade_produto` via `monkeypatch` e quebram silenciosamente fora do arquivo de negócio, e deixar o domínio "hypothesis-ready" (invariantes I1-I9 expressáveis como asserção reutilizável) para a Phase 19, sem adicionar a dependência `hypothesis` agora.

Output: `test_pedidos_motor.py` migrado (testes de regra mudada reescritos, cenários nomeados cobertos), os dois arquivos de assinatura fixa atualizados, suíte completa (`uv run pytest -q` via Docker) 100% verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-PATTERNS.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-02-SUMMARY.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-05-SUMMARY.md
@.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-06-SUMMARY.md

# Estes três são os únicos arquivos que este plano tem permissão de modificar.
@app/tests/test_pedidos_motor.py
@app/tests/test_pedidos_motor_performance_024.py
@app/tests/test_pedidos_processing_cancellation_024.py

# Estado real do motor pós-14-06 — a fonte de verdade da assinatura atual,
# não o RESEARCH.md (que descreve a intenção, não necessariamente o nome
# final escolhido pelo plano 14-06, que teve discretion sobre a estrutura
# interna do ledger).
@app/modules/pedidos/domain/motor_adequacao.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever conscientemente os testes cuja regra de orçamento mudou</name>
  <files>app/tests/test_pedidos_motor.py</files>
  <read_first>
    - app/tests/test_pedidos_motor.py (estado atual completo — procurar por NOME de função, não por número de linha: os planos 14-03 a 14-06 podem já ter inserido testes antes/depois dos trechos abaixo) — localizar especificamente `test_adequar_grade_produto_aumenta_extremos_dentro_do_orcamento`, `test_adequar_grade_produto_falta_alem_da_tolerancia_vira_stand_by`, `test_adequar_grade_produto_falta_dentro_da_tolerancia_reduz_parcial` e `test_processar_pedidos_prioriza_maior_valor_e_isola_sem_credito`
    - app/modules/pedidos/domain/motor_adequacao.py — assinatura ATUAL (pós-14-06) de `processar_pedidos` e `adequar_grade_produto`: quais parâmetros de orçamento/modo existem de fato, se `adequar_grade_produto` ainda é chamável isoladamente com um `tolerancia` simples ou se passou a exigir o ledger de pedido
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-06-SUMMARY.md — contrato final do ledger de orçamento (nome do objeto/parâmetros, como `total_original`/`consumido_previo` ou equivalente são passados, como "quanto foi consumido" fica observável a partir do retorno de `processar_pedidos`)
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-02-SUMMARY.md — nome real das factories de item/estoque e do helper de asserção das invariantes I1-I9
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md, seção `<decisions>` → "Orçamento ±5% (ALOC-07, ALOC-08, ALOC-09, ALOC-10)" — a regra travada, verbatim
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md — invariante I5 e os cenários nomeados "Orçamento atravessa produtos", "Não se compensam", "Double-spend"
  </read_first>
  <behavior>
    - Antes de qualquer edição, rodar `pytest app/tests/test_pedidos_motor.py -q` e registrar quais testes estão vermelhos. Os três testes de `adequar_grade_produto` isolado (orçamento por produto, `ceil`/`floor` assimétrico) são candidatos a estar vermelhos ou a já terem sido removidos/stubados por um plano anterior — qualquer um dos dois estados é aceitável como ponto de partida.
    - `test_adequar_grade_produto_falta_alem_da_tolerancia_vira_stand_by` é o teste que precisa **morrer conscientemente**: ele afirma que uma falta além de `ceil(total_grade_do_produto * tolerancia)` vai inteira para stand-by, sem NUNCA reduzir. A regra nova mede o corte sobre o **pedido inteiro**, com `floor` (não `ceil`), e permite reduzir parcialmente enquanto o orçamento de corte do pedido cobrir. Este teste, como está escrito hoje (chama `adequar_grade_produto` isolada, sem orçamento externo), testa uma regra que deixou de existir. Ele deve ser REMOVIDO do arquivo, não adaptado — substituído pelos testes novos de `processar_pedidos` abaixo, que provam a mesma invariante (I5, corte não excede o teto do pedido) na semântica correta.
    - `test_adequar_grade_produto_aumenta_extremos_dentro_do_orcamento` e `test_adequar_grade_produto_falta_dentro_da_tolerancia_reduz_parcial`: mesma causa raiz (orçamento por produto isolado). Remover ambos pelo mesmo motivo — a unidade de orçamento deixou de ser o produto, é o pedido.
    - Novo teste `test_processar_pedidos_orcamento_pedido_corte_atravessa_produtos_respeita_teto` (nome contém `orcamento_pedido`, alinhado ao Test Map do 14-RESEARCH.md): um único pedido (`nr_pedido=1`, canal "Franquia") com 3 produtos distintos, cada um com um único tamanho "M" (grade de tamanho único deliberadamente, para não disparar furo de grade e isolar a variável em teste — orçamento, não furo). Solicitar 34+33+33 = 100 peças no total do pedido (a base do orçamento é o pedido completo, não cada produto). Estoque disponível: 2 peças a menos que o solicitado em CADA um dos 3 produtos (falta de 2 em cada, 6 no total demandado). O teto de corte do pedido é `floor(100 * 0.05) == 5`. Construir os itens na MESMA ordem de entrada em que devem ser processados (produto 1, produto 2, produto 3) e assertar: o primeiro e o segundo produto (consumindo 2+2=4 do teto de 5) saem com `status_item == "Gerar OR"` e `qt_liquida` reduzida para bater com o estoque disponível; o terceiro produto, que precisaria de mais 2 peças de corte mas só resta 1 no orçamento, vai para `status_item == "Pedido em Stand By"` com `qt_liquida` PRESERVADA (não reduzida) — replicando a mecânica de preservação de `marcar_stand_by` já provada em `test_marcar_stand_by_preserva_valores_e_remove_campos_transitorios`. Se a ordem real de consumo do ledger entre produtos do mesmo pedido não for a ordem de entrada da lista (confirmar lendo `motor_adequacao.py`), ajustar os valores de estoque/solicitado do fixture para que o teste continue determinístico e sem ambiguidade sobre qual dos três produtos fica em stand-by — nunca deixar o teste dependente de uma ordem não garantida.
    - Novo teste `test_processar_pedidos_orcamento_pedido_corte_e_adicao_nao_se_compensam`: mesmo pedido, mas agora o cliente também tem um produto com sobra de estoque (extremo com estoque suficiente para adicionar) além de um produto que precisa cortar. Assertar que o orçamento de ADIÇÃO consumido por um produto não reduz o orçamento de CORTE disponível para outro produto do mesmo pedido, e vice-versa — os dois contadores são independentes (ALOC-08). Usar o helper de invariantes de 14-02 (se ele expuser uma asserção específica para I5) para verificar os dois lados separadamente.
    - Novo teste `test_processar_pedidos_orcamento_acumulado_entre_execucoes_nao_reseta` (nome contém `acumulado_execucoes`, alinhado ao Test Map): processar o MESMO pedido em duas chamadas de `processar_pedidos`, com o parâmetro de consumo acumulado da segunda chamada refletindo exatamente o que a primeira chamada já consumiu (ler em `14-06-SUMMARY.md` como esse parâmetro é nomeado e alimentado — é um dado de entrada do domínio, não algo que o domínio busca sozinho, por decisão explícita de fronteira em 14-CONTEXT.md). Assertar que a segunda chamada NÃO trata o pedido como se tivesse 5% novos disponíveis — o teto efetivo da segunda chamada é `floor(total*0.05) - já_consumido_na_primeira`, nunca `floor(total*0.05)` de novo.
    - Ajustar (não remover) `test_processar_pedidos_prioriza_maior_valor_e_isola_sem_credito`: as asserções de prioridade (`resultado["selecionados"] == [(1, "PROD1")]`) e de isolamento de crédito (`resultado["bloqueados_credito"] == [3]`) são regra que NÃO mudou — preservar exatamente. Só a asserção do item 2 (`resultado["resultados"][(2, "PROD1")][0]`) pode precisar de ajuste: hoje ela espera `status_item == "Pedido em Stand By"` vindo da falta de estoque sem orçamento explícito de fora. Confirmar contra o comportamento real pós-14-05 (modo precisa ser explicitado na chamada) e ajustar somente essa asserção pontual, documentando no comentário do teste por que ela mudou (referenciar ALOC-02/ALOC-03).
  </behavior>
  <action>
    Executar a sequência descrita no `<behavior>`: (1) rodar a suíte do arquivo isolada para ver o estado real; (2) remover os três testes de `adequar_grade_produto` isolado por orçamento (citar ALOC-07/ALOC-08 no comentário de remoção ou no commit, explicando que a unidade de orçamento mudou de produto para pedido); (3) adicionar os três testes novos de `processar_pedidos` descritos acima, usando os nomes exatos indicados (contêm as substrings `orcamento_pedido` e `acumulado_execucoes` do Test Map de `14-RESEARCH.md`) e as factories de item/estoque de `14-02` sempre que aplicável; (4) ajustar a asserção pontual do item 2 em `test_processar_pedidos_prioriza_maior_valor_e_isola_sem_credito`. Cada assertividade numérica nova precisa remontar a uma frase específica de `14-CONTEXT.md` § Decisions ou de uma linha da tabela de cenários nomeados de `14-VALIDATION.md` — se o comportamento real do código divergir do que está documentado nessas duas fontes, PARAR e não escrever o teste bate-a-bate com o output observado: reportar a divergência no SUMMARY.md como gap de um plano anterior (14-04/14-05/14-06), citando qual decisão da fase 14-CONTEXT.md o código não cumpre.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_adequar_grade_produto_aumenta_extremos_dentro_do_orcamento\|def test_adequar_grade_produto_falta_alem_da_tolerancia_vira_stand_by\|def test_adequar_grade_produto_falta_dentro_da_tolerancia_reduz_parcial" app/tests/test_pedidos_motor.py` retorna `0` (os três testes de orçamento por produto isolado saíram do arquivo).
    - `grep -c "^def test_processar_pedidos_orcamento_pedido_" app/tests/test_pedidos_motor.py` retorna pelo menos `2`.
    - `grep -c "^def test_processar_pedidos_orcamento_acumulado_execucoes" app/tests/test_pedidos_motor.py` retorna `1` (nome contém a substring `acumulado_execucoes` do Test Map).
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -q` termina com `0 failed`.
    - Nenhum teste novo escrito nesta task afirma um valor numérico que não remonte a uma frase específica de `14-CONTEXT.md` ou `14-VALIDATION.md` (checagem manual do próprio autor da task antes de fechar).
  </acceptance_criteria>
  <done>Os testes que exerciam a regra antiga de orçamento por produto isolado saíram do arquivo; os testes que provam a regra nova (ledger por pedido, floor nos dois lados, dois orçamentos independentes, acumulado entre execuções) existem e passam.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar os dois arquivos de assinatura fixa e cobrir os cenários nomeados restantes</name>
  <files>app/tests/test_pedidos_motor_performance_024.py, app/tests/test_pedidos_processing_cancellation_024.py, app/tests/test_pedidos_motor.py</files>
  <read_first>
    - app/tests/test_pedidos_motor_performance_024.py, linhas 118-133 (`_adequar_observando_subset`, assinatura atual `itens, estoque_local, cd_prod_cor, tolerancia=0.05`, chamada a `adequar_original(...)` na linha 131) e linha 75 (`monkeypatch.setattr(motor, "_indexar_estoque_por_produto", _indice_legado)` — este outro monkeypatch não deveria precisar de alteração, pois `_indexar_estoque_por_produto` não muda de assinatura nesta fase, per 14-RESEARCH.md § "Mapa cirúrgico")
    - app/tests/test_pedidos_processing_cancellation_024.py, linhas 53-74 (`_slow_grade`, assinatura atual `items, available_stock, product_code, tolerance=0.05, *, cancel_token=None`, chamada a `original_grade(...)` nas linhas 66-72, `monkeypatch.setattr(motor, "adequar_grade_produto", _slow_grade)` na linha 74)
    - app/modules/pedidos/domain/motor_adequacao.py — assinatura REAL e atual de `adequar_grade_produto` (procurar por nome, a fonte de verdade é o código, não o RESEARCH.md)
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-VALIDATION.md § "Cenários nomeados" (tabela completa de 11 linhas) e § "Por que a suíte completa é obrigatória a cada wave" (contexto de por que estes dois arquivos existem e por que quebram em silêncio)
    - app/tests/test_pedidos_motor.py — estado completo após a Task 1, e todos os nomes de função `test_*` já existentes (grep `^def test_` para listar de uma vez) — usar para cruzar contra a tabela de cenários nomeados antes de escrever qualquer teste novo, evitando duplicar cobertura já entregue por 14-03/14-04/14-05
  </read_first>
  <action>
Passo 1 — arquivos de assinatura fixa: rodar a suíte completa (não só estes dois arquivos) e observar se algum teste falha com `TypeError` relacionado a `adequar_grade_produto` (sintoma de assinatura desatualizada: `missing N required positional argument` ou `unexpected keyword argument`). Se falhar: em `test_pedidos_motor_performance_024.py`, reescrever a assinatura de `_adequar_observando_subset` para espelhar EXATAMENTE, parâmetro por parâmetro (nomes, ordem, valores default, keyword-only), a assinatura real de `adequar_grade_produto` lida no `motor_adequacao.py` atual — nunca adivinhar um nome de parâmetro; copiar literalmente. A chamada interna a `adequar_original(...)` deve repassar todos os parâmetros novos recebidos, preservando o comportamento de contagem (`tamanhos_dos_subsets.append(len(estoque_local))`) inalterado. Em `test_pedidos_processing_cancellation_024.py`, aplicar o mesmo tratamento a `_slow_grade`, preservando `cancel_token` como parâmetro keyword-only e a chamada a `original_grade(...)` repassando todos os parâmetros. Se a suíte completa já passar sem nenhum `TypeError` nesses dois arquivos (porque um plano anterior já sincronizou a assinatura), não fazer nenhuma edição neles — só confirmar e documentar isso no SUMMARY.md.

Passo 2 — cobertura dos cenários nomeados: listar todos os `test_*` de `test_pedidos_motor.py` (incluindo os adicionados por 14-03, 14-04, 14-05, 14-06 e pela Task 1 deste plano) e cruzar, um a um, contra as 11 linhas da tabela "Cenários nomeados" de `14-VALIDATION.md` (Disputa da mantenedora — nos dois modos, Furo no meio, Não é furo, Tamanho único, Tamanho desconhecido, Orçamento atravessa produtos, Não se compensam, Double-spend, Furo sensível à ordem, Empate de prioridade, Permutação de produtos). Para cada linha, ler o teste candidato (não confiar só na semelhança de nome) e confirmar que a asserção bate com a coluna "Esperado". "Orçamento atravessa produtos", "Não se compensam" e "Double-spend" já foram cobertos pela Task 1 deste plano — confirmar, não recriar. "Permutação de produtos" é o cenário com maior chance de estar sem teste (nenhum plano 14-01 a 14-06 o lista como requirement explícito): se de fato faltar, escrever `test_processar_pedidos_permutacao_de_produtos_produz_resultado_identico` — um único pedido (`nr_pedido=1`) com 2+ produtos distintos, chamando `processar_pedidos` duas vezes com a MESMA lista de itens em ordens diferentes (produto A antes de B, depois B antes de A) e assertando que `resultado["selecionados"]`, `resultado["preteridos"]` e o conteúdo de `resultado["resultados"]` para cada par são byte-a-byte idênticos entre as duas chamadas — mesmo padrão já usado pelo teste de desempate de `14-03` (independência de ordem de entrada), aplicado à ordem dos PRODUTOS em vez da ordem dos PEDIDOS. Para qualquer outra linha da tabela sem teste identificável, escrever o teste faltante usando os números exatos daquela linha da tabela e, quando a linha citar "Disputa da mantenedora", usar literalmente os números de `14-CONTEXT.md` § "Cenário numérico ditado pela mantenedora" (Cliente A: 10 peças, pedido de 1.000 pçs / R$ 50.000; Cliente B: 15 peças, pedido de 50 pçs / R$ 1.000; estoque = 20) rodando a chamada nos DOIS modos (adequar e sem_adequar), não só em um.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) termina com `0 failed`, incluindo `test_pedidos_motor_performance_024.py` e `test_pedidos_processing_cancellation_024.py`.
    - Nenhum `TypeError` relacionado a `adequar_grade_produto` aparece na saída do pytest para esses dois arquivos.
    - Para cada uma das 11 linhas de `14-VALIDATION.md` § "Cenários nomeados", existe pelo menos um teste em `app/tests/test_pedidos_motor.py` cuja asserção corresponde à coluna "Esperado" daquela linha — confirmado por leitura, não só por grep de substring.
    - Se "Permutação de produtos" precisou de teste novo: `grep -c "^def test_processar_pedidos_permutacao_de_produtos" app/tests/test_pedidos_motor.py` retorna `1`.
  </acceptance_criteria>
  <done>Os dois arquivos de assinatura fixa espelham a assinatura real e atual de adequar_grade_produto; todos os 11 cenários nomeados da fase têm cobertura de teste automatizada e verificada por leitura, não só por nome.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar a fase — suíte completa verde, sem tocar produção, domínio hypothesis-ready</name>
  <files>app/tests/test_pedidos_motor.py, app/tests/test_pedidos_motor_performance_024.py, app/tests/test_pedidos_processing_cancellation_024.py</files>
  <read_first>
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-02-SUMMARY.md — nome exato do helper de asserção das invariantes I1-I9 (Wave 0 Requirements de `14-VALIDATION.md`: "Helper de asserção para as invariantes I1-I9, reutilizável pelos testes de propriedade da Phase 19")
    - .planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-CONTEXT.md, seção "Working tree compartilhado" — trabalho não commitado de SSO em `app/modules/auth/`, `alembic/versions/031_*`, `microsoft_sso.py`, que este plano NUNCA deve incluir em `git add`
    - pyproject.toml — seção de dependências (confirmar que `hypothesis` não está listado)
  </read_first>
  <action>
Passo 1 — confirmar que nenhum arquivo de produção foi tocado: rodar `git status --short -- app/modules app/shared alembic` (ou equivalente restrito a diretórios de produção) e confirmar saída vazia. Se algum arquivo de produção aparecer modificado por este plano, REVERTER essa alteração específica (não commitar produção a partir deste plano) e registrar no SUMMARY.md, com precisão, qual teste falhou e por que a causa raiz aponta para um plano anterior (14-03/14-04/14-05/14-06) incompleto — este plano não corrige produção, só revela e reporta o gap.

Passo 2 — suíte completa definitiva: rodar a suíte completa por Docker e confirmar 100% verde. Contar o total de testes executados e comparar com a expectativa (20 testes originais - 3 removidos na Task 1 + no mínimo 3 novos de orçamento (Task 1) + os cenários nomeados adicionados na Task 2, mais os testes já adicionados por 14-03/14-04/14-05/14-06) — o número deve ser maior que o total original de 20, nunca menor, salvo os 3 removidos conscientemente na Task 1 (documentar essa contagem no SUMMARY.md).

Passo 3 — confirmar reuso do helper de invariantes: usando o nome real do helper (lido no `read_first`), rodar `grep -c "<nome_do_helper>" app/tests/test_pedidos_motor.py` e confirmar uso em pelo menos 2 dos testes reescritos/adicionados nas Tasks 1 e 2 (preferencialmente nos testes de orçamento, onde I5 e I8 são diretamente verificáveis). Se a Task 1/2 não usou o helper em nenhum teste, voltar e adicionar a chamada a ele em pelo menos dois testes antes de fechar esta task — a prova de "hypothesis-ready" é justamente que a MESMA função de asserção usada pelos testes unitários hoje é a que os testes de propriedade vão reusar na Phase 19.

Passo 4 — confirmar que `hypothesis` não foi adicionado: `grep -c "hypothesis" pyproject.toml` deve retornar `0`; `grep -rn "^import hypothesis\|^from hypothesis" app/tests/` deve retornar vazio.

Passo 5 — commit final: `git add` citando explicitamente só os três arquivos de teste modificados por este plano (nunca `git add -A` nem `git add .`, dado o working tree compartilhado com o trabalho de SSO), mensagem de commit no padrão Conventional Commits (escopo `pedidos` ou `tests`), sem `Co-Authored-By` nem qualquer menção a Claude no corpo do commit.
  </action>
  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q</automated>
    <automated>git status --short -- app/modules app/shared alembic</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` termina com `0 failed`.
    - `git status --short -- app/modules app/shared alembic` retorna vazio (nenhum arquivo de produção alterado por este plano).
    - `grep -c "hypothesis" pyproject.toml` retorna `0`.
    - `grep -rn "^import hypothesis\|^from hypothesis" app/tests/` não retorna nenhuma linha.
    - O helper de asserção de invariantes I1-I9 (nome confirmado em `14-02-SUMMARY.md`) é usado em pelo menos 2 testes de `app/tests/test_pedidos_motor.py`.
    - Commit criado com `git add` restrito aos 3 arquivos de teste deste plano; `git log -1 --format=%B` não contém "Claude" nem "Co-Authored-By".
  </acceptance_criteria>
  <done>Suíte completa verde, nenhum arquivo de produção tocado, domínio comprovadamente pronto para os testes de propriedade da Phase 19, commit fechado sem coautoria e sem git add amplo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suíte de teste ↔ regra de negócio documentada em `14-CONTEXT.md`/`14-VALIDATION.md` | O único "atacante" plausível aqui é o próprio processo de migração: a tentação de ajustar uma asserção até bater com o output do código, em vez de derivar a asserção da regra documentada. Isso não é uma ameaça externa, é um risco de integridade da verificação. |
| Working tree compartilhado (este plano ↔ trabalho de SSO Microsoft não commitado em `app/modules/auth/`, `alembic/versions/031_*`) | `git add` amplo (`-A`/`.`) neste working tree commitaria trabalho alheio incompleto junto com este plano. |

## STRIDE Threat Register

Este plano só escreve/edita testes — não há superfície de rede, autenticação nem dado de usuário final envolvido. A análise honesta é que Spoofing, Elevation of Privilege e Denial of Service não se aplicam a arquivos de teste síncronos e sem I/O. As ameaças relevantes aqui são de **integridade da verificação** (falso verde) e de **processo** (commit indevido em working tree compartilhado).

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Tampering (falso verde por asserção adaptada ao output, não à regra) | HIGH | `app/tests/test_pedidos_motor.py` — testes novos de orçamento/cenários nomeados | mitigate | Task 1 e Task 2 exigem que toda asserção numérica remonte a uma frase específica de `14-CONTEXT.md` § Decisions ou a uma linha de `14-VALIDATION.md` § Cenários nomeados; se o comportamento real divergir do documentado, a ação instrui PARAR e reportar gap de plano anterior, nunca escrever o teste bate-a-bate com o output observado. |
| T-14-07-02 | Tampering (regressão de teste silenciosa por assinatura de monkeypatch desatualizada — os dois arquivos "quebram em silêncio fora do arquivo de negócio", per `14-VALIDATION.md`) | MEDIUM | `app/tests/test_pedidos_motor_performance_024.py::_adequar_observando_subset`, `app/tests/test_pedidos_processing_cancellation_024.py::_slow_grade` | mitigate | Task 2 exige rodar a suíte COMPLETA (não só o arquivo do motor) e espelhar a assinatura real de `adequar_grade_produto` parâmetro por parâmetro, nunca por suposição; a suíte completa é o critério de saída de todo o plano (Task 3), não o arquivo do motor isolado. |
| T-14-07-03 | Tampering (mascarar bug de produção editando arquivo de produção a partir de um plano de teste, fora de escopo) | MEDIUM | qualquer arquivo em `app/modules/`, `app/shared/`, `alembic/` | mitigate | Task 3 Passo 1 roda `git status --short` restrito a diretórios de produção como gate de saída explícito; se algo aparecer modificado, o plano reverte e reporta, nunca commita produção a partir daqui. |
| T-14-07-04 | Repudiation (commit indevido em working tree compartilhado com trabalho de SSO não commitado) | LOW | working tree do repositório (fora do escopo deste plano: `app/modules/auth/`, `alembic/versions/031_*`, `microsoft_sso.py`) | mitigate | Task 3 Passo 5 exige `git add` com os 3 caminhos explícitos dos arquivos deste plano, nunca `-A`/`.`; `git status --short -- app/modules app/shared alembic` (mesmo comando do Passo 1) serve de dupla-checagem antes do commit. |
| T-14-07-SC | Tampering (supply chain — dependência nova instalada fora do escopo aprovado) | LOW | `pyproject.toml` | mitigate | Este plano não instala nenhum pacote; `hypothesis` é aprovado mas reservado para a Phase 19 — Task 3 Passo 4 verifica explicitamente a ausência de `hypothesis` em `pyproject.toml` e em imports de `app/tests/`. |

Nenhuma ameaça de integridade da verificação (T-14-07-01) fica sem mitigação prática: a diferença entre "reescrever conscientemente" e "ajustar até passar" é precisamente o que separa este plano de produzir prova real versus produzir um número verde vazio — por isso é o item de maior severidade do registro, mesmo sendo um risco de processo e não de exploração externa.
</threat_model>

## Artifacts this phase produces

- `app/tests/test_pedidos_motor.py` (modificado) — os três testes de orçamento por produto isolado (`test_adequar_grade_produto_aumenta_extremos_dentro_do_orcamento`, `test_adequar_grade_produto_falta_alem_da_tolerancia_vira_stand_by`, `test_adequar_grade_produto_falta_dentro_da_tolerancia_reduz_parcial`) removidos; `test_processar_pedidos_orcamento_pedido_corte_atravessa_produtos_respeita_teto`, `test_processar_pedidos_orcamento_pedido_corte_e_adicao_nao_se_compensam`, `test_processar_pedidos_orcamento_acumulado_execucoes_nao_reseta` adicionados; `test_processar_pedidos_prioriza_maior_valor_e_isola_sem_credito` ajustado no item 2; cobertura confirmada/completada para os 11 cenários nomeados de `14-VALIDATION.md`, incluindo `test_processar_pedidos_permutacao_de_produtos_produz_resultado_identico` se ausente.
- `app/tests/test_pedidos_motor_performance_024.py` (modificado, se necessário) — `_adequar_observando_subset` com assinatura espelhando `adequar_grade_produto` pós-14-06.
- `app/tests/test_pedidos_processing_cancellation_024.py` (modificado, se necessário) — `_slow_grade` com assinatura espelhando `adequar_grade_produto` pós-14-06, `cancel_token` preservado keyword-only.

<verification>
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_motor.py -q` — verde após a Task 1.
- `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) — verde após a Task 2 e novamente após a Task 3 como critério de saída da fase inteira, não apenas do arquivo do motor.
- `git status --short -- app/modules app/shared alembic` — vazio, prova de que nenhum arquivo de produção foi tocado por este plano.
- `grep -c "hypothesis" pyproject.toml` — `0`.
</verification>

<success_criteria>
- Nenhum teste de `test_pedidos_motor.py` afirma mais a regra antiga de orçamento por produto isolado; a regra nova (ledger por pedido, floor nos dois lados, dois orçamentos independentes, acumulado entre execuções) tem prova automatizada dedicada.
- Os dois arquivos que fixam a assinatura de `adequar_grade_produto` via monkeypatch espelham a assinatura real e atual, sem quebrar em silêncio.
- Os 11 cenários nomeados de `14-VALIDATION.md` têm cobertura de teste automatizada, verificada por leitura da asserção, não só por nome.
- Suíte completa do repositório (`uv run pytest -q` via Docker) 100% verde — este é o critério de saída da fase, não o arquivo do motor isolado.
- Nenhum arquivo de produção foi alterado por este plano; o helper de invariantes I1-I9 de 14-02 está em uso por pelo menos dois testes, confirmando que o domínio está pronto para os testes de propriedade da Phase 19 sem adicionar `hypothesis` agora.
</success_criteria>

<output>
Create `.planning/phases/14-motor-de-aloca-o-puro-regras-de-neg-cio/14-07-SUMMARY.md` when done
</output>
