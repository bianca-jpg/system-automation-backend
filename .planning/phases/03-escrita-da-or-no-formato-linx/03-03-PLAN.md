---
phase: 03-escrita-da-or-no-formato-linx
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/modules/pedidos/application/casos_uso.py
  - app/modules/pedidos/service.py
  - app/tests/test_pedidos_routes.py
autonomous: true
requirements: [LINX-02]

must_haves:
  truths:
    - "O fluxo /adequar (executar_adequacao) grava em ordens_reserva_linx, por cliente×produto×cor, nome_clifor/pedido/produto/cor_produto/preco1/valor_embalado/qtde_embalada, antes do commit já existente (critério 1)"
    - "O fluxo /sem_adequar (executar_sem_adequacao) grava a mesma linha Linx com os mesmos campos preenchidos (critério 2)"
    - "Uma falha na gravação Linx desfaz também ordens_reserva e pedidos_processados na mesma transação — nenhuma tabela fica com dado parcial (critério 3)"
    - "executar_alteracao_grade permanece intocado nesta fase — D-02 revista: a edição manual de grade fica para o milestone v1.2"
    - "montar_linha_linx e salvar_linhas_linx ficam reexportados em pedidos/service.py, chamáveis de fora dos 2 fluxos de geração (D-02a)"
  artifacts:
    - path: "app/modules/pedidos/application/casos_uso.py"
      provides: "executar_adequacao e executar_sem_adequacao gravando a linha Linx antes do commit"
      contains: "await salvar_linhas_linx(db, linhas_linx)"
    - path: "app/modules/pedidos/service.py"
      provides: "re-export de montar_linha_linx e salvar_linhas_linx (D-02a)"
      contains: "salvar_linhas_linx"
  key_links:
    - from: "app/modules/pedidos/application/casos_uso.py::executar_adequacao"
      to: "app/modules/pedidos/infrastructure/repositorio_ordens_linx.py::salvar_linhas_linx"
      via: "chamada direta, entre recalcular_estoque_virtual e await db.commit()"
      pattern: "await salvar_linhas_linx"
    - from: "app/modules/pedidos/application/casos_uso.py::executar_sem_adequacao"
      to: "app/modules/pedidos/infrastructure/repositorio_ordens_linx.py::salvar_linhas_linx"
      via: "idem"
      pattern: "await salvar_linhas_linx"
---

<objective>
Ligar `montar_linha_linx` (Plano 03-01) e `carregar_referencia_posicoes`/`salvar_linhas_linx` (Plano 03-02) aos 2 fluxos de geração — `executar_adequacao` e `executar_sem_adequacao` — SEMPRE entre `recalcular_estoque_virtual(db)` (gatilho já existente do placebo `estoque_virtual`, não reordenar) e o `await db.commit()` já existente de cada fluxo. `executar_alteracao_grade` NÃO é tocado (D-02 revista em `03-CONTEXT.md`: a edição manual de grade fica para o milestone v1.2). Reexportar `montar_linha_linx` e `salvar_linhas_linx` em `pedidos/service.py` (D-02a) para consumo futuro fora dos 2 fluxos.

Purpose: fecha o requirement LINX-02 e os critérios 1-3 do ROADMAP da Fase 3 — é o plano que efetivamente faz a linha Linx nascer/atualizar a cada geração, na mesma transação da OR interna.

Output: `executar_adequacao`/`executar_sem_adequacao` gravando a linha Linx antes do commit; `pedidos/service.py` expondo a função pública de (re)gravação (D-02a); testes mockados dos 2 fluxos provando os campos exigidos pelos critérios 1/2, e um teste com sessão real provando o rollback conjunto (critério 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-CONTEXT.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-RESEARCH.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-PATTERNS.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-01-SUMMARY.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-02-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring nos 2 fluxos de geração + re-export D-02a</name>

  <files>app/modules/pedidos/application/casos_uso.py, app/modules/pedidos/service.py</files>

  <read_first>
    - `app/modules/pedidos/application/casos_uso.py` (arquivo inteiro — ler o estado REAL do arquivo antes de editar; referenciar `executar_adequacao` e `executar_sem_adequacao` por NOME, nunca por número de linha, pois o arquivo pode ter sido editado por outra sessão)
    - `app/modules/pedidos/service.py` (arquivo inteiro, ~89 linhas) — padrão de re-export + `__all__` com comentário por bloco
    - `app/modules/pedidos/domain/ordem_reserva_linx.py` (produzido no Plano 03-01) — assinatura de `montar_linha_linx`
    - `app/modules/pedidos/infrastructure/repositorio_ordens_linx.py` (produzido no Plano 03-02) — assinatura de `salvar_linhas_linx`
    - `app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py` (produzido no Plano 03-02) — `carregar_referencia_posicoes`
    - `.planning/phases/03-escrita-da-or-no-formato-linx/03-CONTEXT.md` decisões D-01 (warning no ponto de chamada) e D-02a (função pública única)
  </read_first>

  <action>
    Em `casos_uso.py`: adicionar `carregar_referencia_posicoes` ao bloco de import já existente de `app.modules.pedidos.infrastructure.repositorio_ingestao_readmodel`; adicionar `from app.modules.pedidos.domain.ordem_reserva_linx import montar_linha_linx` e `from app.modules.pedidos.infrastructure.repositorio_ordens_linx import salvar_linhas_linx` como imports novos (imports diretos dos módulos, nunca via `service.py` — mesmo padrão de `repositorio_ordens.py`).

    Dentro de `executar_adequacao`, no bloco `if ors_a_gravar:`, IMEDIATAMENTE DEPOIS da chamada existente a `recalcular_estoque_virtual(db)` e ANTES do `await db.commit()` já existente (NUNCA depois do commit — quebraria o critério 3), inserir: `cd_prod_cors = {cd for _nr, cd in ors_a_gravar}`; `referencia = await carregar_referencia_posicoes(db, cd_prod_cors)`; `linhas_linx = []`; laço `for (nr, cd), itens in ors_a_gravar.items():` chamando `linha = montar_linha_linx(nr, cd, itens, referencia.get(cd, {}), tipo="com")`; se `linha is None` (D-01), `logger.warning("produto %s sem nenhuma posição em produto_tamanho_posicao — linha Linx não gravada (OR interna segue normal).", cd)` e `continue` (o `logger` do módulo já existe no topo do arquivo — a função pura não loga, o aviso é responsabilidade do chamador); senão `linhas_linx.append(linha)`; depois do laço, `await salvar_linhas_linx(db, linhas_linx)`.

    Dentro de `executar_sem_adequacao`, MESMO bloco de código, na mesma posição relativa (depois de `recalcular_estoque_virtual(db)`, antes do `await db.commit()`), usando `resultados` (o dict/`defaultdict` já construído nesse fluxo) em vez de `ors_a_gravar`, e `tipo="sem"` em vez de `"com"`.

    NÃO tocar `executar_alteracao_grade` (D-02 revista — o fluxo de edição manual de grade opera sobre pedidos ainda abertos, não sobre uma OR já gerada; corrigir isso é o milestone v1.2, GRADE-01..04).

    Em `service.py`: adicionar `from app.modules.pedidos.domain.ordem_reserva_linx import montar_linha_linx` e `from app.modules.pedidos.infrastructure.repositorio_ordens_linx import salvar_linhas_linx` ao bloco de imports; adicionar ambos ao `__all__`, imediatamente após a entrada existente de `salvar_ordens_reserva`, com o comentário `# função pública de (re)gravar a linha Linx de um par — D-02a, chamável de fora dos 2 fluxos de geração (ex.: futura edição manual, v1.2)`.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run python -c "from app.modules.pedidos.service import montar_linha_linx, salvar_linhas_linx; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - O comando `<automated>` acima imprime `OK`
    - `grep -c "await salvar_linhas_linx" app/modules/pedidos/application/casos_uso.py` retorna `2` (uma chamada em cada fluxo)
    - `grep -A40 "def executar_alteracao_grade" app/modules/pedidos/application/casos_uso.py | grep -c "salvar_linhas_linx"` retorna `0` (D-02 revista: fluxo intocado)
    - `grep -c "salvar_linhas_linx" app/modules/pedidos/service.py` retorna `2` (import + `__all__`)
    - `grep -c "montar_linha_linx" app/modules/pedidos/service.py` retorna `2` (import + `__all__`)
  </acceptance_criteria>

  <done>`executar_adequacao` e `executar_sem_adequacao` gravam a linha Linx entre o recálculo do estoque virtual e o commit já existente; `executar_alteracao_grade` permanece intocado (D-02 revista); `montar_linha_linx` e `salvar_linhas_linx` reexportados em `service.py` (D-02a).</done>
</task>

<task type="auto">
  <name>Task 2: Testes de integração dos 2 fluxos (critérios 1/2) + rollback conjunto (critério 3)</name>

  <files>app/tests/test_pedidos_routes.py</files>

  <read_first>
    - `app/tests/test_pedidos_routes.py` (arquivo inteiro — `test_adequar_pedidos_fluxo_completo_mockado` e `test_sem_adequar_pedidos_marca_processado_mockado`, funções a estender; referenciar por NOME, o arquivo pode ter sido editado pelos Planos 03-02)
    - `app/tests/test_ingestao_sync.py` linhas 434-479 (`test_sincronizar_tudo_nao_persiste_nada_se_uma_etapa_falhar`) — molde exato do teste de rollback conjunto: `side_effect=RuntimeError`, `async with async_session_factory() as session: with pytest.raises(RuntimeError): ...`, depois sessão nova de verificação
    - `.planning/ROADMAP.md` critérios 1, 2 e 3 da Fase 3
  </read_first>

  <action>
    Em `test_adequar_pedidos_fluxo_completo_mockado`: adicionar ao bloco `with (...)` dois patches novos — `patch("app.modules.pedidos.application.casos_uso.carregar_referencia_posicoes", new=AsyncMock(return_value={"X": {"M": 1}}))` e `patch("app.modules.pedidos.application.casos_uso.salvar_linhas_linx", new=AsyncMock()) as mock_salvar_linx`. Depois dos asserts existentes, adicionar (critério 1): `mock_salvar_linx.assert_awaited_once()`; `linhas_arg = mock_salvar_linx.await_args.args[1]`; `assert len(linhas_arg) == 1`; `linha = linhas_arg[0]`; e os asserts campo a campo: `linha["nome_clifor"] == "Cliente 1"`, `linha["pedido"] == 1`, `linha["produto"] == "X"`, `linha["cor_produto"] is None`, `linha["preco1"] == 100.0`, `linha["valor_embalado"] == 500.0`, `linha["qtde_embalada"] == 5`, `linha["e1"] == 5`.

    Em `test_sem_adequar_pedidos_marca_processado_mockado`: mesmo padrão, com `carregar_referencia_posicoes` devolvendo `{"Y": {"P": 1}}` (bate com `cd_prod_cor="Y"`, `sg_tamanho="P"` do `dados_fake` já existente no teste). Depois dos asserts existentes, adicionar (critério 2): `mock_salvar_linx.assert_awaited_once()`; `linha = mock_salvar_linx.await_args.args[1][0]`; `linha["nome_clifor"] == "Cliente 5"`, `linha["pedido"] == 5`, `linha["produto"] == "Y"`, `linha["cor_produto"] is None`, `linha["preco1"] == 100.0`, `linha["valor_embalado"] == 200.0`, `linha["qtde_embalada"] == 2`, `linha["e1"] == 2`.

    Adicionar `from app.modules.pedidos.application import casos_uso` ao bloco de imports do topo. Adicionar 1 teste novo, na seção "salvar_ordens_reserva direto na camada de serviço" (sem `TestClient`, sessão real): `test_rollback_conjunto_quando_gravacao_linx_falha` (critério 3) — `nr_fake = 999999030`; `dados_fake` com 1 item (`nr_pedido=nr_fake, cd_prod_cor="TESTE_ROLLBACK_LINX", sg_tamanho="M", ds_grupo="G", qt_liquida=1, vl_liquido=10.0, canal="Franquia", status_credito="Com Credito"`). Mockar `carregar_pedidos_itens` (retorna `dados_fake`), `carregar_processados` (retorna `set()`), `carregar_pares_processados_erp` (retorna `set()`), `carregar_referencia_posicoes` (retorna `{}`), `recalcular_estoque_virtual` (`AsyncMock()` sem `side_effect` — isola o teste do placebo `estoque_virtual`, fora do escopo desta fase), e `salvar_linhas_linx` com `side_effect=RuntimeError("boom")`. `salvar_ordens_reserva` e `salvar_processados` NÃO são mockados (escrevem de verdade na sessão, sem commit — são as escritas cujo rollback este teste prova). Dentro de `async with async_session_factory() as session: with pytest.raises(RuntimeError): await casos_uso.executar_sem_adequacao(session)` (saída sem commit descarta tudo). Depois, com uma sessão NOVA (`async with async_session_factory() as verificacao:`), assertar via `text(...)` que `SELECT count(*) FROM ordens_reserva WHERE nr_pedido = :nr`, `SELECT count(*) FROM pedidos_processados WHERE nr_pedido = :nr` e `SELECT count(*) FROM ordens_reserva_linx WHERE nr_pedido = :nr` são todos `0` — prova que a falha no passo NOVO desta fase (`salvar_linhas_linx`) desfaz também as escritas REAIS que já tinham acontecido na mesma sessão.
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_routes.py -v</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` acima mostra todos os testes do arquivo (existentes + novos) em `passed`, `0 failed`
    - `grep -c "mock_salvar_linx.assert_awaited_once" app/tests/test_pedidos_routes.py` retorna `2`
    - `grep -c "def test_rollback_conjunto_quando_gravacao_linx_falha" app/tests/test_pedidos_routes.py` retorna `1`
    - Regressão: `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) sai com código `0`
  </acceptance_criteria>

  <done>Os 2 fluxos de geração provam, por teste mockado, que gravam a linha Linx com os 7 campos exigidos pelos critérios 1/2; um teste com sessão real prova que uma falha na gravação Linx desfaz também `ordens_reserva` e `pedidos_processados` (critério 3); suíte completa permanece verde.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Artefatos que ESTE PLANO entrega (fecha a Fase 3 — visão cumulativa completa):

| Artefato | Tipo | Onde |
|----------|------|------|
| `executar_adequacao` (estendido) | caso de uso — grava linha Linx antes do commit | `app/modules/pedidos/application/casos_uso.py` |
| `executar_sem_adequacao` (estendido) | caso de uso — grava linha Linx antes do commit | `app/modules/pedidos/application/casos_uso.py` |
| re-export `montar_linha_linx` + `salvar_linhas_linx` (D-02a) | barrel de import externo | `app/modules/pedidos/service.py` |
| `test_rollback_conjunto_quando_gravacao_linx_falha` + extensões dos 2 testes de fluxo | teste de integração (mockado + sessão real) | `app/tests/test_pedidos_routes.py` |

Consumidores a jusante: o milestone v1.2 (GRADE-01..04) chama `montar_linha_linx` + `salvar_linhas_linx` via `pedidos.service` para regravar a linha Linx quando a edição manual de grade passar a afetar a OR já gerada.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `POST /adequar`, `POST /sem_adequar` (rotas existentes) | Já protegidas por RBAC (`OPERACIONAL`+); esta fase não cria endpoint novo nem muda o contrato de auth — só adiciona um efeito colateral interno (a gravação Linx). |
| `ordens_reserva_linx` (dado novo persistido) | Sem endpoint de leitura próprio nesta fase — só é escrita, nunca lida por HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Escrita Linx acontecendo DEPOIS do `await db.commit()` já existente romperia a atomicidade exigida pelo critério 3 | mitigate | Chamada a `salvar_linhas_linx` entra SEMPRE antes do commit já existente nos 2 fluxos — provado por teste de rollback conjunto com sessão real. |
| T-03-06 | Information Disclosure | `nome_clifor` (dado de cliente) persistido em `ordens_reserva_linx` sem os mesmos controles de leitura de `ordens_reserva` | accept | Nenhum endpoint de leitura de `ordens_reserva_linx` é criado nesta fase; revisar RBAC quando uma superfície de leitura/exportação for adicionada (INTG-01). |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | Nenhum pacote novo instalado nesta fase. |
</threat_model>

<verification>
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run python -c "from app.modules.pedidos.service import montar_linha_linx, salvar_linhas_linx; print('OK')"` → `OK`
2. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_routes.py -v` → todos os testes `passed`
3. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) → verde, sem regressão — encerra a Fase 3
</verification>

<success_criteria>
- `executar_adequacao` grava a linha Linx com os 7 campos exigidos, antes do commit (critério 1)
- `executar_sem_adequacao` grava a mesma linha Linx com os mesmos campos (critério 2)
- Falha na gravação Linx desfaz `ordens_reserva` e `pedidos_processados` na mesma transação (critério 3)
- `executar_alteracao_grade` permanece intocado (D-02 revista)
- `montar_linha_linx`/`salvar_linhas_linx` reexportados em `service.py` (D-02a)
- Suíte completa verde — os 5 critérios de sucesso do ROADMAP da Fase 3 estão cobertos por teste automatizado
</success_criteria>

<output>
Criar `.planning/phases/03-escrita-da-or-no-formato-linx/03-03-SUMMARY.md` ao terminar, registrando: (a) os testes estendidos/novos e seus resultados, (b) confirmação de que `executar_alteracao_grade` não foi tocado, (c) fechamento formal do requirement LINX-02 e dos 5 critérios de sucesso da Fase 3 — última fase do milestone v1.1.
</output>
