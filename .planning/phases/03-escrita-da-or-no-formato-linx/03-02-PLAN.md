---
phase: 03-escrita-da-or-no-formato-linx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/modules/ingestao/api_leitura.py
  - app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py
  - app/modules/pedidos/infrastructure/repositorio_ordens_linx.py
  - app/tests/test_ingestao_api_leitura.py
  - app/tests/test_pedidos_routes.py
autonomous: true
requirements: [LINX-02]

must_haves:
  truths:
    - "A leitura da referência de posição filtra por cd_prod_cor IN (...) da rodada, em chunks de 500 — nunca carrega as 569.726 linhas de produto_tamanho_posicao de uma vez"
    - "Rodar o upsert duas vezes para o mesmo (nr_pedido, cd_prod_cor) atualiza a linha existente em ordens_reserva_linx em vez de duplicá-la (critério 4 do ROADMAP)"
    - "O upsert usa SELECT + decide (nunca db.get), porque a PK de OrdemReservaLinx (id) é diferente da chave natural (nr_pedido, cd_prod_cor)"
  artifacts:
    - path: "app/modules/ingestao/api_leitura.py"
      provides: "obter_referencia_posicoes_por_produtos — batch SELECT filtrado, chunk de 500"
      contains: "def obter_referencia_posicoes_por_produtos"
    - path: "app/modules/pedidos/infrastructure/repositorio_ordens_linx.py"
      provides: "salvar_linhas_linx — upsert por (nr_pedido, cd_prod_cor), sem commit"
      contains: "def salvar_linhas_linx"
  key_links:
    - from: "app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py"
      to: "app/modules/ingestao/api_leitura.py"
      via: "re-export com alias, mesmo padrão de carregar_pedidos_itens"
      pattern: "obter_referencia_posicoes_por_produtos as carregar_referencia_posicoes"
    - from: "app/modules/pedidos/infrastructure/repositorio_ordens_linx.py"
      to: "app/modules/pedidos/models.py::OrdemReservaLinx"
      via: "select(OrdemReservaLinx).where(nr_pedido ==, cd_prod_cor ==) — nunca db.get"
      pattern: "select(OrdemReservaLinx).where"
---

<objective>
Construir as 2 peças de infraestrutura que a Fase 3 precisa além da montagem pura da linha (Plano 03-01, independente deste): (1) a leitura em lote da referência de posição, filtrada pelos produtos da rodada atual (nunca a tabela `produto_tamanho_posicao` inteira — 569.726 linhas reais, medido na Fase 2), e (2) o repositório de upsert de `ordens_reserva_linx`, que precisa de SELECT + decide (não `db.get`) porque a PK real (`id`, autoincrement) é diferente da chave natural `(nr_pedido, cd_prod_cor)` usada pela `UniqueConstraint`.

Purpose: sem a leitura em lote, cada geração pagaria o custo de escanear 569.726 linhas; sem o upsert correto, a segunda geração do mesmo par colidiria com `uq_ordens_reserva_linx_chave` (`IntegrityError`). Ambas as peças são consumidas pelo Plano 03-03 (wiring nos 2 fluxos de geração).

Output: `obter_referencia_posicoes_por_produtos` (ingestão) + `carregar_referencia_posicoes` (re-export cross-context) + `salvar_linhas_linx` (repositório), com testes provando filtragem correta e upsert sem duplicação (critério 4 do ROADMAP).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-CONTEXT.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-RESEARCH.md
@.planning/phases/03-escrita-da-or-no-formato-linx/03-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: obter_referencia_posicoes_por_produtos (batch, chunk 500) + re-export</name>

  <files>app/modules/ingestao/api_leitura.py, app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py, app/tests/test_ingestao_api_leitura.py</files>

  <read_first>
    - `app/modules/ingestao/api_leitura.py` (arquivo inteiro, 194 linhas) — padrão das funções já expostas (`obter_estoque_por_canal`, `obter_pedidos_processados_erp`), todas com `select(...)` já importado no topo
    - `app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py` (arquivo inteiro, 37 linhas) — padrão de re-export com alias das 5 funções já importadas de `ingestao.api_leitura`
    - `app/modules/ingestao/models.py` classe `ProdutoTamanhoPosicao` (linhas 148-168) — `UniqueConstraint("cd_prod_cor", "sg_tamanho")`, sem `index=True` próprio
    - `app/tests/test_ingestao_api_leitura.py` (arquivo inteiro, 64 linhas) — molde de teste read-only (sem escrita, sem rollback necessário) contra o Postgres real
    - `.planning/phases/03-escrita-da-or-no-formato-linx/03-RESEARCH.md` Pitfall 2 (nunca carregar `produto_tamanho_posicao` inteira — 569.726 linhas reais)
  </read_first>

  <action>
    Em `app/modules/ingestao/api_leitura.py`: adicionar `ProdutoTamanhoPosicao` ao import existente de `app.modules.ingestao.models` (junto de `Estoque, FaturamentoColecao, Pedido, PedidoProcessadoErp`). Adicionar constante de módulo `_TAMANHO_CHUNK = 500` com comentário explicando a margem sob o limite de 65.535 parâmetros por statement do protocolo Postgres. Implementar `async def obter_referencia_posicoes_por_produtos(db: AsyncSession, cd_prod_cors: set[str]) -> dict[str, dict[str, int]]`: se `cd_prod_cors` vazio, devolve `{}` sem consultar o banco; senão converte para lista e itera em fatias de `_TAMANHO_CHUNK`, executando por fatia `select(ProdutoTamanhoPosicao.cd_prod_cor, ProdutoTamanhoPosicao.sg_tamanho, ProdutoTamanhoPosicao.nr_posicao).where(ProdutoTamanhoPosicao.cd_prod_cor.in_(chunk))`, acumulando em `referencia.setdefault(cd, {})[tam] = pos`. NUNCA fazer `select(ProdutoTamanhoPosicao)` sem `.where(...)` (Pitfall 2 — a tabela tem 569.726 linhas reais, medidas na Fase 2). Devolve `{}` para um `cd_prod_cor` sem nenhuma posição cadastrada (não gera `KeyError`; D-01 é decidido por quem chama, Plano 03-03).

    Em `app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py`: adicionar ao bloco de import já existente de `app.modules.ingestao.api_leitura` a linha `obter_referencia_posicoes_por_produtos as carregar_referencia_posicoes,` (mesmo padrão de alias das 5 funções já importadas).

    Em `app/tests/test_ingestao_api_leitura.py`: adicionar `from sqlalchemy import select` e `from app.modules.ingestao.models import ProdutoTamanhoPosicao` aos imports do topo. Adicionar 3 testes, seguindo o padrão read-only já usado no arquivo (sem escrita, sem necessidade de rollback):
    1. `test_obter_referencia_posicoes_por_produtos_filtra_pelos_produtos_da_rodada` — descobre 2 `cd_prod_cor` reais distintos já ingeridos (`select(ProdutoTamanhoPosicao.cd_prod_cor).distinct().limit(2)`, assert `len == 2` — a referência real da Fase 2 tem 569.726 linhas, então isso nunca falha); chama `obter_referencia_posicoes_por_produtos` com esses 2 códigos; assert `set(referencia.keys()) == set(esses 2 códigos)` e que cada um tem ao menos 1 posição.
    2. `test_obter_referencia_posicoes_por_produtos_conjunto_vazio_devolve_dict_vazio` — chama com `set()`; assert `== {}`.
    3. `test_obter_referencia_posicoes_por_produtos_produto_inexistente_nao_aparece` — chama com `{"PRODUTO_QUE_NUNCA_EXISTIU|999"}`; assert `== {}` (sem `KeyError`).
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_api_leitura.py -v -k referencia_posicoes</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` acima mostra `3 passed`, `0 failed`
    - `grep -c "def obter_referencia_posicoes_por_produtos" app/modules/ingestao/api_leitura.py` retorna `1`
    - `grep -c "_TAMANHO_CHUNK" app/modules/ingestao/api_leitura.py` retorna `2` (declaração + uso no `range`)
    - `grep -c "carregar_referencia_posicoes" app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py` retorna `1`
    - `grep -c "select(ProdutoTamanhoPosicao)" app/modules/ingestao/api_leitura.py` retorna `0` (nunca um SELECT sem filtro — Pitfall 2)
  </acceptance_criteria>

  <done>`obter_referencia_posicoes_por_produtos` filtra por produtos da rodada em chunks de 500, nunca carrega a tabela inteira; `carregar_referencia_posicoes` re-exporta para o módulo `pedidos`; 3 testes provam filtragem correta com dados reais já ingeridos na Fase 2.</done>
</task>

<task type="auto">
  <name>Task 2: salvar_linhas_linx (upsert SELECT+decide) — critério 4</name>

  <files>app/modules/pedidos/infrastructure/repositorio_ordens_linx.py, app/tests/test_pedidos_routes.py</files>

  <read_first>
    - `app/modules/pedidos/infrastructure/repositorio_ordens.py` (arquivo inteiro, 147 linhas) — `aprovar_ordem_reserva` (SELECT + decide, molde direto) e `salvar_ordens_reserva` (upsert com `db.get`, NÃO usar como molde de lookup aqui — a PK de `OrdemReserva` É a chave natural, diferente de `OrdemReservaLinx`)
    - `app/modules/pedidos/models.py` classe `OrdemReservaLinx` (linhas 131-237) — PK `id` autoincrement, `UniqueConstraint("nr_pedido", "cd_prod_cor", name="uq_ordens_reserva_linx_chave")`
    - `app/tests/test_pedidos_routes.py` seção "salvar_ordens_reserva direto na camada de serviço" (`test_salvar_ordens_reserva_par_novo_insere_sem_erro` e `test_salvar_ordens_reserva_par_existente_atualiza_sem_erro`) — molde exato de teste de upsert real, sem `TestClient`, `session.flush()` nunca `commit()`
  </read_first>

  <action>
    Criar `app/modules/pedidos/infrastructure/repositorio_ordens_linx.py` com docstring de módulo explicando: (1) o motivo do SELECT+decide (a PK real de `OrdemReservaLinx` é `id` autoincrement; a chave natural `(nr_pedido, cd_prod_cor)` é só `UniqueConstraint` — `db.get` nunca encontraria a linha existente e toda segunda gravação colidiria com `uq_ordens_reserva_linx_chave`); (2) o contrato "não commita" (mesmo padrão de `repositorio_ordens.py`). Importar `from sqlalchemy import select` e `from sqlalchemy.ext.asyncio import AsyncSession` no topo.

    Implementar `async def salvar_linhas_linx(db: AsyncSession, linhas: list[dict]) -> None`: importar `OrdemReservaLinx` de `app.modules.pedidos.models` DENTRO da função (padrão do módulo, igual a todas as funções de `repositorio_ordens.py`). Para cada `linha` em `linhas`: executar `select(OrdemReservaLinx).where(OrdemReservaLinx.nr_pedido == linha["nr_pedido"], OrdemReservaLinx.cd_prod_cor == linha["cd_prod_cor"])`, `.scalar_one_or_none()`; se `None`, `db.add(OrdemReservaLinx(**linha))`; senão, laço `for campo, valor in linha.items(): setattr(obj, campo, valor)`. NUNCA usar `db.get(OrdemReservaLinx, ...)`.

    Em `app/tests/test_pedidos_routes.py`: adicionar `from app.modules.pedidos.infrastructure.repositorio_ordens_linx import salvar_linhas_linx` ao bloco de imports do topo. Adicionar 2 testes na seção "salvar_ordens_reserva direto na camada de serviço" (mesmo padrão: `async with async_session_factory() as session`, `await session.flush()`, nunca `commit()`):
    1. `test_salvar_linhas_linx_par_novo_insere_sem_erro` — 1 linha nova com `nr_pedido`, `cd_prod_cor`, `tipo="com"`, `nome_clifor`; `await salvar_linhas_linx(session, [linha])`; `await session.flush()` sem erro.
    2. `test_salvar_linhas_linx_par_existente_atualiza_sem_duplicar` (critério 4) — 1º upsert grava `tipo="sem", preco1=10.0`; `flush()`; 2º upsert do MESMO par (`nr_pedido`, `cd_prod_cor`) com `tipo="com", preco1=20.0`; `flush()`; consultar via `text("SELECT count(*) FROM ordens_reserva_linx WHERE nr_pedido = :nr AND cd_prod_cor = :cd")` (`text` já importado no arquivo) e assertar `== 1`; consultar `preco1`/`tipo` e assertar que refletem o 2º upsert (`20.0`/`"com"`, não `10.0`/`"sem"`).
  </action>

  <verify>
    <automated>docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_routes.py -v -k salvar_linhas_linx</automated>
  </verify>

  <acceptance_criteria>
    - A saída do comando `<automated>` acima mostra `2 passed`, `0 failed`
    - `grep -c "def salvar_linhas_linx" app/modules/pedidos/infrastructure/repositorio_ordens_linx.py` retorna `1`
    - `grep -c "db.get(OrdemReservaLinx" app/modules/pedidos/infrastructure/repositorio_ordens_linx.py` retorna `0` (nunca `db.get` — PK ≠ chave natural)
    - `grep -c "def test_salvar_linhas_linx" app/tests/test_pedidos_routes.py` retorna `2`
  </acceptance_criteria>

  <done>`salvar_linhas_linx` faz upsert real por `(nr_pedido, cd_prod_cor)` sem duplicar em rodadas sucessivas (critério 4), provado por teste com sessão real nunca commitada.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Artefatos que ESTE PLANO entrega:

| Artefato | Tipo | Onde |
|----------|------|------|
| `obter_referencia_posicoes_por_produtos` | função assíncrona (leitura em lote, cross-context) | `app/modules/ingestao/api_leitura.py` |
| `carregar_referencia_posicoes` (alias) | re-export cross-context | `app/modules/pedidos/infrastructure/repositorio_ingestao_readmodel.py` |
| `salvar_linhas_linx` | função assíncrona (upsert, sem commit) | `app/modules/pedidos/infrastructure/repositorio_ordens_linx.py` (novo) |
| 3 testes de `obter_referencia_posicoes_por_produtos` | teste de integração (leitura, sem escrita) | `app/tests/test_ingestao_api_leitura.py` |
| 2 testes de `salvar_linhas_linx` | teste de integração (sessão real, nunca commitada) | `app/tests/test_pedidos_routes.py` |

Consumidores a jusante: o Plano 03-03 importa `carregar_referencia_posicoes` e `salvar_linhas_linx` diretamente em `casos_uso.py`; `pedidos/service.py` reexporta `salvar_linhas_linx` no Plano 03-03 (D-02a), para consumo externo (v1.2).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `produto_tamanho_posicao` (569.726 linhas, já validada na Fase 2) → filtrada por produtos da rodada antes de chegar ao módulo `pedidos` | Leitura cross-context já estabelecida (mesmo padrão de `obter_estoque_por_canal`); nenhuma mudança de modelo de confiança. |
| Upsert de `ordens_reserva_linx` | Sem endpoint HTTP novo — chamado só internamente pelos casos de uso (Plano 03-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service (interno) | `obter_referencia_posicoes_por_produtos` — carregar a referência sem filtro consultaria 569.726 linhas a cada `/adequar`/`/sem_adequar` | mitigate | Sempre `WHERE cd_prod_cor IN (...)` em chunks de 500; nunca `select(ProdutoTamanhoPosicao)` sem filtro — coberto por teste automatizado e por checagem estática (grep). |
| T-03-04 | Tampering | `salvar_linhas_linx` — usar `db.get` com a chave natural (em vez da PK `id`) faria toda segunda gravação tentar INSERT e colidir com `uq_ordens_reserva_linx_chave` (`IntegrityError`) | mitigate | SELECT + decide (`scalar_one_or_none`), nunca `db.get` — coberto por teste automatizado do critério 4 (upsert sem duplicar). |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | Nenhum pacote novo instalado nesta fase. |
</threat_model>

<verification>
1. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_ingestao_api_leitura.py -v` → todos os casos (existentes + novos) em `passed`
2. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest app/tests/test_pedidos_routes.py -v -k salvar_linhas_linx` → `2 passed`
3. `docker compose -f .docker/docker-compose.yml exec -T api uv run pytest -q` (suíte completa) → verde, sem regressão
</verification>

<success_criteria>
- A leitura da referência sempre filtra por produtos da rodada, nunca a tabela inteira
- Duas gravações sucessivas do mesmo `(nr_pedido, cd_prod_cor)` resultam em 1 linha, não 2 (critério 4)
- Nenhum uso de `db.get` para `OrdemReservaLinx`
- Suíte completa verde
</success_criteria>

<output>
Criar `.planning/phases/03-escrita-da-or-no-formato-linx/03-02-SUMMARY.md` ao terminar, registrando: (a) os 5 testes novos e seus resultados, (b) confirmação de que a leitura nunca escaneia a tabela inteira, (c) assinaturas finais de `obter_referencia_posicoes_por_produtos` e `salvar_linhas_linx` para referência do Plano 03-03.
</output>
