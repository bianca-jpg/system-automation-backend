---
phase: 01-schema-linx-e-refer-ncia-de-posi-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/modules/ingestao/models.py
  - app/modules/pedidos/models.py
  - alembic/env.py
  - app/tests/test_schema_guard.py
autonomous: true
requirements: [LINX-01]

must_haves:
  truths:
    - "A classe ProdutoTamanhoPosicao existe e declara a tabela produto_tamanho_posicao com 5 colunas (cd_prod_cor, sg_tamanho, nr_posicao + id, synced_at)"
    - "A classe OrdemReservaLinx existe e declara a tabela ordens_reserva_linx com 82 colunas (77 do layout Linx + 5 de controle)"
    - "Base.metadata passa a conhecer as duas tabelas novas quando alembic/env.py é carregado"
    - "test_schema_guard.py gera casos de teste para as duas tabelas novas (antes destes imports ele era cego a elas)"
    - "Nenhum model existente é alterado e nenhuma dependência nova é instalada"
  artifacts:
    - path: "app/modules/ingestao/models.py"
      provides: "class ProdutoTamanhoPosicao (estilo Mapped/mapped_column)"
      contains: "class ProdutoTamanhoPosicao"
    - path: "app/modules/pedidos/models.py"
      provides: "class OrdemReservaLinx (estilo Column) com o layout Linx completo"
      contains: "class OrdemReservaLinx"
    - path: "alembic/env.py"
      provides: "registro dos 2 models novos em target_metadata"
      contains: "ProdutoTamanhoPosicao"
    - path: "app/tests/test_schema_guard.py"
      provides: "cobertura do guard de drift para as 2 tabelas novas"
      contains: "OrdemReservaLinx"
  key_links:
    - from: "alembic/env.py"
      to: "app.modules.ingestao.models.ProdutoTamanhoPosicao / app.modules.pedidos.models.OrdemReservaLinx"
      via: "import explícito (sem autodiscovery) que popula Base.metadata"
      pattern: "ProdutoTamanhoPosicao|OrdemReservaLinx"
    - from: "app/tests/test_schema_guard.py"
      to: "Base.metadata.tables"
      via: "parametrize sobre sorted(Base.metadata.tables) — só cobre tabela cujo model foi importado"
      pattern: "ProdutoTamanhoPosicao|OrdemReservaLinx"
---

<objective>
Declarar os dois models ORM novos do milestone — `ProdutoTamanhoPosicao` (módulo `ingestao`) e `OrdemReservaLinx` (módulo `pedidos`) — e registrá-los nos dois pontos de import explícito do repositório (`alembic/env.py` e `app/tests/test_schema_guard.py`).

Purpose: os models são a fonte de verdade que a migration 015 (Plano 02) vai espelhar coluna a coluna, e são também o que o `test_schema_guard.py` compara contra o `information_schema` real. Sem os imports dos dois pontos, a migration "funciona" mas os dois mecanismos de guarda contra drift ficam cegos para as tabelas novas — exatamente o incidente da migration 012 perdida que motivou a criação do guard (ver 01-RESEARCH.md, Pitfall 1).

Output: 2 classes ORM novas + 2 blocos de import estendidos. Nenhum DDL é executado neste plano (o banco não muda) e nenhuma dependência nova é instalada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-RESEARCH.md
@.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

<decisoes_travadas>
Decisões já tomadas (PROJECT.md Key Decisions + orquestrador). **Não reabrir, não "simplificar", não adiar:**

1. `produto_tamanho_posicao` vive no módulo `ingestao`, no estilo `Mapped[...] = mapped_column(...)` — o estilo usado 100% naquele arquivo.
2. `ordens_reserva_linx` vive no módulo `pedidos`, no estilo `Column(...)` sem `Mapped` — o estilo usado 100% naquele arquivo. **Não uniformizar os dois módulos**: a divergência de estilo é pré-existente do repo, não uma escolha desta fase.
3. Colunas do layout Linx todas `nullable=True`; só as de controle interno (`id`, `nr_pedido`, `cd_prod_cor`, `tipo`, `created_at`) são `NOT NULL`.
4. `e1..e48` são `Integer` com **default 0** (`server_default="0"`) e `nullable=True`. A 01-RESEARCH.md sugeria adiar o default para a Fase 3 — a decisão travada prevalece sobre a recomendação da pesquisa.
5. `UniqueConstraint("nr_pedido", "cd_prod_cor", name="uq_ordens_reserva_linx_chave")` entra **já nesta fase** (a Fase 3 precisa dela para o upsert `ON CONFLICT`; tabela vazia, custo zero). Resolve a Open Question 1 da 01-RESEARCH.md: **sim**.
6. `id` + `synced_at` entram em `produto_tamanho_posicao` (consistência com as 4 tabelas irmãs de `ingestao`). Resolve a Open Question 2 da 01-RESEARCH.md: **sim**.
7. **SEM `ForeignKeyConstraint`** de `ordens_reserva_linx` para `ordens_reserva`/`pedidos`: `nr_pedido`/`cd_prod_cor` são campos de correlação, não FK (01-RESEARCH.md, Pitfall 4).
8. **SEM `index=True` em `OrdemReservaLinx.nr_pedido`** — decisão deste plano, divergindo do exemplo de 01-RESEARCH.md/01-PATTERNS.md: o `UniqueConstraint("nr_pedido", "cd_prod_cor")` já cria um btree cujo prefixo esquerdo é `nr_pedido`, então um índice separado seria redundante. É o precedente já documentado no próprio repo em `FaturamentoColecao` (`app/modules/ingestao/models.py`, comentário nas linhas 114-115: "Sem index=True: o unique (colecao, canal) já serve de índice ... prefixo esquerdo do btree"). Consequência para o Plano 02: a migration 015 **não** tem `op.create_index` e o `downgrade()` tem apenas 2 `op.drop_table`.
9. Nenhuma dependência nova (`uv add` não se aplica a esta fase) e nenhum model existente é alterado.
</decisoes_travadas>

<tasks>

<task type="auto">
  <name>Task 1: Model ProdutoTamanhoPosicao no módulo ingestao</name>

  <files>app/modules/ingestao/models.py</files>

  <read_first>
    - `app/modules/ingestao/models.py` — arquivo que será editado; ver o estilo real das 4 classes existentes (`Pedido`, `PedidoProcessadoErp`, `Estoque`, `FaturamentoColecao`) e o bloco de imports das linhas 8-14
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-PATTERNS.md` — seção "app/modules/ingestao/models.py — nova classe ProdutoTamanhoPosicao": o analog é a classe `Estoque` (linhas 83-98)
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-RESEARCH.md` — seção "`produto_tamanho_posicao` — colunas → tipo proposto"
  </read_first>

  <action>
    Antes de editar, tente chamar `backstage_get_coding_standards` (MCP `backstage`), conforme exige `.claude/CLAUDE.md`. Se a ferramenta não estiver disponível (sem `.mcp.json`/PAT no ambiente), registre isso no SUMMARY em uma linha e siga os padrões dos análogos deste repositório — não bloqueie a execução por causa disso.

    Adicionar a classe `ProdutoTamanhoPosicao(Base)` ao FINAL de `app/modules/ingestao/models.py` (depois de `FaturamentoColecao`), no estilo `Mapped[tipo] = mapped_column(...)` idêntico a `Estoque`.

    Docstring: explicar que é a referência tamanho→posição da grade Linx, vinda da view Databricks `programa_estagio.refined.system_automation_prod_tamanho_ref`, em full refresh a cada sync como as demais fontes de `ingestao`, e que sem ela não há como preencher `e1..e48` em `ordens_reserva_linx` (Fases 2 e 3).

    `__tablename__ = "produto_tamanho_posicao"`.

    `__table_args__` com um único `UniqueConstraint("cd_prod_cor", "sg_tamanho", name="uq_produto_tamanho_posicao_chave")` — chave natural da referência, necessária para o full refresh da Fase 2 não duplicar linhas.

    Colunas, nesta ordem, todas `NOT NULL` (5 no total):
    - `id`: `Mapped[int]`, `Integer`, `primary_key=True`, `autoincrement=True`
    - `cd_prod_cor`: `Mapped[str]`, `String(64)`, `nullable=False`
    - `sg_tamanho`: `Mapped[str]`, `String(16)`, `nullable=False`
    - `nr_posicao`: `Mapped[int]`, `Integer`, `nullable=False`
    - `synced_at`: `Mapped[datetime]`, `DateTime(timezone=True)`, `server_default=func.now()`, `nullable=False`

    NÃO adicionar imports novos: `datetime`, `DateTime`, `Integer`, `String`, `UniqueConstraint` e `func` já estão importados no topo do arquivo. NÃO declarar `index=True` em nenhuma coluna (o unique já indexa `cd_prod_cor` como prefixo esquerdo). NÃO tocar nas 4 classes existentes.
  </action>

  <verify>
    <automated>uv run python -c "from app.modules.ingestao.models import ProdutoTamanhoPosicao as P; c=P.__table__.columns; assert P.__tablename__=='produto_tamanho_posicao'; assert len(c)==5, len(c); assert {x.name for x in c}=={'id','cd_prod_cor','sg_tamanho','nr_posicao','synced_at'}; assert all(not x.nullable for x in c); assert [x.name for x in P.__table__.primary_key.columns]==['id']; assert any(u.name=='uq_produto_tamanho_posicao_chave' for u in P.__table__.constraints if hasattr(u,'name') and u.name); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `app/modules/ingestao/models.py` contém `class ProdutoTamanhoPosicao(Base):`
    - `ProdutoTamanhoPosicao.__tablename__ == "produto_tamanho_posicao"`
    - `len(ProdutoTamanhoPosicao.__table__.columns) == 5` e os nomes são exatamente `id, cd_prod_cor, sg_tamanho, nr_posicao, synced_at`
    - Todas as 5 colunas têm `nullable == False`; a PK é `["id"]`
    - Existe a constraint nomeada `uq_produto_tamanho_posicao_chave`
    - O comando `<automated>` acima imprime `OK` e sai com código 0
    - `git diff --stat app/modules/ingestao/models.py` mostra apenas linhas adicionadas (nenhuma removida) — as 4 classes existentes intactas
    - `uv run pytest -q` continua verde (nenhuma regressão de import)
  </acceptance_criteria>

  <done>A classe `ProdutoTamanhoPosicao` existe no módulo `ingestao` com as 5 colunas e a unique de chave natural, no mesmo estilo `Mapped` do resto do arquivo, sem imports novos e sem alterar nada existente.</done>
</task>

<task type="auto">
  <name>Task 2: Model OrdemReservaLinx no módulo pedidos (layout Linx completo, 82 colunas)</name>

  <files>app/modules/pedidos/models.py</files>

  <read_first>
    - `app/modules/pedidos/models.py` — arquivo que será editado; ver o estilo `Column(...)` das 3 classes existentes (`PedidoProcessado`, `OrdemReserva`, `PedidoModificacao`) e o bloco de imports das linhas 13-16
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-PATTERNS.md` — seção "app/modules/pedidos/models.py — nova classe OrdemReservaLinx": o analog é `OrdemReserva` (linhas 33-50), com o bloco de imports a estender
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-RESEARCH.md` — seção "Layout completo do CSV Linx (`Tabelas_de_OR.csv`) — 32 colunas → tipo proposto" (tabela nome→tipo, coluna a coluna) e "Colunas de controle"
  </read_first>

  <action>
    Estender o import do topo de `app/modules/pedidos/models.py` (linha 13) para incluir `Boolean`, `Numeric` e `UniqueConstraint`, mantendo `Column`, `DateTime`, `Integer`, `String`, `func` e o import de `JSONB` (ainda usado por `OrdemReserva`/`PedidoModificacao`). Ordem alfabética: `Boolean, Column, DateTime, Integer, Numeric, String, UniqueConstraint, func`.

    Adicionar a classe `OrdemReservaLinx(Base)` ao FINAL do arquivo, no estilo `Column(...)` sem `Mapped`, com docstring explicando que é a "bandeja de saída" no layout aceito pelo ERP Linx (`Tabelas_de_OR.csv`), que correlaciona com `OrdemReserva` via `(nr_pedido, cd_prod_cor)` mas **sem FK** (é projeção de saída para sistema externo, não o modelo interno), e que os campos sem fonte conhecida hoje (rastreados como INTG-03) ficam NULL.

    `__tablename__ = "ordens_reserva_linx"`.

    `__table_args__` com um único `UniqueConstraint("nr_pedido", "cd_prod_cor", name="uq_ordens_reserva_linx_chave")`.

    **Colunas de controle interno (5, todas NOT NULL), primeiro bloco:**
    `id` Integer `primary_key=True` `autoincrement=True`; `nr_pedido` Integer `nullable=False` (SEM `index=True` — ver decisão travada 8); `cd_prod_cor` String(64) `nullable=False`; `tipo` String(8) `nullable=False` com comentário `# 'sem' | 'com'`; `created_at` DateTime(timezone=True) `server_default=func.now()` `nullable=False`.

    **Colunas do layout Linx (77, TODAS `nullable=True`), segundo bloco, exatamente nesta ordem e com estes tipos:**
    `nome_clifor` String(255); `produto` String(64); `cor_produto` String(16); `filial` String(32); `item` Integer; `pedido` Integer; `pedido_cor_produto` String(64); `romaneio` String(32); `caixa` String(32); `pedido_produto` String(64); `packs` Integer; `entrega` DateTime(timezone=True); `caixa_fechada` Boolean; `representante` String(255); `ipi` Numeric(14, 2); `preco1` Numeric(14, 2); `preco2` Numeric(14, 2); `preco3` Numeric(14, 2); `preco4` Numeric(14, 2); `desconto_item` Numeric(14, 2); `valor_embalado` Numeric(14, 2); `qtde_embalada` Integer; `origem` String(32); `mata_saldo` Boolean; **`e1` até `e48`** Integer com `server_default="0"`; `item_pedido` Integer; `ordem_producao` String(64); `licenciado_royalties` Boolean; `percent_desconto` Numeric(6, 2); `caixa_virtual` String(32).

    As 48 colunas `e1..e48` devem ser declaradas **explicitamente, uma por linha** — NUNCA via loop, `exec` ou comprehension no model (o atalho com comprehension é aceitável só no arquivo de migration, Plano 02). Motivo: legibilidade/autocomplete e depuração, alinhado à constraint "mantenedora é dev iniciante" de PROJECT.md.

    Comentário curto acima do bloco do layout apontando para `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-RESEARCH.md` como fonte de tipo/origem de cada coluna, e outro acima do bloco `e1..e48` explicando que é a grade posicional resolvida via `produto_tamanho_posicao` (Fases 2 e 3).

    Total esperado: 5 + 77 = **82 colunas**. NÃO adicionar `ForeignKeyConstraint`. NÃO usar JSONB para a grade. NÃO tocar nas 3 classes existentes.
  </action>

  <verify>
    <automated>uv run python -c "from app.modules.pedidos.models import OrdemReservaLinx as O; c=O.__table__.columns; n={x.name for x in c}; assert O.__tablename__=='ordens_reserva_linx'; assert len(c)==82, len(c); ctrl={'id','nr_pedido','cd_prod_cor','tipo','created_at'}; assert ctrl<=n; assert {f'e{i}' for i in range(1,49)}<=n; assert all(not c[x].nullable for x in ctrl); assert all(c[x].nullable for x in n-ctrl); assert [x.name for x in O.__table__.primary_key.columns]==['id']; assert any(getattr(k,'name','')=='uq_ordens_reserva_linx_chave' for k in O.__table__.constraints); assert not O.__table__.foreign_keys; assert not O.__table__.indexes; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `app/modules/pedidos/models.py` contém `class OrdemReservaLinx(Base):`
    - `len(OrdemReservaLinx.__table__.columns) == 82`
    - As 5 colunas de controle (`id`, `nr_pedido`, `cd_prod_cor`, `tipo`, `created_at`) têm `nullable == False`; as 77 restantes têm `nullable == True`
    - As 48 colunas `e1`..`e48` existem, são `Integer` e têm `server_default` (verificável: `uv run python -c "from app.modules.pedidos.models import OrdemReservaLinx as O; assert all(O.__table__.columns[f'e{i}'].server_default is not None for i in range(1,49)); print('OK')"`)
    - PK é `["id"]`; existe a constraint `uq_ordens_reserva_linx_chave`; `OrdemReservaLinx.__table__.foreign_keys` é vazio; `OrdemReservaLinx.__table__.indexes` é vazio
    - `grep -c "^    e" app/modules/pedidos/models.py` retorna pelo menos 48 (as colunas `e1..e48` declaradas linha a linha, não geradas por loop)
    - `grep -vE '^\s*#' app/modules/pedidos/models.py | grep -cE 'ForeignKey|for i in range|exec\('` retorna `0` (grep sai com código 1 quando o count é 0 — resultado esperado, não falha)
    - `uv run pytest -q` continua verde
  </acceptance_criteria>

  <done>A classe `OrdemReservaLinx` existe no módulo `pedidos` com 82 colunas (5 de controle NOT NULL + 77 do layout Linx nullable, `e1..e48` com default 0), unique `uq_ordens_reserva_linx_chave`, sem FK e sem índice extra, no estilo `Column` do arquivo.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar os 2 models em alembic/env.py e em test_schema_guard.py</name>

  <files>alembic/env.py, app/tests/test_schema_guard.py</files>

  <read_first>
    - `alembic/env.py` — arquivo que será editado; bloco de imports explícitos nas linhas 10-27 que popula `target_metadata = Base.metadata`
    - `app/tests/test_schema_guard.py` — arquivo que será editado; bloco de imports nas linhas 23-36 e o `@pytest.mark.parametrize("nome_tabela", sorted(Base.metadata.tables))` da linha 49, que é o motivo pelo qual um model não importado simplesmente não é testado
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-PATTERNS.md` — seções "alembic/env.py (edição de bloco de imports existente)" e "app/tests/test_schema_guard.py (edição de bloco de imports existente)", com o antes/depois exato
    - `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-RESEARCH.md` — Pitfall 1 (por que esquecer um dos dois pontos dá falso verde)
  </read_first>

  <action>
    Em `alembic/env.py`: adicionar `ProdutoTamanhoPosicao` ao import `from app.modules.ingestao.models import (...)` (ordem alfabética, depois de `PedidoProcessadoErp`) e `OrdemReservaLinx` ao import `from app.modules.pedidos.models import (...)` (ordem alfabética, depois de `OrdemReserva`). Manter os comentários `# noqa: F401` existentes. Nenhuma outra linha do arquivo muda.

    Em `app/tests/test_schema_guard.py`: aplicar exatamente as mesmas duas adições nos mesmos dois blocos de import (linhas 25-36). Nenhuma outra linha do arquivo muda — o teste já é parametrizado sobre `sorted(Base.metadata.tables)` e passa a gerar 2 casos novos por função de teste automaticamente.

    Por que os dois lugares e não só um: `env.py` alimenta o `target_metadata` que o Alembic usa para detectar diffs pendentes (critério 4 do ROADMAP); `test_schema_guard.py` alimenta a parametrização do guard contra drift model↔banco. Esquecer um dos dois não quebra nada visivelmente — só deixa a guarda cega, que é precisamente o incidente da migration 012 perdida (ver docstring do próprio `test_schema_guard.py`).

    Neste momento as tabelas ainda NÃO existem no banco (a migration é o Plano 02): é esperado que os 4 casos novos do guard entrem em `skip` com a mensagem "não existe no banco (migration pendente?)". Isso é o comportamento correto agora, não uma falha.
  </action>

  <verify>
    <automated>uv run pytest app/tests/test_schema_guard.py --collect-only -q -k "produto_tamanho_posicao or ordens_reserva_linx"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest app/tests/test_schema_guard.py --collect-only -q -k "produto_tamanho_posicao or ordens_reserva_linx"` coleta exatamente **4** testes (2 tabelas × 2 funções de teste) e sai com código 0
    - `grep -vE '^\s*#' alembic/env.py | grep -cE 'ProdutoTamanhoPosicao|OrdemReservaLinx'` retorna `2`
    - `grep -vE '^\s*#' app/tests/test_schema_guard.py | grep -cE 'ProdutoTamanhoPosicao|OrdemReservaLinx'` retorna `2`
    - `uv run pytest app/tests/test_schema_guard.py -q` sai com código 0, com 4 testes em `skipped` (as 2 tabelas novas ainda não existem no banco — esperado antes do Plano 02) e nenhum `failed`
    - `uv run pytest -q` (suíte completa) sai com código 0
    - `git diff --stat alembic/env.py app/tests/test_schema_guard.py` mostra no máximo 2 linhas adicionadas por arquivo
    - Verificação cruzada opcional (requer a stack de pé): `docker compose -f .docker/docker-compose.yml exec api uv run alembic check 2>&1 | grep -cE 'produto_tamanho_posicao|ordens_reserva_linx'` retorna ≥ 1 — o Alembic agora VÊ os 2 models e reclama que faltam no banco, provando que `env.py` está correto. Esse "erro" é o resultado esperado neste plano e será zerado pelo Plano 02.
  </acceptance_criteria>

  <done>Os dois models novos estão importados em `alembic/env.py` e em `app/tests/test_schema_guard.py`; o guard coleta 4 casos novos (em skip, por enquanto) e a suíte completa segue verde.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Símbolos e arquivos criados/alterados por ESTE plano (o restante da fase está no Plano 02):

| Artefato | Tipo | Onde |
|----------|------|------|
| `ProdutoTamanhoPosicao` | classe ORM (Base) | `app/modules/ingestao/models.py` |
| `produto_tamanho_posicao` | nome de tabela (declarado, ainda não criado no banco) | `ProdutoTamanhoPosicao.__tablename__` |
| `uq_produto_tamanho_posicao_chave` | UniqueConstraint (`cd_prod_cor`, `sg_tamanho`) | `app/modules/ingestao/models.py` |
| `OrdemReservaLinx` | classe ORM (Base), 82 colunas | `app/modules/pedidos/models.py` |
| `ordens_reserva_linx` | nome de tabela (declarado, ainda não criado no banco) | `OrdemReservaLinx.__tablename__` |
| `uq_ordens_reserva_linx_chave` | UniqueConstraint (`nr_pedido`, `cd_prod_cor`) | `app/modules/pedidos/models.py` |
| imports `ProdutoTamanhoPosicao` / `OrdemReservaLinx` | registro em `target_metadata` | `alembic/env.py` |
| imports `ProdutoTamanhoPosicao` / `OrdemReservaLinx` | cobertura do guard de drift | `app/tests/test_schema_guard.py` |

Artefato do Plano 02 (não deste): `alembic/versions/015_schema_linx_referencia_posicao.py`.
</artifacts_this_phase_produces>

<source_coverage_audit>
Auditoria das 4 fontes para a Fase 1 inteira (Planos 01 e 02). Nenhum item MISSING.

**GOAL (ROADMAP — goal + 4 success criteria da Fase 1)**

| Item | Plano | Status |
|------|-------|--------|
| G1 `produto_tamanho_posicao` criada com `cd_prod_cor`, `sg_tamanho`, `nr_posicao` | 01-01 T1 (model) + 01-02 T1/T2 (DDL) | COVERED |
| G2 `ordens_reserva_linx` criada com o layout do CSV + colunas de controle | 01-01 T2 (model) + 01-02 T1/T2 (DDL) | COVERED |
| G3 `SELECT COUNT(*)` = 0 nas duas logo após a migration (sem backfill) | 01-02 T2 | COVERED |
| G4 `downgrade -1` remove as duas e `upgrade head` recria | 01-02 T3 | COVERED |
| G5 Nenhuma coluna de tabela existente alterada; autogenerate sem diffs pendentes | 01-02 T1 (por construção) + T3 (verificação) | COVERED |
| G6 Migration manual (sem `--autogenerate`), padrão simples da 013 | 01-02 T1 | COVERED |

**REQ (REQUIREMENTS.md — phase_req_ids)**

| Item | Plano | Status |
|------|-------|--------|
| LINX-01 tabela `ordens_reserva_linx` no layout Linx, campos sem fonte vazios | 01-01 T2, 01-02 T1/T2 | COVERED |
| LINX-04 tabela Linx começa vazia (sem backfill) | 01-02 T1 (nenhum INSERT no `upgrade`) + T2 (COUNT=0) | COVERED |

**RESEARCH (01-RESEARCH.md + 01-PATTERNS.md)**

| Item | Plano | Status |
|------|-------|--------|
| R1 `create_table` simples (padrão 013), não copiar o condicional da 014 | 01-02 T1 | COVERED |
| R2 Estilo por arquivo: `Mapped` em ingestao, `Column` em pedidos | 01-01 T1/T2 | COVERED |
| R3 Import explícito nos 2 pontos (`env.py`, `test_schema_guard.py`) — Pitfall 1 | 01-01 T3 | COVERED |
| R4 Sem `ForeignKeyConstraint` (Pitfall 4) | 01-01 T2, 01-02 T1 | COVERED |
| R5 `e1..e48` explícitas no model; comprehension só na migration | 01-01 T2, 01-02 T1 | COVERED |
| R6 Nenhuma dependência nova; Package Legitimacy Audit N/A | ambos (nenhum `uv add`) | COVERED |
| R7 Open Question 1 — `UniqueConstraint(nr_pedido, cd_prod_cor)` nesta fase | 01-01 decisão travada 5 | COVERED (decidido: sim) |
| R8 Open Question 2 — `id` + `synced_at` em `produto_tamanho_posicao` | 01-01 decisão travada 6 | COVERED (decidido: sim) |
| R9 Nota "default 0 de e1..e48" (pesquisa sugeria adiar) | 01-01 decisão travada 4 | COVERED (decisão travada do projeto prevalece: `server_default="0"`) |
| R10 Open Question 3 — tentar `backstage_get_coding_standards` antes de editar | 01-01 T1, 01-02 T1 | COVERED |
| R11 Validation Architecture / sampling rate | `01-VALIDATION.md` (Per-Task Verification Map) + `<verify>` de cada task | COVERED |
| R12 Nota: `alembic check` como diff-check sem gerar arquivo (fallback: `revision --autogenerate` descartado) | 01-02 T3 | COVERED |

**CONTEXT (PROJECT.md Key Decisions + decisões travadas do orquestrador; não há CONTEXT.md com IDs D-NN nesta fase)**

| Item | Plano | Status |
|------|-------|--------|
| C1 Migration 015 manual, padrão 013 | 01-02 T1 | COVERED |
| C2 `produto_tamanho_posicao` em ingestao (Mapped) | 01-01 T1 | COVERED |
| C3 `ordens_reserva_linx` em pedidos (Column) | 01-01 T2 | COVERED |
| C4 Ambas nascem vazias, sem backfill | 01-02 T1/T2 | COVERED |
| C5 Layout nullable, controle NOT NULL | 01-01 T2, 01-02 T1 | COVERED |
| C6 `e1..e48` Integer default 0 | 01-01 T2, 01-02 T1 | COVERED |
| C7 Nenhuma tabela existente tocada | 01-02 T1/T3 | COVERED |
| C8 Imports em `env.py` e `test_schema_guard.py` | 01-01 T3 | COVERED |
| C9 `UniqueConstraint(nr_pedido, cd_prod_cor)` já nesta fase | 01-01 T2, 01-02 T1 | COVERED |
| C10 `synced_at` em `produto_tamanho_posicao` | 01-01 T1 | COVERED |

**Exclusões (não são gaps):** INTG-01/02/03 e UI-01 são Future Requirements (REQUIREMENTS.md); ING-01 e LINX-03 são da Fase 2; LINX-02 é da Fase 3; ingestão de dados e escrita da linha Linx estão explicitamente fora do escopo desta fase (01-RESEARCH.md, Architectural Responsibility Map).
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova nesta fase) | Este plano só declara classes Python — nenhum endpoint HTTP, nenhuma entrada de usuário, nenhum dado atravessa fronteira. As colunas declaradas só recebem dados na Fase 3. |
| desenvolvedor → repositório | Único vetor real: código adicionado ao repo pode desalinhar model e banco (drift), que é o que o `test_schema_guard.py` existe para pegar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `alembic/env.py` / `app/tests/test_schema_guard.py` (drift model↔banco silencioso) | mitigate | Task 3 adiciona os 2 imports nos 2 pontos; verificado por comando (`--collect-only` deve coletar 4 testes e os greps devem retornar 2). É a mitigação da classe de bug documentada na docstring do próprio guard (incidente de 2026-07-23). |
| T-01-02 | Information Disclosure | `OrdemReservaLinx.nome_clifor` / `representante` (dados de cliente numa tabela nova) | accept | Nesta fase a tabela é apenas uma declaração e nasce vazia — não há dado para expor, e nenhum endpoint lê a tabela. A decisão de controle de acesso (RBAC equivalente ao de `ordens_reserva`) pertence à Fase 3, quando a escrita real acontece. Registrado explicitamente para não se perder. |
| T-01-03 | Tampering | `ordens_reserva_linx` sem `ForeignKeyConstraint` para `ordens_reserva` | accept | Ausência de FK é decisão de design travada (bandeja de saída desacoplada, 01-RESEARCH.md Pitfall 4). Consequência aceita: linhas órfãs são possíveis. Mitigação de integridade fica na camada de aplicação da Fase 3 (escrita na mesma transação) + `uq_ordens_reserva_linx_chave` contra duplicatas. |
| T-01-SC | Tampering | supply chain (npm/pip/cargo) | N/A | Nenhum pacote é instalado nesta fase (01-RESEARCH.md, "Package Legitimacy Audit: não aplicável"). Se qualquer `uv add` aparecer durante a execução, PARE: é sinal de desvio de escopo. |
</threat_model>

<verification>
Após as 3 tasks, no diretório raiz do backend (`backend`):

1. `uv run pytest -q` — suíte completa verde (os 4 casos novos do schema guard em `skipped`, porque a migration é o Plano 02).
2. `uv run pytest app/tests/test_schema_guard.py --collect-only -q -k "produto_tamanho_posicao or ordens_reserva_linx"` — coleta 4 testes.
3. `uv run python -c "from app.modules.ingestao.models import ProdutoTamanhoPosicao; from app.modules.pedidos.models import OrdemReservaLinx; from app.shared.database.base import Base; t=set(Base.metadata.tables); assert {'produto_tamanho_posicao','ordens_reserva_linx'} <= t; print(len(OrdemReservaLinx.__table__.columns), len(ProdutoTamanhoPosicao.__table__.columns))"` — imprime `82 5`.
4. `git diff --stat` — apenas os 4 arquivos de `files_modified`, somente adições nos 2 arquivos de import.

Processo (`.claude/CLAUDE.md`): trabalhar em branch `feat/` a partir de `develop`; commits Conventional Commits, escopo sugerido `feat(schema): ...`. Nunca commitar em `main`.
</verification>

<success_criteria>
- `ProdutoTamanhoPosicao` declarada em `app/modules/ingestao/models.py` com 5 colunas NOT NULL e `uq_produto_tamanho_posicao_chave`
- `OrdemReservaLinx` declarada em `app/modules/pedidos/models.py` com 82 colunas (5 controle NOT NULL + 77 layout nullable, `e1..e48` com `server_default="0"`), `uq_ordens_reserva_linx_chave`, sem FK e sem índice extra
- Ambas importadas em `alembic/env.py` e em `app/tests/test_schema_guard.py`
- `uv run pytest -q` verde; nenhum model existente alterado; nenhuma dependência nova
</success_criteria>

<output>
Criar `.planning/phases/01-schema-linx-e-refer-ncia-de-posi-o/01-01-SUMMARY.md` ao terminar, registrando: (a) se o MCP `backstage_get_coding_standards` estava disponível, (b) a contagem final de colunas de cada tabela, (c) confirmação de que as tabelas ainda não existem no banco (guard em skip) — o Plano 02 depende dessa informação.
</output>
