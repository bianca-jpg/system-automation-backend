---
phase: 05-auth-otp-seed-jwt-fail-fast-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/shared/config/settings.py
  - .env.example
  - app/tests/test_settings_seed.py
  - docs/seguranca.md
  - docs/index.md
  - app/tests/test_docs_seguranca.py
autonomous: true
requirements: [SEC-02, SEC-04]
user_setup: []

must_haves:
  truths:
    - "Uma instalação nova, sem SEED_AUTH_ON_STARTUP definido no ambiente, sobe SEM criar os usuários seed"
    - "Boot com ENV=PROD e SEED_AUTH_ON_STARTUP=true continua falhando com a mensagem existente"
    - ".env.example não induz mais o desenvolvedor a ligar o seed por padrão"
    - "Existe um runbook que lista os 5 usuários seed e diz como detectá-los, rotacioná-los e removê-los"
    - "O runbook quebra o build se alguém adicionar/remover um usuário seed sem atualizar a documentação"
  artifacts:
    - path: "app/shared/config/settings.py"
      provides: "default seguro de seed_auth_on_startup"
      contains: "default=False, validation_alias=\"SEED_AUTH_ON_STARTUP\""
    - path: "docs/seguranca.md"
      provides: "runbook SEC-04 de usuários seed"
      min_lines: 60
    - path: "app/tests/test_settings_seed.py"
      provides: "prova automatizada do default e do fail-fast PROD"
    - path: "app/tests/test_docs_seguranca.py"
      provides: "guarda de drift entre DEFAULT_AUTH_USERS e o runbook"
  key_links:
    - from: "app/tests/test_docs_seguranca.py"
      to: "app/modules/auth/bootstrap/seed.py::DEFAULT_AUTH_USERS"
      via: "import direto da tupla + assert de presença no markdown"
      pattern: "DEFAULT_AUTH_USERS"
    - from: "docs/index.md"
      to: "docs/seguranca.md"
      via: "linha na tabela de documentação"
      pattern: "\\(seguranca\\.md\\)"
---

<objective>
Fechar SEC-02 (default seguro para o seed de auth) e SEC-04 (runbook dos usuários seed legados).

Hoje `SEED_AUTH_ON_STARTUP` tem `default=True` em `app/shared/config/settings.py:432-434`: qualquer ambiente que não seja PROD sobe com 5 contas de senha pública (`basico123`, `oper123`, `gestor123`, `admin123`, `admintec123`) — inclusive staging, homologação e a máquina de qualquer pessoa que clone o repositório. A metade "não roda em PROD" já existe (`settings.py:594-595`) e **não deve ser tocada**. Falta virar o default e documentar o que fazer com as contas que já foram criadas nos ambientes onde o seed já rodou.

Purpose: eliminar credenciais conhecidas como caminho de acesso default e dar um procedimento executável para limpar o passivo já existente.
Output: default `False`, `.env.example` coerente, `docs/seguranca.md` (novo runbook), dois arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-auth-otp-seed-jwt-fail-fast-security/05-RESEARCH.md
@.planning/phases/05-auth-otp-seed-jwt-fail-fast-security/05-PATTERNS.md
@.planning/phases/05-auth-otp-seed-jwt-fail-fast-security/05-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Virar o default de SEED_AUTH_ON_STARTUP para False e provar em teste</name>
  <files>app/shared/config/settings.py, .env.example, app/tests/test_settings_seed.py</files>
  <read_first>
    - app/shared/config/settings.py (ler linhas 1-60 para `ENV`/`model_config`, 425-440 para o campo `seed_auth_on_startup`, e 505-600 para `validate_cross_field_constraints`)
    - .env.example (linhas 150-165, bloco "Inicialização")
    - app/tests/conftest.py (ver que `os.environ.setdefault("SEED_AUTH_ON_STARTUP", "false")` já existe — o teste precisa remover essa env var para medir o default de verdade)
    - app/bootstrap.py (confirmar que `lifespan` só chama `run_auth_seed()` sob `settings.seed_auth_on_startup`)
  </read_first>
  <action>
Em `app/shared/config/settings.py`, no campo `seed_auth_on_startup` (hoje em 432-434), trocar `default=True` por `default=False`, mantendo `validation_alias="SEED_AUTH_ON_STARTUP"`. Adicionar comentário curto acima do campo, em português, no estilo dos comentários já existentes no arquivo, explicando que o seed cria contas com senha conhecida e por isso é opt-in explícito (ver `docs/seguranca.md`). NÃO tocar no bloco `if ENV == "PROD": ... if self.seed_auth_on_startup: raise ValueError("SEED_AUTH_ON_STARTUP deve ser false em PROD")` — esse é o SEC-02 já implementado.

Em `.env.example` linha 159, trocar `SEED_AUTH_ON_STARTUP=true` por `SEED_AUTH_ON_STARTUP=false` e ajustar o comentário logo acima (linha 158) para dizer que ligar o seed é opt-in de desenvolvimento local, proibido em PROD, e apontar `docs/seguranca.md`.

Criar `app/tests/test_settings_seed.py` com três testes, todos construindo `Settings` diretamente (não `get_settings()`, que é `@lru_cache`) e sempre passando `_env_file=None` para não herdar um `.env` local:
  1. `test_seed_auth_desligado_por_padrao`: `monkeypatch.delenv("SEED_AUTH_ON_STARTUP", raising=False)`; `assert Settings(_env_file=None).seed_auth_on_startup is False`.
  2. `test_seed_auth_pode_ser_ligado_explicitamente`: `monkeypatch.setenv("SEED_AUTH_ON_STARTUP", "true")`; `assert Settings(_env_file=None).seed_auth_on_startup is True` (com ENV não-PROD, o valor default do processo de teste).
  3. `test_seed_auth_ligado_em_prod_falha_o_boot`: `monkeypatch.setattr("app.shared.config.settings.ENV", "PROD")` e, via `monkeypatch.setenv`, fornecer valores que passem nas OUTRAS validações PROD do mesmo validador antes de chegar na checagem de seed — `DATABASE_URL` apontando para host remoto com usuário e senha diferentes de `automation` (ex.: `postgresql+asyncpg://automation_app:senha-remota-forte@db.interno.example:5432/system_automation`), `REDIS_URL`/`CELERY_BROKER_URL`/`CELERY_RESULT_BACKEND` apontando para um host remoto (ex.: `redis://cache.interno.example:6379/0`), `JWT_SECRET` com 40 caracteres, e `SEED_AUTH_ON_STARTUP=true`. Envolver em `pytest.raises(ValidationError)` (de `pydantic`) e assertar que `"SEED_AUTH_ON_STARTUP deve ser false em PROD"` aparece em `str(exc.value)`.

Convenções: nomes de teste em português como no resto de `app/tests/`, sem docstring obrigatória, imports na ordem stdlib → terceiros → `app.*`.
  </action>
  <verify>
    <automated>uv run pytest app/tests/test_settings_seed.py -q</automated>
    <automated>uv run pytest app/tests/test_auth_flows.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "default=False, validation_alias=\"SEED_AUTH_ON_STARTUP\"" app/shared/config/settings.py` casa (ou o par `default=False` + `validation_alias="SEED_AUTH_ON_STARTUP"` aparece no mesmo `Field(...)`)
    - `grep -c 'SEED_AUTH_ON_STARTUP=true' .env.example` retorna 0
    - `grep -c 'SEED_AUTH_ON_STARTUP=false' .env.example` retorna 1
    - `grep -c 'SEED_AUTH_ON_STARTUP deve ser false em PROD' app/shared/config/settings.py` retorna 1 (a validação PROD pré-existente continua intacta)
    - `uv run pytest app/tests/test_settings_seed.py -q` passa com 3 testes coletados
    - `uv run pytest app/tests/test_auth_flows.py -q` continua verde (nenhuma regressão de auth)
  </acceptance_criteria>
  <done>Instalação sem a variável definida não roda seed; ligar em PROD ainda explode no boot; ambos os comportamentos provados por teste automatizado.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever o runbook docs/seguranca.md e travá-lo contra drift</name>
  <files>docs/seguranca.md, docs/index.md, app/tests/test_docs_seguranca.py</files>
  <read_first>
    - app/modules/auth/bootstrap/seed.py (fonte de verdade dos 5 e-mails, senhas e papéis em `DEFAULT_AUTH_USERS`; comportamento idempotente de `seed_auth_users`)
    - docs/index.md (tabela "Documentação" — o runbook entra como uma linha nova)
    - docs/comunicacoes.md (tom, profundidade e estrutura de seção esperados num doc operacional deste repositório)
    - app/modules/parametros/infrastructure/models.py (linhas 54-70: `ParametroChangeRequest.requested_by` é FK NOT NULL para `auth_users.id` sem `ondelete` — é o que pode bloquear um DELETE)
    - app/modules/comunicacoes/infrastructure/models.py (linhas 28-42: FK `requested_by_user_id` com `ondelete="SET NULL"`)
    - app/modules/realtime/infrastructure/models.py (linhas 143-160: `realtime_read_cursors.user_id` com `ondelete="CASCADE"`)
    - app/tests/test_pipeline_safety.py (estilo de teste que assere conteúdo de arquivo, para espelhar no guard de drift)
  </read_first>
  <action>
Criar `docs/seguranca.md` em português, com estas seções (títulos exatos, `#` para o título e `##` para as seções):

  1. `# Segurança` + parágrafo de abertura dizendo que o doc cobre as contas seed de autenticação e os controles de auth do backend.
  2. `## Usuários seed: o que são` — explicar que `app/modules/auth/bootstrap/seed.py::seed_auth_users` insere 5 contas idempotentes por e-mail quando `SEED_AUTH_ON_STARTUP=true`, que as senhas estão em texto claro no repositório (portanto públicas), e que desde a Fase 5 o default é `false` e PROD recusa subir com `true` (`app/shared/config/settings.py`).
  3. `## As 5 contas` — tabela markdown com colunas `E-mail | Papel | Senha default`, uma linha por entrada de `DEFAULT_AUTH_USERS`, com os valores exatos: `basico@example.com`/`basico`/`basico123`, `operacional@example.com`/`operacional`/`oper123`, `gestor@example.com`/`gestor`/`gestor123`, `administrador@example.com`/`administrador`/`admin123`, `admintec@example.com`/`admin_tecnico`/`admintec123`.
  4. `## Detecção` — bloco SQL com `SELECT id, email, roles, confirmed_at, created_at FROM auth_users WHERE email IN ('basico@example.com', 'operacional@example.com', 'gestor@example.com', 'administrador@example.com', 'admintec@example.com') ORDER BY id;` e a instrução de rodar isso em cada ambiente compartilhado (dev, staging, produção) antes de decidir entre rotação e remoção.
  5. `## Rotação (ambiente que ainda depende das contas)` — procedimento numerado usando os endpoints reais: `POST /api/auth/password/recovery` com o e-mail da conta, ler o código OTP entregue por e-mail (a entrega em PROD passa a existir na Fase 5, plano 03; fora de PROD o código sai no log de `app/modules/auth/infrastructure/repositorio_otp.py`), e `POST /api/auth/password/recovery/confirm` com `email`, `confirmation_code` e `new_password`. Registrar o pré-requisito: a caixa postal `@example.com` precisa existir e ser acessível; se não existir, a rotação é impossível e o caminho correto é a remoção.
  6. `## Remoção (recomendado em ambientes compartilhados)` — procedimento numerado: (a) garantir antes um usuário administrativo real — registrar por `POST /api/auth/register` + `POST /api/auth/register/confirm` e promover com `PUT /api/auth/users/{id}/roles` usando um token de admin ainda válido; (b) confirmar que `SEED_AUTH_ON_STARTUP` está `false` no ambiente, senão o próximo boot recria tudo; (c) `DELETE FROM auth_users WHERE email IN (...)`. Documentar explicitamente o comportamento de FK: `comunicacoes.requested_by_user_id` tem `ON DELETE SET NULL` e `realtime_read_cursors.user_id` tem `ON DELETE CASCADE` (o delete passa), mas `parametro_change_requests.requested_by` é `NOT NULL` sem `ON DELETE` — se a conta seed já pediu mudança de parâmetro, o DELETE falha com violação de chave estrangeira e o caminho correto passa a ser a rotação de senha, não a remoção.
  7. `## Prevenção` — lista dos controles que impedem a reincidência, com o arquivo de cada um: default `False` em `app/shared/config/settings.py`, fail-fast de PROD no mesmo arquivo, `JWT_SECRET` forte obrigatório em PROD (SEC-01), e ponteiro para os demais controles desta fase (hash de OTP, entrega por e-mail, revogação de sign-out, rate limit).
  8. `## Checklist por ambiente` — checklist de caixas `- [ ]` cobrindo: rodar a query de detecção, decidir rotação ou remoção, executar, confirmar `SEED_AUTH_ON_STARTUP=false`, e reexecutar a detecção para confirmar o resultado.

Em `docs/index.md`, acrescentar na tabela "Documentação" a linha `| [Segurança](seguranca.md) | Usuários seed, rotação/remoção, hash de OTP, revogação de sessão e rate limit |`, mantendo o formato das linhas vizinhas.

Criar `app/tests/test_docs_seguranca.py` (guard de drift, sem banco, no estilo de `app/tests/test_pipeline_safety.py` — resolve o caminho via `Path(__file__).resolve().parents[2]`) com:
  - `test_runbook_cobre_todas_as_contas_seed`: importa `DEFAULT_AUTH_USERS` de `app.modules.auth.bootstrap.seed`, lê `docs/seguranca.md` e assere que o e-mail E a senha default de CADA entrada aparecem no texto (falha se alguém adicionar uma 6ª conta sem documentar).
  - `test_runbook_tem_secoes_obrigatorias`: assere a presença dos títulos `## Detecção`, `## Rotação`, `## Remoção`, `## Prevenção` (aceitar `startswith` das seções cujo título tem sufixo entre parênteses).
  - `test_index_linka_o_runbook`: lê `docs/index.md` e assere que `(seguranca.md)` aparece.
  </action>
  <verify>
    <automated>uv run pytest app/tests/test_docs_seguranca.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/seguranca.md` existe e contém os 8 títulos de seção descritos, incluindo literalmente `## Detecção`, `## Prevenção` e `## Checklist por ambiente`
    - Os 5 e-mails de `DEFAULT_AUTH_USERS` e suas 5 senhas default aparecem em `docs/seguranca.md`
    - `docs/seguranca.md` cita `parametro_change_requests` e a consequência do FK `NOT NULL` sobre o `DELETE`
    - `grep -c "(seguranca.md)" docs/index.md` retorna 1
    - `uv run pytest app/tests/test_docs_seguranca.py -q` passa com 3 testes coletados
    - Adicionar mentalmente uma 6ª conta a `DEFAULT_AUTH_USERS` faria `test_runbook_cobre_todas_as_contas_seed` falhar (o teste lê a tupla, não uma cópia literal)
  </acceptance_criteria>
  <done>Existe um procedimento executável para o passivo de contas seed, linkado no índice de docs, e o build falha se a documentação divergir do código.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → boot da aplicação | variáveis de ambiente controlam se contas privilegiadas são criadas automaticamente |
| internet → `POST /api/auth/sign-in` | credenciais públicas (repositório aberto) viram acesso autenticado direto |
| leitor do repositório → banco de qualquer ambiente | as senhas seed estão em texto claro no código-fonte |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | HIGH | `app/modules/auth/bootstrap/seed.py::DEFAULT_AUTH_USERS` + `settings.seed_auth_on_startup` | mitigate | Task 1: default `False` — nenhum ambiente cria `admintec@example.com`/`admintec123` sem opt-in explícito; fail-fast PROD pré-existente mantido intacto |
| T-05-12 | Elevation of Privilege | HIGH | contas seed já criadas em ambientes onde o seed rodou | mitigate | Task 2: runbook com query de detecção + procedimento de rotação e de remoção, incluindo o bloqueio de FK que decide entre os dois caminhos |
| T-05-13 | Information Disclosure | LOW | `.env.example` | mitigate | Task 1: exemplo deixa de sugerir `true`, evitando que setups novos reintroduzam o problema |
| T-05-14 | Repudiation | LOW | drift entre `seed.py` e o runbook | mitigate | Task 2: `test_docs_seguranca.py` importa `DEFAULT_AUTH_USERS` e quebra o build se uma conta nova não for documentada |
| T-05-SC | Tampering | LOW | instalação de pacotes | accept | Esta fase não instala nenhum pacote novo (RESEARCH.md § Package Legitimacy Audit: não aplicável); nenhum `uv add`/`pip install` faz parte deste plano |

Todos os threats HIGH acima têm critério de aceitação correspondente: T-05-05 → Task 1 (grep do `default=False` + teste do default); T-05-12 → Task 2 (seções obrigatórias + cobertura das 5 contas).
</threat_model>

## Artifacts this phase produces

Lista **da fase inteira** (os itens marcados `← plano 01` nascem neste plano). O passe de source-grounding deve tratar todos como símbolos novos, não como drift:

- `app/modules/auth/domain/otp_hash.py::hash_otp_code`, `::verify_otp_code`
- `app/modules/auth/infrastructure/otp_email.py::enviar_codigo_otp`, `::OTP_EMAIL_SUBJECT`
- `app/modules/auth/domain/exceptions.py::RevogacaoIndisponivelError`
- `app/modules/auth/application/casos_uso.py::_SIGNED_OUT_SINCE_KEY_TEMPLATE`, `::_SIGNED_OUT_TTL_SECONDS`, `::_chave_sign_out`
- `app/modules/auth/infrastructure/http/routes.py::_aplicar_rate_limit`, `::_SIGN_IN_USER_RATE_PER_MINUTE`, `::_SIGN_IN_IP_RATE_PER_MINUTE`, `::_OTP_CONFIRM_USER_RATE_PER_MINUTE`, `::_OTP_CONFIRM_IP_RATE_PER_MINUTE`, `::_OTP_REQUEST_USER_RATE_PER_MINUTE`, `::_OTP_REQUEST_IP_RATE_PER_MINUTE`, `::_RATE_LIMIT_UNAVAILABLE_RETRY_AFTER_SECONDS`
- `alembic/versions/029_auth_otp_code_hash.py` (revision `029`, down_revision `028`)
- `docs/seguranca.md` ← plano 01
- `app/tests/test_settings_seed.py` ← plano 01, `app/tests/test_docs_seguranca.py` ← plano 01
- `app/tests/test_auth_otp_hash.py`, `app/tests/test_migration_029_auth_otp_code_hash.py`, `app/tests/test_auth_otp_email.py`, `app/tests/test_auth_sessao_revogacao.py`, `app/tests/test_auth_rate_limit.py`
- `app/tests/conftest.py::_FakeAuthRateRedis`, `::auth_rate_limit_redis`
- `app/tests/test_auth_flows.py::otp_existe`, `::ler_otp_do_log` (substituem `ler_otp`/`_ler_otp_async`)
- Claim JWT novo `iat` em refresh tokens (`app/modules/auth/domain/tokens.py::create_refresh_token`)
- Chave Redis nova `auth:signed_out_since:{user_id}`
- Namespaces de rate limit novos: `auth_sign_in`, `auth_otp_request`, `auth_otp_confirm`

<verification>
- `uv run pytest app/tests/test_settings_seed.py app/tests/test_docs_seguranca.py -q` — verde
- `uv run pytest -q` — suíte completa verde (este plano mexe em `Settings`, que outros módulos leem)
- `grep -c 'SEED_AUTH_ON_STARTUP=true' .env.example` → 0
- Revisão humana: `docs/seguranca.md` descreve procedimentos que uma pessoa consegue executar sem perguntar nada a mais
</verification>

<success_criteria>
- SEC-02 fechado: `seed_auth_on_startup` default `False`, `.env.example` coerente, fail-fast PROD preservado, tudo provado por `app/tests/test_settings_seed.py`
- SEC-04 fechado: `docs/seguranca.md` existe, cobre as 5 contas, ensina detecção/rotação/remoção/prevenção, está linkado em `docs/index.md` e é protegido contra drift por `app/tests/test_docs_seguranca.py`
- Suíte completa (`uv run pytest -q`) verde
</success_criteria>

<output>
Criar `.planning/phases/05-auth-otp-seed-jwt-fail-fast-security/05-01-SUMMARY.md` ao concluir.
</output>
