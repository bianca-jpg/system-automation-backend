---
quick_id: 260831-cf1
status: planned
---

# Quick Task 260831-cf1: Front consome `tolerancia_adequacao` real (CONTRACT-01)

## Contexto

`CONTRACT-01`: front não consumia `GET /api/v1/parametros` para o fator de
adequação — `widgets/pedido-dashboard/ui/orders-list.tsx` (repo
`frontend`) hardcodava `1.05` em dois pontos (linhas 735 e 741)
para projetar o faturamento "com adequação" na fila de Aguardando.

**Decisão da mantenedora (2026-08-31):** corrigir agora, mesmo com a Fase 8
(que endereçaria isso formalmente) marcada como dependente da Fase 11
(fluxo de solicitação/aprovação de parâmetros, em andamento paralelo pela
Victoria). Justificativa: Fase 11 trata da escrita (`PUT`/change-request);
este fix só precisa da leitura (`GET /api/v1/parametros`), que já funciona
independente do estado da Fase 11.

`order-detail-modal.tsx`, citado no mesmo achado original (STATE.md
2026-08-14), não existe mais na árvore principal do front — só sobrevive
numa worktree órfã (`.claude/worktrees/fervent-goodall-a7cbaa/`), sinal de
que já foi refatorado/substituído por `product-grade-detail-modal.tsx`, que
não tem o hardcode. Achado já não procede para esse arquivo.

## Tasks

### Task 1 — Hook de leitura

- Novo `features/pedidos/model/use-tolerancia-adequacao.ts`:
  `useToleranciaAdequacaoFator()` via `useQuery` (`@tanstack/react-query`),
  chama `fetchParametros({ search: "tolerancia_adequacao", pageSize: 1 })`
  (já existente em `entities/parametro/api/parametros.api.ts`), filtra por
  `row.name === "tolerancia_adequacao"` (o `search` do backend é substring,
  não chave exata) e devolve `1 + tolerancia`. Fallback `1.05` (mesmo default
  do backend, `Settings.tolerancia_adequacao`) enquanto carrega ou se falhar.

### Task 2 — Consumo em orders-list.tsx

- Trocar os dois `* 1.05` (linhas 735/741) por `* fatorAdequacao`, vindo do
  hook novo.

### Task 3 — Testes

- `use-tolerancia-adequacao.test.tsx`: fallback antes do fetch resolver,
  valor real (0.1 → 1.1), fallback se a chave não vier, fallback se a API
  falhar.
- Os 4 arquivos de teste que renderizam `OrdersList`
  (`orders-list.test.tsx`, `.pagination.test.tsx`, `.processing.test.tsx`,
  `orders-list-skeleton.test.tsx`) precisam mockar
  `useToleranciaAdequacaoFator` retornando `1.05` — sem isso, `useQuery`
  levanta "No QueryClient set" (nenhum desses testes envolve
  `QueryClientProvider` hoje).
- Verify: `pnpm typecheck` + `pnpm test` (suíte completa).
