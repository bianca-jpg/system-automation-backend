"""Adaptadores de infraestrutura do bounded context Realtime.

Importe sempre o submódulo desejado (``.models``, ``.repository``,
``.redis_gateway``, ``.connection_manager``, ``.runtime``). O registro das
tabelas no ``Base.metadata`` para o Alembic é feito por ``alembic/env.py``.
"""
